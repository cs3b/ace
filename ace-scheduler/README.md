# ace-scheduler

Local scheduler for ACE workflows. Provides the `ace-scheduler` CLI for scheduled tasks and event triggers.

## Commands

- `ace-scheduler --help`
- `ace-scheduler --version`

## Configuration

Defaults: `.ace-defaults/scheduler/config.yml`
Project: `.ace/scheduler/config.yml`
