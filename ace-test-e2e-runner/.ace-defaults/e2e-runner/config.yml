# Default configuration for e2e test runner
# This file provides defaults for the ace-test-e2e-runner gem

paths:
  # Where test scenarios are stored in packages
  scenarios: "test/e2e"
  # File extension for test scenarios
  extension: ".mt.md"
  # Directory for test execution artifacts (gitignored)
  cache_dir: ".cache/ace-test-e2e"

patterns:
  # Glob pattern for finding test scenarios
  discovery: "test/e2e/**/*.mt.md"

# Test scenario frontmatter requirements
required_fields:
  - test-id
  - title
  - area

# Optional fields with defaults
optional_fields:
  priority: medium
  duration: "~5min"
  automation-candidate: false

# Cleanup behavior
cleanup:
  # Whether to delete test artifacts after test completion
  # Set to false to keep artifacts for debugging
  enabled: false

# Test ID format validation
test_id:
  # Pattern: MT-{AREA}-{NNN}
  pattern: "^MT-[A-Z]+-\\d{3}$"
  # Known area codes
  areas:
    - LINT
    - REVIEW
    - BUILD
    - GIT
    - SEARCH
    - BUNDLE
