#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative "../lib/ace/docs"
require_relative "../lib/ace/docs/cli"

# Start CLI with exit status code
# The CLI.start method now handles all routing and returns an exit code
result = Ace::Docs::CLI.start(ARGV)
exit(result.is_a?(Integer) ? result : 0)
