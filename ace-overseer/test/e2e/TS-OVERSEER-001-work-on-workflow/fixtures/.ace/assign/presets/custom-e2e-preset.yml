name: custom-e2e-preset
description: Custom preset for E2E testing

parameters:
  taskref:
    required: true
    description: Task reference

steps:
  - name: quick-fix
    instructions:
      - "Quick fix for task {{taskref}}."
