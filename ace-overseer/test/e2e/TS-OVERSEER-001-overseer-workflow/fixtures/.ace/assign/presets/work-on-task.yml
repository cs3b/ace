name: work-on-task
description: E2E test default preset

parameters:
  taskref:
    required: true
    description: Task reference

steps:
  - name: implement
    instructions:
      - "Work on task {{taskref}}."
