#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative "../lib/ace/overseer"
require "dry/cli"

args = ARGV.empty? ? ["--help"] : ARGV

begin
  Dry::CLI.new(Ace::Overseer::CLI).call(arguments: args)
rescue Ace::Core::CLI::Error => e
  warn e.message
  exit(e.exit_code)
end
