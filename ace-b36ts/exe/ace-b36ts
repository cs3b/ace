#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative "../lib/ace/b36ts"
require "dry/cli"

args = ARGV.empty? ? ["--help"] : ARGV

begin
  Dry::CLI.new(Ace::B36ts::CLI).call(arguments: args)
rescue Ace::Core::CLI::Error => e
  warn e.message
  exit(e.exit_code)
end
