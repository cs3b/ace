#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative "../lib/ace/prompt"
require_relative "../lib/ace/prompt/cli"

begin
  Ace::Prompt::CLI.start(ARGV)
rescue => e
  warn "Error: #{e.message}"
  warn e.backtrace.join("\n") if ENV["DEBUG"]
  exit 1
end
