#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative "../lib/ace/coworker"

# Start CLI with default command routing and exit with status code
# The routing logic is now in CLI.start for testability
exit_code = Ace::Coworker::CLI.start(ARGV)
exit(exit_code.is_a?(Integer) ? exit_code : 0)
