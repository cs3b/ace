---
id: 8r9.t.i05.r
status: draft
priority: high
created_at: '2026-04-10 12:00:48'
estimate: small
dependencies:
- 8r9.t.i05.0
tags:
- testing
- migration
- fast
- feat
- e2e
parent: 8r9.t.i05
bundle:
  presets:
  - project
  files:
  - ace-monorepo-e2e/README.md
  - ".ace-tasks/8r9.t.hzr-pilot-restarted-e2e-architecture-on/8r9.t.hzr-pilot-restarted-e2e-architecture-on-ace-b36ts.s.md"
  commands:
  - ace-task show 8r9.t.i05.r --content
  - ace-task show 8r9.t.hzr --content
---

# Simplify ace-monorepo-e2e to the fast/feat/e2e testing model

## Objective

Rebaseline ace-monorepo-e2e on the new public testing taxonomy without inventing deterministic layers that the package does not currently need.

## User Experience

- Operators continue to run scenario coverage through `ace-test-e2e ace-monorepo-e2e` without being told to create placeholder deterministic layers.
- Reviewers can see that `fast` and `feat` are inactive for this package today rather than accidentally omitted.
- Future deterministic coverage remains possible, but this task does not require inventing it.

## Expected Behavior

1. Keep the package scenario-only if that remains the correct scope.
2. Update docs, workflow text, and task references so the public contract is `fast`, `feat`, and `e2e`.
3. Remove any stale references to `test-e2e/scenarios`, `ace-test <pkg> e2e`, or deterministic `test/e2e/*_test.rb` migration.
4. Explicitly document that no `fast` or `feat` layer is required unless deterministic coverage is introduced later.

## Package Migration Shape

- `test/e2e/` remains the only active test directory unless later work introduces deterministic coverage.
- No placeholder `test/fast/` or `test/feat/` directory should be created unless needed by actual tests.

## Interface Contract

- `ace-test-e2e ace-monorepo-e2e`
- `ace-test ace-monorepo-e2e` remains out of scope until the package gains deterministic `fast` or `feat` coverage.

## Validation Questions

- None. The current package state is scenario-only, and this task intentionally preserves that shape.

## Vertical Slice Decomposition

- This task is one standalone scenario-only migration slice for `ace-monorepo-e2e`. It ships independently once docs and task references match the new taxonomy.

## Verification

- `ace-test-e2e ace-monorepo-e2e`

## Success Criteria

- [ ] ace-monorepo-e2e keeps only the scenario coverage it actually needs.
- [ ] No repo task or docs reference still tells users to run `ace-test ace-monorepo-e2e e2e`.
- [ ] The package contract is described in terms of `fast`, `feat`, and `e2e`, with `e2e` as the only active layer for now.
