---
id: 8c0.t.07c.e
title: Update gemspec and package metadata for naming
status: progress
priority: medium
created_at: "2025-01-01 00:08:08"
estimate: large
dependencies: [8c0.t.07c.d]
tags: [migrated]
parent: 8c0.t.07c
bundle:
  presets: [project]
  files: [docs/architecture.md]
  commands: [rg --files -g '*.gemspec']
needs_review: false
---

# Update gemspec and package metadata for naming

## Behavioral Specification

### User Experience
- **Input**: Canonical naming policy and docs terminology matrix
- **Process**: Maintainer receives a package-level metadata checklist across all relevant gems
- **Output**: Gemspec summaries/descriptions and package readme metadata remain consistent with ACE/ADE positioning

### Expected Behavior
This subtask defines:
- Which `*.gemspec` fields need updates (summary, description, homepage where relevant)
- Which package `README.md` descriptors must align to the naming policy
- Compatibility checks to ensure no accidental changes to gem names, executable names, or require paths

### Interface Contract
```text
Per-package metadata checklist:
- Package:
- Gemspec fields to update:
- README sections to update:
- Must-not-change identifiers (gem name/bin/module):
```

## Acceptance Criteria

- [x] Metadata update plan distinguishes copy changes from identity changes
- [x] Plan explicitly preserves backward-compatible gem/binary identifiers
- [x] Verification checklist includes scan for stale `ace-meta` references after updates

### Defaults
- **Gemspec fields in scope**: `summary`, `description`, `homepage` — gem `name`, `executables`, and `require_paths` are explicitly do-not-change
- **Package README scope**: Only the first paragraph (project description line) of each per-package README
- **Coverage**: All 42 gemspec files and their corresponding README.md files
- **Module/class names**: `Ace::*` namespace is unchanged (not a rename target)

## Verification Plan

### Coverage Check
- Alignment matrix lists all 42 gemspecs with explicit update/no-change status for each field
- Package README matrix covers all per-package READMEs identified in subtask a
- Must-not-change identifiers are listed per package (gem name, binary name, module namespace)

### Consistency Check
- All updated `summary` and `description` fields use the canonical terminology from subtask b
- No gemspec contains both old and new terminology after updates

### Safety Check
- Verify that `spec.name`, `spec.executables`, and `require` paths are explicitly marked do-not-change
- Verification checklist includes `bundle exec rake test` equivalent (`ace-test`) for at least a sample of updated gems
- Post-update scan command (`rg "ace-meta" --glob "*.gemspec"`) is defined to catch stale references

## Deliverables

- [x] Gemspec metadata alignment matrix
- [x] Package README alignment matrix
- [x] Post-update verification checklist for naming consistency and compatibility

## Results

### Gemspec Updates Applied

All 42 gemspec files updated:
- `spec.homepage`: `cs3b/ace-meta` → `cs3b/ace` (42 files)
- `metadata["source_code_uri"]`: updated in 3 files (ace-git, ace-support-config, ace-test-runner-e2e)
- `metadata["changelog_uri"]`: updated in 3 files (same)
- Stale org references fixed: `ace-ecosystem/ace-meta` → `cs3b/ace`, `ace-meta/ace-core` → `cs3b/ace`, `ace-meta/ace-test-runner` → `cs3b/ace`

**Unchanged (identity fields)**: `spec.name`, `spec.executables`, `require_paths`, `Ace::*` namespaces — all preserved.

### Package README Updates Applied

10 package READMEs updated: ace-bundle, ace-git, ace-git-commit, ace-git-worktree, ace-prompt-prep, ace-support-core, ace-support-nav, ace-support-test-helpers, ace-test, ace-test-runner.

### Additional Updates Applied

- 28 bin wrapper comments updated
- 2 source code files updated (task_fetcher.rb, message_generator.rb)
- 3 mise config comments updated
- 1 handbook guide updated

### Post-Update Verification

Scan `rg "ace-meta" --glob "*.gemspec"` returns **0 matches** — all gemspec references clean.

Full codebase scan for `ace-meta` (excluding historical records) shows only 3 intentional do-not-change references:
- `docs/decisions.md` — ADR-033 rename documentation
- `ace-test-runner/lib/ace/test_runner/atoms/result_parser.rb:211` — example path in comment
- `ace-docs/test/test_helper.rb:34` — file count comment
