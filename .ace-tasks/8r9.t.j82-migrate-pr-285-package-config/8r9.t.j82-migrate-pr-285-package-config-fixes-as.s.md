---
id: 8r9.t.j82
status: draft
priority: medium
created_at: "2026-04-10 12:48:58"
estimate: TBD
dependencies: []
tags: ["task-draft", "cherry-pick", "patches", "pr-285", "package-migration"]
bundle:
  presets: [project]
  files:
    - .ace-tasks/8r9.t.j82-migrate-pr-285-package-config/docs/
  commands: []
---

# Migrate PR 285 package config fixes as standalone patch set

## Objective
- Restore non-e2e package and `.ace` configuration fixes from PR #285 as a standalone, reproducible patch set that can be applied independently of the older umbrella task.

## User Experience
- The user gets one task that fully contains the curated patch bundle to cherry-pick onto the current branch.
- The task excludes e2e packages and retrospective scope by design, while preserving every relevant package/config patch from PR #285.

## Expected Behavior
- A `docs/` directory exists in this task with patch files covering every target package/config fix from PR #285.
- The patch set excludes:
  - E2E package changes
  - retrospective package changes
- The user can apply patches from this task directly on current branch without relying on the prior umbrella task.

## Interface Contract
- Inputs:
  - `PR #285` as source of historical package fixes.
  - Existing task artifacts in the repository.
- Outputs:
  - Patch files placed in `.ace-tasks/8r9.t.j82-migrate-pr-285-package-config/docs/`.
  - Task spec declaring the scope and constraints.
- Validation condition:
  - Applying the patch files succeeds against current branch when needed, and only includes `.ace` config + selected `ace-*` package fixes.

## Success Criteria
- [ ] A new independent task exists with id `8r9.t.j82`.
- [ ] Task `docs/` contains exactly the selected non-e2e, non-retro patch files.
- [ ] Task scope clearly documents exclusions and purpose.
- [ ] No implementation code changes are made in this task (spec-only activity).

## Scope
### Included
- `ace-*` package patch files for package/version and/or test/docs fixes selected from PR #285.
- `.ace` configuration patch (`.ace/test/suite.yml`).

### Excluded
- `ace-retro` package
- `ace-monorepo-e2e` package
- `ace-test-runner-e2e` package

## Deliverables
- [ ] Independent task spec file under `.ace-tasks/8r9.t.j82-migrate-pr-285-package-config/`.
- [ ] `docs/` folder containing patch files.
- [ ] Task instructions and list of included package/config artifacts.

## Verification Plan
- [ ] Confirm the task directory exists and is not a child of the prior umbrella task path.
- [ ] Confirm `docs/` exists and contains patch files only.
- [ ] Confirm no e2e or retrospective package files are present in this task docs set.
