---
id: 8c0.t.068
title: "233.05: Progressive Strategy (Context Carryover)"
status: pending
priority: medium
created_at: "2025-01-01 00:06:54"
estimate: medium
dependencies: [8c0.t.067.d]
tags: [migrated]
deferred: true
deferred_reason: No clear use case yet where sequential review with carryover provides significant value over parallel chunked reviews. Learnings from task 227 retro indicate we should avoid adding complexity before verifying need (YAGNI).
---

# 233.05: Progressive Strategy (Context Carryover)

## Goal

Sequential review with findings summary carried forward between chunks.

## Deliverables

1. `ace-review/lib/ace/review/molecules/strategies/progressive_strategy.rb`
2. Update synthesis to handle progressive flow
3. Unit tests

## Flow

```
Chunk 1 → Review → Extract findings summary
    ↓
Chunk 2 + "Previous findings: ..." → Review → Extract findings
    ↓
Chunk 3 + "Previous findings: ..." → Review
    ↓
Final synthesis with all findings
```

## Interface

```ruby
strategy = SubjectStrategy.for(:progressive, {
  max_tokens_per_chunk: 100_000,
  summary_prompt: "Summarize your findings in 2-3 sentences for context carryover."
})

# Progressive returns units with carryover instructions
units = strategy.prepare(subject, context)
# → [
#     { content: "...", metadata: { chunk_index: 0, requires_summary: true } },
#     { content: "...", metadata: { chunk_index: 1, inject_previous_summary: true } },
#     ...
#   ]
```

## Summary Extraction Format

LLM responses must include a structured `<context_carryover>` block for reliable extraction:

```markdown
## Review Findings

... main review content ...

<context_carryover>
- Key finding 1: Brief description
- Key finding 2: Brief description
- Patterns observed: List any cross-cutting concerns
</context_carryover>
```

The extraction atom will:
1. Look for `<context_carryover>` tags
2. Fall back to extracting bullet points from a "## Summary" section
3. Fall back to first 3 sentences of the response if no structured format found

```ruby
# Ace::Review::Atoms::FindingsExtractor
def self.extract(response)
  # Primary: XML tags
  if match = response.match(/<context_carryover>(.*?)<\/context_carryover>/m)
    return match[1].strip
  end

  # Fallback: Summary section
  if match = response.match(/## Summary\n(.*?)(\n##|\z)/m)
    return match[1].strip
  end

  # Last resort: First 3 sentences
  response.split(/[.!?]\s+/).take(3).join(". ") + "."
end
```

## Execution

The `MultiModelExecutor` must handle progressive strategy specially:
1. Execute chunk 0
2. Extract summary from response using `FindingsExtractor`
3. Inject summary into chunk 1 prompt
4. Execute chunk 1
5. Extract summary, inject into chunk 2...
6. Final synthesis

## Acceptance Criteria

- [ ] Each chunk review receives summary of prior findings
- [ ] Sequential execution (not parallel)
- [ ] Summary extraction works with standard review output
- [ ] Final report shows progressive context was used
- [ ] Falls back to chunked if summary extraction fails
- [ ] All tests pass
