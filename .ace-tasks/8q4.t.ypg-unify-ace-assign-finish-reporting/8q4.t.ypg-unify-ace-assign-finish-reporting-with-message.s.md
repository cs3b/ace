---
id: 8q4.t.ypg
status: draft
priority: medium
created_at: "2026-03-05 23:08:17"
estimate: TBD
dependencies: []
tags: [ace-assign, cli]
bundle:
  presets: ["project"]
  files:
    - ace-assign/lib/ace/assign/cli/commands/finish.rb
    - ace-assign/lib/ace/assign/cli/commands/fail.rb
    - ace-assign/test/commands/finish_command_test.rb
    - ace-assign/docs/usage.md
    - ace-assign/CHANGELOG.md
  commands: []
---

# Unify ace-assign finish reporting with --message flag

## Behavioral Specification

### User Experience
- **Input**: Report content provided via `--message/-m` flag (inline string or file path) or piped stdin
- **Process**: The command auto-detects whether `--message` value is an existing file path (reads content) or an inline string (uses directly). Falls back to stdin if no flag given.
- **Output**: Phase completed with report content saved; confirmation message printed

### Expected Behavior

Replace the `--report` option on `ace-assign finish` with `--message/-m`, matching the naming pattern already used by `ace-assign fail --message`. The new flag uses smart detection to determine whether the provided value is a file path or an inline string:

- If the value is an existing file path, read and use the file's content
- Otherwise, treat the value as an inline string

This eliminates the need for agents and users to create temporary files for simple reports. The resolution order is: `--message` (file or string) > stdin > error.

No backward compatibility shim is needed for `--report` (ADR-024, pre-1.0).

### Interface Contract

```bash
# --help output:
#   --message=VALUE, -m    Report content: string, file path, or pipe stdin

# Inline string (primary use case for agents)
ace-assign finish --message "## Report\nImplementation complete."
ace-assign finish -m "Done: all tests pass"

# File path (auto-detected when path exists on disk)
ace-assign finish --message report.md
ace-assign finish -m /tmp/detailed-report.md

# Stdin piping (unchanged, lowest priority)
echo "Phase complete" | ace-assign finish

# Error: no input provided
ace-assign finish
# => Error: "Missing report input: provide --message <string|file> or pipe stdin."
```

**Error Handling:**
- No `--message` and no stdin: `"Missing report input: provide --message <string|file> or pipe stdin."`
- `--message` with empty string: Same error as no input
- `--message` file path that doesn't exist: Treated as inline string (not an error)

**Edge Cases:**
- Value that looks like a path but doesn't exist on disk: used as inline string
- Value that is an existing file path: file content is read
- Both `--message` and stdin provided: `--message` takes precedence (matches current `--report` > stdin behavior)

### Success Criteria

- [ ] `--report` option is removed from `ace-assign finish`
- [ ] `--message/-m` option is added with desc `"Report content: string, file path, or pipe stdin"`
- [ ] Inline string via `--message` is used directly as report content
- [ ] Existing file path via `--message` reads file content
- [ ] Stdin fallback works when no `--message` is provided
- [ ] Error message matches: `"Missing report input: provide --message <string|file> or pipe stdin."`
- [ ] Help text documents all three input methods
- [ ] All existing tests updated (s/report/message/) and pass
- [ ] `ace-assign fail --message` pattern is consistent with `ace-assign finish --message`

### Validation Questions

All resolved by user decision in the plan:

- [x] **Flag naming**: `--message/-m` (matches `fail --message`)
- [x] **Backward compatibility**: Not needed (ADR-024, pre-1.0)
- [x] **Detection strategy**: Existing file path = read file; otherwise = inline string
- [x] **Resolution order**: `--message` > stdin > error

### Vertical Slice Decomposition (Task/Subtask Model)

- **Slice Type**: Standalone task
- **Slice Outcome**: `ace-assign finish` accepts report content via `--message` (inline or file), stdin, with clear error on missing input
- **Advisory Size**: small
- **Context Dependencies**: `finish.rb`, `fail.rb` (reference pattern), `finish_command_test.rb`, `usage.md`, `CHANGELOG.md`

### Verification Plan

#### Unit / Component Validation
- [ ] `--message "inline text"` uses the string directly as report content
- [ ] `--message report.md` (existing file) reads file content
- [ ] `--message nonexistent.md` (non-existing path) uses "nonexistent.md" as inline string
- [ ] `-m "short form"` works as alias
- [ ] Stdin piping works when no `--message` given
- [ ] `--message` takes precedence over stdin when both present

#### Failure / Invalid-Path Validation
- [ ] No `--message` and no stdin raises error with message: `"Missing report input: provide --message <string|file> or pipe stdin."`
- [ ] `--message ""` (empty string) raises same missing-input error
- [ ] `--message "  "` (whitespace only) raises same missing-input error

#### Verification Commands
- [ ] `ace-test test/commands/finish_command_test.rb`: All tests pass
- [ ] `ace-test`: Full test suite passes

## Objective

Simplify the `ace-assign finish` reporting workflow by replacing the file-only `--report` flag with a smart `--message/-m` flag that accepts inline strings or file paths. This eliminates the overhead of creating temporary report files, making the reporting process more ergonomic for agents and interactive users alike. Aligns `finish` with the `fail --message` pattern for CLI consistency.

## Scope of Work

- **User Experience Scope**: `ace-assign finish` report input via `--message/-m` flag and stdin
- **System Behavior Scope**: Smart detection (file vs string), resolution order, error messaging
- **Interface Scope**: CLI flag change (`--report` removed, `--message/-m` added)

### Deliverables

#### Behavioral Specifications
- Smart detection logic: file path existence check determines behavior
- Resolution order: `--message` > stdin > error
- Error message wording

#### Validation Artifacts
- Updated test suite covering inline string, file path, stdin, and error cases
- Updated usage documentation

### Files to Modify

1. `ace-assign/lib/ace/assign/cli/commands/finish.rb` -- replace `option :report` with `option :message, aliases: ["-m"]`, rewrite `resolve_report_content`
2. `ace-assign/test/commands/finish_command_test.rb` -- update existing tests (s/report/message/), add new tests for smart detection
3. `ace-assign/docs/usage.md` -- update `--report` references to `--message`
4. `ace-assign/CHANGELOG.md` -- Changed + Removed entries

No changes to PhaseWriter, AssignmentExecutor, or downstream code -- they already accept `report_content:` as a string.

## Out of Scope

- Changes to `ace-assign fail` (already uses `--message`)
- Changes to PhaseWriter or AssignmentExecutor internals
- New output formatting or reporting features
- Interactive/editor-based report input

## References

- Source idea: `8q4u8i` -- Streamlined ace-assign reporting via direct content piping
- Pattern reference: `ace-assign/lib/ace/assign/cli/commands/fail.rb` (existing `--message` usage)
- Usage documentation: `ux/usage.md` (draft usage scenarios)
- ADR-024: Pre-1.0 breaking changes permitted
