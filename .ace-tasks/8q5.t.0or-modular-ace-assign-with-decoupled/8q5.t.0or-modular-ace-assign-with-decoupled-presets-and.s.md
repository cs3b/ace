---
id: 8q5.t.0or
status: pending
priority: medium
created_at: "2026-03-06 00:27:30"
estimate: TBD
dependencies: [8q5.t.0m6, 8q5.t.0n9]
tags: [ace-assign, architecture, modular]
bundle:
  presets: [project]
  files: [ace-assign/lib/ace/assign/atoms/catalog_loader.rb, ace-assign/lib/ace/assign/atoms/preset_expander.rb, ace-assign/lib/ace/assign/atoms/composition_rules.rb, ace-assign/lib/ace/assign/organisms/assignment_executor.rb, ace-assign/lib/ace/assign/molecules/assignment_manager.rb, ace-assign/lib/ace/assign/molecules/skill_assign_source_resolver.rb, ace-assign/handbook/workflow-instructions/assign/compose.wf.md, ace-assign/handbook/workflow-instructions/assign/create.wf.md, ace-assign/.ace-defaults/assign/catalog/phases/, ace-support-nav/lib/ace/support/nav/, ace-support-nav/lib/ace/support/nav/molecules/source_registry.rb, .ace-tasks/8q5.t.0m6-migrate-batch-processing-workflows-to/3-resolve-intent-into-catalog-and/8q5.t.0m6.3-resolve-intent-into-catalog-and-skill-phases.s.md, .ace-tasks/8q5.t.0or-modular-ace-assign-with-decoupled/ux/usage.md]
  commands: []
needs_review: false
---

# Modular ace-assign with Decoupled Presets and Sources

## Behavioral Specification

### User Experience
- **Input**: Users compose assignments from modular building blocks registered by individual gems. Instead of ace-assign knowing about ace-task or ace-review internals, each gem registers its own phases and presets into a discoverable catalog.
- **Process**: ace-assign discovers registered sources (similar to ace-nav's protocol sources), loads their phase definitions into a merged catalog, and the compose/create workflows use that catalog to build assignments. The `assign:` frontmatter block in skill/workflow files remains the declarative interface for workflow-backed expansion.
- **Output**: Assignments that compose phases from multiple gems seamlessly. Adding a new assignable capability to any gem requires only registering a phase definition — no changes to ace-assign core.

### Expected Behavior

Today ace-assign has:
- A hardcoded phase catalog in `.ace-defaults/assign/catalog/phases/` (work-on-task, lint, create-pr, etc.)
- Preset composition via `composition_rules.rb` with knowledge of specific phases
- `skill_assign_source_resolver.rb` that resolves skill references

The modular redesign:
1. **Source registration**: Each gem declares its assignable phases in a standard location (e.g., `.ace-defaults/assign/catalog/phases/*.phase.yml`). ace-assign discovers these via the configuration cascade, similar to how ace-nav discovers protocol sources.
2. **Decoupled phase catalog**: The merged catalog is built from all registered sources at runtime. No gem needs to know about phases from other gems.
3. **Preset composition**: Presets reference phases by name. Since phases are registered by source gems, presets can compose cross-gem workflows without coupling.
4. **Frontmatter registration**: Gems can declare assignable behaviors in their `assign:` frontmatter blocks, making their capabilities discoverable.

The flow after redesign:
- `ace-assign catalog` inspects all registered phases from all gems
- `/as-assign-compose` and `/as-assign-create` resolve requested phases or presets from that merged catalog
- `ace-assign create FILE` remains the deterministic executor for generated job YAML
- Assignment execution uses existing mechanisms (phases, existing `context: fork`, skill metadata expansion)

### Interface Contract

```yaml
# Source registration: gem declares its phases
# ace-review/.ace-defaults/assign/catalog/phases/run-review.phase.yml
name: run-review
skill: ace-review-run
tags: [review, quality]
produces: [review-report]
prerequisites:
  - name: onboard
    strength: recommended
    reason: "Context loading improves review quality"
source: ace-review                # NEW: declares which gem owns this phase

# ace-task/.ace-defaults/assign/catalog/phases/work-on-task.phase.yml
name: work-on-task
skill: ace-task-work
tags: [implementation, task]
produces: [code-changes]
source: ace-task
```

```yaml
# Preset composition references phases by name (no gem coupling)
# .ace/assign/presets/full-delivery.yml
name: full-delivery
description: Complete delivery workflow
phases:
  - onboard
  - work-on-task
  - run-review            # from ace-review (discovered, not hardcoded)
  - lint                  # from ace-lint
  - create-pr
```

```bash
# Discovery: see all available phases from all gems
ace-assign catalog
# Output:
# Available phases:
#   onboard          (ace-assign)    [setup]
#   work-on-task     (ace-task)      [implementation, task]
#   run-review       (ace-review)    [review, quality]
#   lint             (ace-lint)      [quality, lint]
#   run-batch-e2e    (ace-test-e2e)  [testing, batch]    # from migrated batch (task 0m6)
#   create-pr        (ace-assign)    [delivery]

# Compose with discovered phases through the workflow surface
/as-assign-compose "onboard, work-on-task, review-pr, create-pr"
```

**Error Handling:**
- Phase name conflict across gems: Last-loaded wins with warning (config cascade order)
- Referenced phase not found: Clear error listing available phases and their sources
- Source gem not installed: Phases from that gem absent from catalog (no error unless referenced)
- Circular prerequisites: Detected and reported during compose/create resolution

**Edge Cases:**
- No external gems installed: ace-assign's built-in phases still work (backward compatible)
- Multiple gems register same phase name: Warning logged, most-specific scope wins (project > user > gem defaults)
- Preset references phase from uninstalled gem: Error with suggestion to install the gem
- Empty catalog after filtering: Clear message "no phases match your criteria"

### Success Criteria

- [ ] **Source registration**: Gems register phases via `.ace-defaults/assign/catalog/phases/` convention
- [ ] **Catalog discovery**: ace-assign discovers phases from all gems at runtime via config cascade
- [ ] **Decoupled presets**: Presets compose phases by name without gem-specific coupling
- [ ] **Source attribution**: `ace-assign catalog` shows which gem provides each phase
- [ ] **Backward compatible**: Existing assignments and presets work without modification
- [ ] **No ace-task/ace-review coupling**: ace-assign core has zero imports from ace-task or ace-review
- [ ] **Extensibility**: New gem can add assignable phases with zero ace-assign changes

### Validation Questions

- [x] **Discovery mechanism**: How does ace-assign find phases from other gems? -> Config cascade: each gem puts phases in `.ace-defaults/assign/catalog/phases/`, merged at runtime like nav sources
- [x] **ace-nav parallel**: Is this the same pattern as ace-nav source registration? -> Yes, analogous: nav discovers protocol sources from gems, assign discovers phase sources from gems
- [x] **Migration path**: How do existing hardcoded phases transition? -> They already live in `.ace-defaults/assign/catalog/phases/`; add `source:` field and remove any gem-specific logic from ace-assign core
- [x] **Public composition surface**: How does this align with smart create? -> Catalog-backed composition stays workflow-driven through `/as-assign-compose` and `/as-assign-create`; deterministic `ace-assign create FILE` continues to consume the generated YAML
- [x] **FORK integration**: How does this relate to existing fork behavior? -> Registered phases may continue to declare existing `context: fork` metadata; this task does not depend on `fork_mode: workflow` or direct skill execution from `8q5.t.0ki`
- [x] **Batch migration**: How does this relate to batch migration (task 0m6)? -> Migrated batch workflows become registered phases with `context: fork`; they're composed into presets like any other phase

### Vertical Slice Decomposition (Task/Subtask Model)

- **Slice Type**: Standalone task
- **Slice Outcome**: ace-assign discovers and composes phases from any registered gem without internal coupling
- **Advisory Size**: large
- **Context Dependencies**: CatalogLoader atom, PresetExpander atom, composition_rules atom, ace-nav source discovery pattern, unified smart create flow (0m6/0m6.3), migrated native pre-commit review phase (0n9)

### Verification Plan

#### Unit / Component Validation
- [ ] CatalogLoader discovers phases from multiple gem directories via config cascade
- [ ] Phase definitions include `source` field for attribution
- [ ] PresetExpander resolves phase names from merged catalog
- [ ] composition_rules work with dynamically-discovered phases

#### Integration / E2E Validation
- [ ] `ace-assign catalog` lists phases from ace-assign + other installed gems
- [ ] `/as-assign-compose` or `/as-assign-create` resolves phases from multiple gems into one assignment
- [ ] Full delivery preset executes with phases from ace-task, ace-review, ace-lint
- [ ] Existing presets and assignments work without modification

#### Failure / Invalid-Path Validation
- [ ] Missing phase (uninstalled gem) produces clear error with gem name
- [ ] Phase name conflict produces warning with source attribution
- [ ] Circular prerequisites detected and reported

#### Verification Commands
- [ ] `ace-test ace-assign` passes after refactoring
- [ ] `ace-assign catalog` shows source attribution for all phases
- [ ] `rg -n "ace/task|ace/review" ace-assign/lib/` finds no direct imports (only phase references)

## Objective

Transition ace-assign from a monolithic package with hardcoded knowledge of ace-task and ace-review into a flexible orchestration engine. By adopting a source registration pattern (like ace-nav's protocol sources), individual gems register their assignable capabilities, and ace-assign discovers and composes them without coupling. This is the architectural capstone that builds on smart-create intent resolution (task 0m6) and phase migration examples such as native pre-commit review (task 0n9).

## Scope of Work

- **User Experience Scope**: Users compose assignments from a discoverable catalog of phases contributed by any gem; `ace-assign catalog` shows available phases with source attribution, and workflow-driven compose/create surfaces use the merged registry
- **System Behavior Scope**: Source registration convention, catalog discovery via config cascade, decoupled preset composition, removal of gem-specific coupling from ace-assign core
- **Interface Scope**: `ace-assign catalog` inspection, `source:` field in phase definitions, and workflow-driven phase discovery via compose/create flows

### Deliverables

#### Behavioral Specifications
- Source registration convention for gems
- Catalog discovery and merging behavior
- Preset composition with dynamic phase resolution

#### Validation Artifacts
- Integration tests for cross-gem phase discovery
- Decoupling verification (no direct imports from ace-task/ace-review in ace-assign)
- Backward compatibility tests for existing presets

### Consumer Packages

Gems that will need to register their phases via `.ace-defaults/assign/catalog/phases/`:
- **ace-assign** (built-in): onboard, commit, create-pr, push-to-remote, security-audit, and other core phases (already in place)
- **ace-task**: work-on-task, plan-task, mark-task-done, task-load (currently in ace-assign, to be migrated)
- **ace-review**: review-pr, apply-feedback (currently in ace-assign, to be migrated)
- **ace-lint**: lint (currently in ace-assign, to be migrated)
- **ace-test-runner**: verify-test, verify-test-suite, fix-tests (currently in ace-assign, to be migrated)
- **ace-test-runner-e2e**: verify-e2e (currently in ace-assign, to be migrated)
- **ace-docs**: update-docs (currently in ace-assign, to be migrated)
- **ace-retro**: create-retro (currently in ace-assign, to be migrated)
- **ace-search**: research (currently in ace-assign, to be migrated)

Note: Phase migration to owning gems is the core deliverable. Existing phases in ace-assign's `.ace-defaults/` continue to work until migrated.

## Out of Scope

- Implementation Details: File structures, code organization, technical architecture
- Technology Decisions: Tool selections, library choices, framework decisions
- New phases or presets (each gem team adds their own post-redesign)
- Changes to assignment execution engine (phases/fork mechanism unchanged)
- UI or interactive composition tools

## References

- Source idea: `.ace-ideas/8q3zd9-modular-ace-assign-with-decoupled/8q3zd9-modular-ace-assign-with-decoupled-presets-and.idea.s.md`
- CatalogLoader: `ace-assign/lib/ace/assign/atoms/catalog_loader.rb`
- PresetExpander: `ace-assign/lib/ace/assign/atoms/preset_expander.rb`
- Composition rules: `ace-assign/lib/ace/assign/atoms/composition_rules.rb`
- ace-nav source pattern: `ace-support-nav/lib/ace/support/nav/`
- Batch migration task: `.ace-tasks/8q5.t.0m6-*/`
- Smart create intent slice: `.ace-tasks/8q5.t.0m6-migrate-batch-processing-workflows-to/3-resolve-intent-into-catalog-and/8q5.t.0m6.3-resolve-intent-into-catalog-and-skill-phases.s.md`
- Native review task: `.ace-tasks/8q5.t.0n9-*/`
