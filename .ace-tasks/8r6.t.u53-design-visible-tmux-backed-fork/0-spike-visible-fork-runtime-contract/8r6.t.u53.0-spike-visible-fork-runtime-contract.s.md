---
id: 8r6.t.u53.0
status: pending
priority: medium
created_at: "2026-04-07 20:05:45"
estimate: TBD
dependencies: []
tags: []
parent: 8r6.t.u53
bundle:
  presets: [project]
  files: [ace-assign/lib/ace/assign/cli/commands/fork_run.rb, ace-assign/lib/ace/assign/molecules/fork_session_launcher.rb, ace-assign/handbook/workflow-instructions/assign/drive.wf.md, ace-tmux/lib/ace/tmux/atoms/tmux_command_builder.rb, ace-tmux/lib/ace/tmux/organisms/window_manager.rb, ace-tmux/lib/ace/tmux/models/session.rb, ace-tmux/lib/ace/tmux/models/window.rb, ace-tmux/lib/ace/tmux/models/pane.rb, ace-tmux/lib/ace/tmux/cli/commands/start.rb, ace-tmux/lib/ace/tmux/cli/commands/window.rb, ace-overseer/lib/ace/overseer/molecules/tmux_window_opener.rb, ace-llm-providers-cli/lib/ace/llm/providers/cli/codex_client.rb, ace-llm-providers-cli/lib/ace/llm/providers/cli/claude_code_client.rb]
  commands: [ace-task show 8r6.t.u53 --content, ace-task show 8r6.t.u53.0 --content]
needs_review: false
---

# Spike ACE tmux runtime visibility and recording contract

## Objective

Validate the end-state contract for ACE-managed tmux runtime visibility and recording, including which current visible-fork concepts survive, which recording concepts are required, and how `ace-assign`, `ace-tmux`, and provider CLI execution boundaries should relate before additional implementation subtasks are drafted.

## Behavioral Specification

### User Experience

- A designer or implementer can answer, from one spike outcome, how a visible fork run should appear in tmux, how recorded manual takeover activity should be captured for later inspection, and what fallback behavior users should experience when visible mode is unavailable.
- A designer or implementer can answer, from one spike outcome, how recording inheritance and override scope work across ACE-managed session, window, pane, and CLI entrypoints.
- The spike produces a concept inventory that makes later implementation subtasks stable instead of speculative.
- The spike resolves whether existing launcher, metadata, and tmux behaviors survive intact, are extended, or should be superseded.

### Expected Behavior

1. The spike validates one coherent end-state scenario for visible fork execution from `ace-assign fork-run` through operator inspection in tmux.
2. The spike validates one coherent end-state scenario for ACE-managed recording from session start through later inspection of pane artifacts after manual takeover work.
3. The spike explicitly identifies which existing concepts remain authoritative, including assignment-state completion and the current provider-backed launcher fallback.
4. The spike explicitly identifies the minimum new concepts required, including runtime tmux state inventory, fork-window placement behavior, recording artifact scope, and recording inheritance/source-scope reporting.
5. The spike produces a concept inventory table showing concept status as kept, changed, new, or rejected.
6. The spike defers further subtask drafting until the inventory makes later decomposition stable.
7. The spike locks the operator-facing diagnostic contract for premature pane exit to include both recent pane tail output and available recording artifact paths.

### Interface Contract

- **Spike output contract**
  - Updated parent task concept inventory and scope notes
  - Clear go/no-go statement for these behavioral contracts:
    - optional visible fork mode
    - sibling `-fork` window naming and placement
    - shared auto-grid pane surface
    - generic `ace-tmux state` runtime inventory
    - optional ACE-managed recording
    - recording override and inheritance behavior across session, window, pane, and CLI entrypoints
    - `.ace-local/tmux` artifact contract
    - assignment-state-authoritative completion
    - premature-pane-exit diagnostics with pane tail plus artifact references
- **Decision contract**
  - The spike does not implement code.
  - The spike may refine task specs and usage draft content if the validated contract differs from the initial draft.
  - The spike treats source-scope reporting in `ace-tmux state` as required, not optional.

### Success Criteria

- [ ] One validated end-state scenario exists for visible fork execution.
- [ ] One validated end-state scenario exists for ACE-managed recording of manual takeover activity.
- [ ] The spike identifies which current concepts survive unchanged, which are extended, and which are newly introduced.
- [ ] The spike leaves no unresolved behavioral ambiguity for window naming, placement, fallback, recording scope, recording source-scope reporting, pane-exit diagnostics, or source-of-truth completion semantics.
- [ ] The parent task can be safely decomposed into implementation subtasks after the spike.

## Vertical Slice Decomposition (Task/Subtask Model)

- **Slice type**: subtask spike
- **Slice outcome**: validated runtime contract and concept inventory for visible fork execution and ACE-managed recording
- **Advisory size**: small
- **Context dependencies**: parent task draft, current `ace-assign` fork path, `ace-tmux` runtime gaps, ACE-managed pane launch behavior, provider CLI command-planning behavior

## Verification Plan

### Unit / Component Validation

- Review the spike output and confirm each end-state concept maps cleanly to one subsystem responsibility.
- Confirm the recording contract stays evidence-oriented and does not claim semantic learning behavior.
- Confirm the recording contract is public at session/window/pane preset scope and that CLI `--record` entrypoints map into the same override model.

### Integration / E2E Validation

- Trace the single validated scenario from `ace-assign fork-run --launch-mode tmux-visible` to a visible pane in `<current-window>-fork` and back to assignment-state completion.
- Trace the single validated scenario from recorded `ace-tmux` session startup to later inspection of pane artifacts under `.ace-local/tmux/`, including effective recording state and `source_scope` in `ace-tmux state`.

### Failure / Invalid Path Validation

- Confirm the spike explicitly covers the no-tmux fallback path and the premature-pane-exit diagnostic path.
- Confirm the spike explicitly requires pane-tail output plus artifact references for premature visible-pane exits when evidence exists.
- Confirm the spike explicitly excludes arbitrary foreign tmux sessions and clarifies that recording does not guarantee perfect semantic attribution inside every interactive program.

### Verification Commands

- `ace-task show 8r6.t.u53 --content`
- `ace-task show 8r6.t.u53.0 --content`

## Scope of Work

- Validate the end-state behavioral contract
- Produce a stable concept inventory
- Refine the parent draft if the concept inventory changes the task boundary
- Validate that recording remains ACE-managed, opt-in, and off by default
- Lock the public recording override model and `ace-tmux state` provenance surface for later implementation subtasks

## Deliverables

### Behavioral Specifications

- validated visible fork interaction model
- validated tmux runtime inspection scope
- validated ACE-managed recording scope and artifact contract
- validated recording inheritance and override contract, including `source_scope` reporting
- validated fallback and completion semantics
- validated premature-pane-exit diagnostic contract

### Validation Artifacts

- concept inventory with kept/changed/new/rejected status
- updated parent-task references if refinement is needed

## Out of Scope

- package implementation
- code-level decomposition beyond what the concept inventory requires
- redaction, semantic interpretation, or learning workflows built on captured logs
- drafting later implementation subtasks before the spike completes

## References

- parent task `8r6.t.u53`
- `ace-assign/lib/ace/assign/cli/commands/fork_run.rb`
- `ace-tmux/lib/ace/tmux/organisms/window_manager.rb`
