---
id: 8r9.t.i06.6
status: draft
priority: high
created_at: '2026-04-11 17:20:00'
estimate: TBD
dependencies:
- 8r9.t.i06.0
tags:
- testing
- migration
- fast
- feat
- e2e
parent: 8r9.t.i06
bundle:
  presets:
  - project
  files:
  - ace-hitl/README.md
  - ".ace-tasks/8r9.t.hzr-pilot-restarted-e2e-architecture-on/8r9.t.hzr-pilot-restarted-e2e-architecture-on-ace-b36ts.s.md"
  commands:
  - ace-task show 8r9.t.i06.6 --content
  - ace-task show 8r9.t.hzr --content
---

# Migrate ace-hitl to the fast/feat/e2e testing model

## Objective

Move ace-hitl into the new `fast`/`feat`/`e2e` model with a package-specific migration shape that matches its current test surface.

## Package Migration Shape

- Fast-only package for this migration.
- Move deterministic CLI coverage into `test/fast/commands/`.
- Do not create `test/feat/` or `test/e2e/` unless the refresh task finds a clear package-owned need.

## Expected Behavior

1. Move existing deterministic CLI coverage into `test/fast/`.
2. Keep the default package loop on `fast`.
3. Only introduce `test/feat/` if a clear deterministic feature slice exists beyond the fast loop.
4. Keep `test/e2e/` absent unless the package later owns real agent-runtime scenarios.

## Verification

- `ace-test ace-hitl`
- `ace-test ace-hitl all`

## Success Criteria

- [ ] Deterministic coverage runs from `test/fast/`.
- [ ] No unnecessary `feat` or `e2e` layer is introduced.
- [ ] Package docs and config teach only the new naming.
