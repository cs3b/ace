---
id: 8r9.t.i06.6
status: pending
priority: high
created_at: "2026-04-11 17:20:00"
estimate: TBD
dependencies: [8r9.t.i06.0]
tags: [testing, migration, fast, feat, e2e]
parent: 8r9.t.i06
bundle:
  presets: [project]
  files: [ace-hitl/README.md, ace-hitl/docs/usage.md, .ace-tasks/8r9.t.hzr-pilot-restarted-e2e-architecture-on/8r9.t.hzr-pilot-restarted-e2e-architecture-on-ace-b36ts.s.md]
  commands: [ace-task show 8r9.t.i06.6 --content, ace-task show 8r9.t.hzr --content]
needs_review: false
---

# Migrate ace-hitl to the fast/feat/e2e testing model

## Objective

Move ace-hitl into the new `fast`/`feat`/`e2e` model with a package-specific migration shape that matches its current test surface.

## User Experience

- `ace-test ace-hitl` becomes the only public deterministic loop for this package.
- The package remains fast-only in this migration because the current repo state only contains one deterministic CLI test file and no `test/integration/` or scenario surface.
- Package docs describe the fast-only contract without implying hidden `feat` or `e2e` layers.

## Package Migration Shape

- Fast-only package for this migration.
- Move deterministic CLI coverage into `test/fast/commands/`.
- Do not create `test/feat/` or `test/e2e/` in this migration.

## Expected Behavior

1. Move existing deterministic CLI coverage into `test/fast/`.
2. Keep the default package loop on `fast`.
3. Update package docs and test references to teach only the fast-only contract.
4. Keep `test/feat/` and `test/e2e/` absent for this package.

## Interface Contract

- `ace-test ace-hitl`
- `ace-test ace-hitl all`

## Validation Questions

- None. Current repo inspection found only `test/commands/hitl_cli_test.rb` plus `test/test_helper.rb`, with no `test/integration/`, `test/e2e/`, or scenario assets.

## Vertical Slice Decomposition

- This task is one standalone package migration slice for `ace-hitl`. It ships independently once the package exposes the fast-only contract.

## Verification

- `ace-test ace-hitl`
- `ace-test ace-hitl all`
- Verify the migration does not add `test/feat/` or `test/e2e/` for this package.

## Success Criteria

- [ ] Deterministic coverage runs from `test/fast/`.
- [ ] No unnecessary `feat` or `e2e` layer is introduced.
- [ ] Package docs and test references teach only the new naming.
