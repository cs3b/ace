---
id: 8r9.t.i06.9
status: draft
priority: high
created_at: '2026-04-11 17:20:00'
estimate: TBD
dependencies:
- 8r9.t.i06.0
tags:
- testing
- migration
- fast
- feat
- e2e
parent: 8r9.t.i06
bundle:
  presets:
  - project
  files:
  - ace-support-core/README.md
  - ".ace-tasks/8r9.t.hzr-pilot-restarted-e2e-architecture-on/8r9.t.hzr-pilot-restarted-e2e-architecture-on-ace-b36ts.s.md"
  commands:
  - ace-task show 8r9.t.i06.9 --content
  - ace-task show 8r9.t.hzr --content
---

# Migrate ace-support-core to the fast/feat/e2e testing model

## Objective

Move ace-support-core into the new `fast`/`feat`/`e2e` model with a package-specific migration shape that matches its current test surface.

## Package Migration Shape

- Fast + feat package.
- Move unit-style deterministic tests into `test/fast/`.
- Move current `test/integration/` suites into `test/feat/`.
- Keep `test/e2e/` absent unless the package later owns real scenarios.

## Expected Behavior

1. Move fast-loop deterministic coverage into `test/fast/`.
2. Move current integration-like deterministic coverage into `test/feat/`.
3. Keep the public package contract on `fast`, `feat`, and `all`.
4. Update docs and package-local config to match the new layout.

## Verification

- `ace-test ace-support-core`
- `ace-test ace-support-core feat`
- `ace-test ace-support-core all`

## Success Criteria

- [ ] Deterministic fast-loop coverage lives under `test/fast/`.
- [ ] Existing `test/integration/` suites move to `test/feat/`.
- [ ] Package docs and config stop teaching `integration` or `int` as the public layer name.
