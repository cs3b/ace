---
id: 8r9.t.i06.e
status: pending
priority: high
created_at: "2026-04-11 17:20:00"
estimate: TBD
dependencies: [8r9.t.i06.0]
tags: [testing, migration, fast, feat, e2e]
parent: 8r9.t.i06
bundle:
  presets: [project]
  files: [ace-support-test-helpers/README.md, .ace-tasks/8r9.t.hzr-pilot-restarted-e2e-architecture-on/8r9.t.hzr-pilot-restarted-e2e-architecture-on-ace-b36ts.s.md]
  commands: [ace-task show 8r9.t.i06.e --content, ace-task show 8r9.t.hzr --content]
needs_review: false
---

# Migrate ace-support-test-helpers to the fast/feat/e2e testing model

## Objective

Move ace-support-test-helpers into the new `fast`/`feat`/`e2e` model with a package-specific migration shape that matches its current test surface.

## User Experience

- `ace-test ace-support-test-helpers` becomes the only public deterministic loop for this package.
- The package remains fast-only in this migration because the current repo state contains atom, molecule, and fixture coverage only.
- Package docs teach the fast-only contract instead of implying extra layers.

## Package Migration Shape

- Fast-only package with atom, molecule, and fixture deterministic tests.
- Move those tests into `test/fast/`.
- Do not create `test/feat/` or `test/e2e/` in this migration.

## Expected Behavior

1. Move current deterministic tests into `test/fast/`.
2. Keep the package default loop on `fast`.
3. Update package docs and references to teach the fast-only contract.
4. Do not invent `test/feat/` or `test/e2e/`.

## Interface Contract

- `ace-test ace-support-test-helpers`
- `ace-test ace-support-test-helpers all`

## Validation Questions

- None. Current repo inspection found atom, molecule, and fixture coverage only, with no `test/integration/`, `test/e2e/`, or scenario surface.

## Vertical Slice Decomposition

- This task is one standalone package migration slice for `ace-support-test-helpers`. It ships independently once the package exposes the fast-only contract.

## Verification

- `ace-test ace-support-test-helpers`
- `ace-test ace-support-test-helpers all`
- Verify the migration does not add `test/feat/` or `test/e2e/` for this package.

## Success Criteria

- [ ] Existing deterministic tests run from `test/fast/`.
- [ ] No `test/feat/` layer is introduced without new package-owned behavior.
- [ ] No scenario layer is introduced without a real package-owned need.
