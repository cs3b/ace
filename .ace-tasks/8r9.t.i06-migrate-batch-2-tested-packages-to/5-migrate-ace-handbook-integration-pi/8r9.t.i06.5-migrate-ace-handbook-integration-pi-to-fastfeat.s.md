---
id: 8r9.t.i06.5
status: pending
priority: high
created_at: "2026-04-11 17:20:00"
estimate: TBD
dependencies: [8r9.t.i06.0]
tags: [testing, migration, fast, feat, e2e]
parent: 8r9.t.i06
bundle:
  presets: [project]
  files: [ace-handbook-integration-pi/README.md, .ace-tasks/8r9.t.hzr-pilot-restarted-e2e-architecture-on/8r9.t.hzr-pilot-restarted-e2e-architecture-on-ace-b36ts.s.md]
  commands: [ace-task show 8r9.t.i06.5 --content, ace-task show 8r9.t.hzr --content]
needs_review: false
---

# Migrate ace-handbook-integration-pi to the fast/feat/e2e testing model

## Objective

Move ace-handbook-integration-pi into the new `fast`/`feat`/`e2e` model with a package-specific migration shape that matches its current test surface.

## User Experience

- `ace-test ace-handbook-integration-pi` becomes the only public deterministic loop for this package.
- The package does not expose a separate `feat` or `e2e` layer in this migration because the current repo state only contains flat deterministic tests.
- Package docs describe the package as fast-only instead of implying hidden legacy layers.

## Package Migration Shape

- Fast-only package.
- Move the current flat deterministic tests into `test/fast/`.
- Do not create `test/feat/` or `test/e2e/` in this migration.

## Expected Behavior

1. Move current deterministic package tests into `test/fast/`.
2. Keep `ace-test <package>` on the fast loop only.
3. Update package docs to describe the fast-only contract.
4. Do not invent deterministic `feat` or scenario `e2e` coverage for this migration.

## Interface Contract

- `ace-test ace-handbook-integration-pi`
- `ace-test ace-handbook-integration-pi all`

## Validation Questions

- None. Current repo inspection found only `test/pi_test.rb` plus `test/test_helper.rb`, with no `test/integration/`, `test/e2e/`, or scenario assets.

## Vertical Slice Decomposition

- This task is one standalone package migration slice for `ace-handbook-integration-pi`. It ships independently once the package exposes the fast-only contract.

## Verification

- `ace-test ace-handbook-integration-pi`
- `ace-test ace-handbook-integration-pi all`
- Verify the migration does not add `test/feat/` or `test/e2e/` for this package.

## Success Criteria

- [ ] Current deterministic coverage runs from `test/fast/`.
- [ ] No `test/feat/` or `test/e2e/` layer is introduced without a package-owned reason.
- [ ] Package docs stop teaching legacy layer names.
