---
id: 8r6.t.xeu.0
status: draft
priority: medium
created_at: "2026-04-07 22:16:34"
estimate: TBD
dependencies: []
tags: []
parent: 8r6.t.xeu
bundle:
  presets: [project]
  files: [ace-tmux/lib/ace/tmux/cli.rb, ace-tmux/lib/ace/tmux/atoms/tmux_command_builder.rb, ace-tmux/lib/ace/tmux/models/session.rb, ace-tmux/lib/ace/tmux/models/window.rb, ace-tmux/lib/ace/tmux/models/pane.rb, ace-tmux/lib/ace/tmux/cli/commands/start.rb, ace-tmux/lib/ace/tmux/cli/commands/window.rb, ace-overseer/lib/ace/overseer/molecules/tmux_window_opener.rb, .ace-tasks/8r6.t.u53-design-visible-tmux-backed-fork/8r6.t.u53-design-visible-tmux-backed-fork-execution-for.s.md, .ace-tasks/8re.t.n1d-design-ace-tmux-control-integrations/8re.t.n1d-design-ace-tmux-control-integrations-for-assignment.s.md]
  commands: [ace-task show 8r6.t.xeu --content, ace-task show 8r6.t.xeu.0 --content, ace-task show 8r6.t.u53 --content, ace-task show 8re.t.n1d --content]
---

# Spike ACE tmux read-side delta after shipped runtime/control work

## Objective

Validate the remaining read-side delta after the shipped tmux runtime/control work landed, including whether anything beyond the current `ace-tmux list` contract is still needed and whether recording/provenance should stay attached to this task at all.
This spike does not assume `ace-tmux state`, JSON output, or recording artifacts already belong to the final design. Interactive control questions belong to sibling task `8re.t.n1d`.

## Behavioral Specification

### User Experience

- A designer or implementer can answer, from one spike outcome, what the current `ace-tmux list` surface already guarantees and what genuine read-side gap remains.
- A designer or implementer can answer whether a machine-readable runtime surface is needed and whether it should extend `list` or introduce a distinct command.
- The spike produces a concept inventory that reflects shipped tmux behavior instead of stale speculative contracts.
- The spike explicitly classifies recording inheritance, provenance, and artifact layout as kept here, moved elsewhere, or deferred.
- The spike does not answer interactive control questions such as live send, wait, attach, detach, or pane-tail capture semantics.

### Expected Behavior

1. The spike validates one coherent baseline scenario for generic runtime inspection through shipped `ace-tmux list` behavior.
2. The spike explicitly identifies which original `8r6.t.xeu` concepts are already superseded by shipped work and which remain valid follow-up candidates.
3. The spike explicitly identifies the minimum new concepts, if any, required for reusable runtime state beyond today’s table output.
4. The spike explicitly classifies recording artifact scope, recording inheritance, and `source_scope` provenance as kept, moved, or deferred instead of assuming them.
5. The spike produces a concept inventory table showing concept status as kept, changed, new, or rejected.
6. The spike defers further subtask drafting until the inventory makes later decomposition stable.
7. The spike confirms the inspectability surface is generic and not owned by visible-fork-specific behavior.
8. The spike rejects any attempt to absorb interactive control semantics that belong to sibling task `8re.t.n1d`.

### Interface Contract

- **Spike output contract**
  - Updated parent task concept inventory and scope notes
  - Clear go/no-go statement for these behavioral contracts:
    - shipped `ace-tmux list` runtime inventory baseline
    - optional machine-readable runtime output, if still needed
    - optional ACE-managed recording, if it still belongs here
    - generic session/window/pane identifiers and any added liveness hints
    - ownership of `.ace-local/tmux` and `source_scope`, if retained at all
    - explicit rejection of interactive control command ownership
- **Decision contract**
  - The spike does not implement code.
  - The spike may refine task specs and usage draft content if the validated contract differs from the initial draft.
  - The spike treats visible-fork launch and fork-window behavior as out of scope and owned by sibling task `8r6.t.u53`.
  - The spike treats interactive control commands and live pane-tail capture as out of scope and owned by sibling task `8re.t.n1d`.

### Success Criteria

- [ ] One validated baseline scenario exists for generic tmux runtime inspection using current shipped behavior.
- [ ] The spike identifies which current inspectability concepts survive unchanged, which are extended, and which are newly introduced.
- [ ] The spike explicitly resolves or defers recording scope, recording source-scope reporting, and artifact layout ownership.
- [ ] The spike leaves no ambiguity about the boundary between any future persisted artifact reporting and live control-side pane-tail capture.
- [ ] The parent task can be safely decomposed into implementation subtasks after the spike.

## Vertical Slice Decomposition (Task/Subtask Model)

- **Slice type**: subtask spike
- **Slice outcome**: validated read-side delta and ownership boundaries for generic ACE-managed tmux inspectability
- **Advisory size**: small
- **Context dependencies**: parent task review state, current `ace-tmux` runtime gaps, ACE-managed pane launch behavior, shipped visible-fork work `8r6.t.u53`

## Verification Plan

### Unit / Component Validation

- Review the spike output and confirm each end-state concept maps cleanly to one subsystem responsibility.
- Confirm the spike does not claim recording behavior that is absent from the current repo unless it is explicitly proposed as future work.
- Confirm the spike output explicitly rejects ownership of interactive control commands and live pane-tail capture.

### Integration / E2E Validation

- Trace the single validated scenario from `ace-tmux list` to operator inspection of sessions, windows, and panes.
- Trace the ownership boundary against sibling task `8re.t.n1d` and confirm no concept survives in both tasks under different wording.

### Failure / Invalid Path Validation

- Confirm the spike explicitly excludes arbitrary foreign tmux sessions and scopes the contract to ACE-managed surfaces.
- Confirm the spike explicitly remains usable without visible fork execution.
- Confirm the spike explicitly excludes interactive control semantics and live pane-tail output retrieval.
- Confirm the spike explicitly flags stale `ace-tmux state` / recording assumptions rather than silently carrying them forward.

### Verification Commands

- `ace-task show 8r6.t.xeu --content`
- `ace-task show 8r6.t.xeu.0 --content`
- `ace-task show 8r6.t.u53 --content`
- `ace-task show 8re.t.n1d --content`

## Scope of Work

- Validate the end-state behavioral contract against shipped `ace-tmux list`
- Produce a stable concept inventory
- Refine the parent draft if the concept inventory changes the task boundary
- Explicitly classify recording/provenance ownership instead of assuming it
- Explicitly reject interactive control ownership and live pane-tail capture as non-goals for this spike

## Deliverables

### Behavioral Specifications

- validated tmux runtime inspection scope
- validated recording/provenance ownership decision or deferral
- validated consumer reuse boundary independent of visible fork
- validated read-side boundary versus sibling control-side task `8re.t.n1d`

### Validation Artifacts

- concept inventory with kept/changed/new/rejected status
- updated parent-task references if refinement is needed

## Out of Scope

- package implementation
- code-level decomposition beyond what the concept inventory requires
- visible fork launch, fork-window behavior, or `ace-assign`-specific diagnostics
- interactive control commands (`send`, `wait`, live pane-tail `capture`, `attach`, `detach`)
- redaction, semantic interpretation, or learning workflows built on captured logs
- drafting later implementation subtasks before the spike completes

## References

- parent task `8r6.t.xeu`
- sibling task `8r6.t.u53`
- sibling task `8re.t.n1d`
- `ace-tmux/lib/ace/tmux/cli.rb`
- `ace-overseer/lib/ace/overseer/molecules/tmux_window_opener.rb`
