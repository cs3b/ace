## E2E Change Plan: ace-lint

**Generated:** 2026-04-12
**Based on:** `.ace-tasks/8r9.t.i05-migrate-remaining-packages-to-restarted/c-migrate-ace-lint-to-restarted/8r9.t.i05.c-e2e-review.md`
**Scope:** package-wide (`TS-LINT-001`)

### Impact Summary

| Metric | Current | Proposed | Change |
|--------|---------|----------|--------|
| Scenarios | 1 | 1 | 0 |
| Test Cases | 7 | 7 | 0 |
| Est. cost/run | unchanged | unchanged | 0% |

### REMOVE (0 TCs)

None.

### KEEP (7 TCs)

| TC | Notes |
|----|-------|
| `TC-001-help-survey` | Keeps CLI help/flags discoverability through real binary output |
| `TC-002-valid-lint` | Keeps valid lint/report generation pipeline |
| `TC-003-fix-mode` | Keeps real file mutation behavior under auto-fix |
| `TC-004-error-detection` | Keeps non-zero syntax-error handling and pending report flow |
| `TC-005-batch-mixed` | Keeps mixed-file categorization and report/no-report behavior |
| `TC-006-config-routing` | Keeps real config-routing + CLI override behavior |
| `TC-007-doctor-diagnostics` | Keeps doctor diagnostics behavior on healthy and broken config |

### MODIFY (1 scenario metadata update)

| Target | Change Needed |
|--------|---------------|
| `TS-LINT-001/scenario.yml` | Add/refresh `unit-coverage-reviewed` to point at migrated deterministic `test/fast/...` paths after file moves; keep existing justification and setup contract. |

### CONSOLIDATE (0)

None.

### ADD (0)

None.

### Proposed Scenario Structure

- `TS-LINT-001-lint-pipeline/` remains unchanged with 7 TCs.

### Next Step

Apply scenario metadata update after deterministic test migration to `test/fast`, then verify no stale legacy deterministic path references remain in scenario metadata.
