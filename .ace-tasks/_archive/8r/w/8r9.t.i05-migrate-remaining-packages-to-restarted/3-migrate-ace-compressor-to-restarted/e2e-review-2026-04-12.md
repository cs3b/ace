## E2E Coverage Review: ace-compressor

**Reviewed:** 2026-04-12
**Scope:** package-wide
**Workflow version:** 2.1

### Summary

| Metric | Count |
|--------|-------|
| Package features | 6 |
| Unit test files | 12 |
| Unit assertions | 371 |
| E2E scenarios | 1 |
| E2E test cases | 4 |
| TCs with decision evidence | 4/4 |
| High-risk false-positive TCs | 0/4 |

### Feature Inventory

| Feature | Command | External Tools | Description |
|---------|---------|----------------|-------------|
| Help surface | `ace-compressor --help` | none | Exposes command usage/options to operators |
| Exact compression output | `ace-compressor <src> --mode exact --format stdio` | none | Emits deterministic `ContextPack/3` records |
| Stats reporting | `ace-compressor <src> --mode exact --format stats` | none | Reports output path/source count and compression metrics |
| Per-source output mode | `ace-compressor <a> <b> --mode exact --source-scope per-source --output <dir>` | filesystem | Writes one output path per input in source order |
| Compact refusal contract | `ace-compressor <src> --mode compact --format stdio` | none | Emits refusal/guidance semantics for rule-heavy content |
| Benchmark comparison | `ace-compressor benchmark <src> --modes exact,compact,agent` | none | Compares mode behavior and coverage metrics |

### Coverage Matrix

| Feature | Unit Tests | E2E Tests | Evidence Strength | False-Positive Risk | Status |
|---------|------------|-----------|-------------------|---------------------|--------|
| Help surface | `test/commands/compress_test.rb` | `TC-001` | command-output (stdout/exit) | low | Overlap (keep for binary surface) |
| Exact compression output | `test/commands/compress_test.rb`, `test/organisms/exact_compressor_test.rb`, `test/organisms/compression_runner_test.rb` | `TC-002` | command-output + mode header + section markers | low | Overlap (keep representative CLI path) |
| Stats reporting | `test/commands/compress_test.rb`, `test/organisms/compression_runner_test.rb` | `TC-002` | command-output + stats contract strings | low | Overlap (keep representative CLI path) |
| Per-source output mode | `test/commands/compress_test.rb`, `test/organisms/compression_runner_test.rb` | `TC-003` | command-output + filesystem artifacts | low | Covered |
| Compact refusal contract | `test/commands/compress_test.rb`, `test/organisms/compact_compressor_test.rb` | `TC-004` | exit code + refusal/guidance output + stderr contract | low | Overlap (keep for operator-facing refusal behavior) |
| Benchmark comparison | `test/organisms/benchmark_runner_test.rb`, `test/commands/compress_test.rb` | none | unit assertions only | n/a | Unit-only |

### Overlap Analysis

Most smoke E2E checks overlap unit coverage by design, but still provide value as binary-level contract checks:
- `TC-001` validates packaged executable help wiring.
- `TC-002` validates end-to-end stdio/stats behavior from the CLI entrypoint.
- `TC-004` validates refusal semantics as seen by operators (`exit 1`, stderr guidance).

`TC-003` remains clearly E2E-valued due to multi-input ordering and on-disk output behavior.

Current recommendation: keep all 4 TCs as smoke-level representatives, while ensuring deterministic-detail assertions remain in fast/feat layers.

### E2E Decision Record Coverage

| TC ID | Evidence Status | Missing Fields |
|-------|-----------------|----------------|
| TC-001 help surface | complete | none |
| TC-002 exact stdio and stats | complete | none |
| TC-003 per-source output directory | complete | none |
| TC-004 compact refusal contract | complete | none |

### Gap Analysis

| Feature | External Tools | Unit Coverage | E2E Needed? |
|---------|----------------|---------------|-------------|
| Benchmark comparison table/json formatting | none | yes | no (unit/command coverage sufficient for this migration slice) |

### Health Status

| Scenario / TC | Last Verified | Status |
|---------------|---------------|--------|
| TS-COMP-001 / all TCs | not recorded in scenario metadata | Needs refresh run |

### Runner/Verifier Quality Checks

- Runner files are execution-only and avoid verdict language.
- Verifier files are impact-first and contain explicit PASS criteria per artifact.
- Scenario has `e2e-justification` and `unit-coverage-reviewed` entries.

### Recommendations

1. Keep all existing smoke TCs through this migration, but avoid adding deterministic-detail assertions to E2E.
2. During rewrite, update deterministic test path references in scenario metadata from legacy paths to `test/fast/...` (and optional `test/feat/...` if introduced).
3. Record refreshed execution evidence in rewrite/verification artifacts after migration completes.

### Next Step

Run `ace-bundle wfi://e2e/plan-changes` and produce a concrete KEEP/MODIFY/REMOVE/CONSOLIDATE/ADD plan for TS-COMP-001.
