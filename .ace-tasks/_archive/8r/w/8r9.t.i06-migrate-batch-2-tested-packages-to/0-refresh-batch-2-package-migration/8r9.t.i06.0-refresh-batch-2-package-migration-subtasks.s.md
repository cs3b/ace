---
id: 8r9.t.i06.0
status: done
priority: high
created_at: "2026-04-11 17:20:00"
estimate: TBD
dependencies: [8r9.t.i05]
tags: [testing, migration, planning, fast, feat, e2e]
parent: 8r9.t.i06
bundle:
  presets: [project]
  files: [.ace-tasks/8r9.t.i06-migrate-batch-2-tested-packages-to/8r9.t.i06-migrate-batch-2-tested-packages-to-fastfeat.s.md, .ace-tasks/_archive/8r/w/8r9.t.hzr-pilot-restarted-e2e-architecture-on/8r9.t.hzr-pilot-restarted-e2e-architecture-on-ace-b36ts.s.md]
  commands: [ace-task show 8r9.t.i06.0 --content, ace-task show 8r9.t.i06 --content, ace-task show 8r9.t.hzr --content]
needs_review: false
---

# Refresh batch 2 package migration subtasks from pilot outputs

## Objective

Re-ground the batch 2 package subtasks against the `ace-b36ts` pilot outputs and current repo state before implementation begins.

## User Experience

- The batch 2 package tasks become the current source of truth for which packages are fast-only and which require deterministic `feat` coverage.
- An implementer can open any child task and see the concrete verification contract without re-deriving package shape from the repo.
- The refresh step removes stale assumptions about package-local `e2e` ownership before any migration work starts.

## Expected Behavior

1. Reconfirm current test layout, current use of `test/integration`, and whether each package actually needs `test/feat/` or remains fast-only.
2. Encode the discovered split directly in the child tasks:
   - fast+feat: `ace-support-config`, `ace-support-core`, `ace-support-markdown`
   - fast-only: `ace-handbook-integration-claude`, `ace-handbook-integration-codex`, `ace-handbook-integration-gemini`, `ace-handbook-integration-opencode`, `ace-handbook-integration-pi`, `ace-hitl`, `ace-support-cli`, `ace-support-fs`, `ace-support-items`, `ace-support-mac-clipboard`, `ace-support-test-helpers`, `ace-test`
3. Lock package-specific verification commands around `ace-test <package>` and `ace-test <package> feat` only when the package currently owns deterministic feature coverage.
4. Remove stale references to package-owned `ace-test-e2e <package>` and `test/e2e/` expectations from the batch 2 child specs.
5. Preserve explicit package ownership for all batch 2 children.

## Interface Contract

- `ace-task show 8r9.t.i06 --content`
- `ace-task show 8r9.t.i06.0 --content`
- Child task specs under `.ace-tasks/8r9.t.i06-migrate-batch-2-tested-packages-to/`

## Validation Questions

- None. Repo inspection resolved the package split and showed no current batch 2 package-owned scenario surface.

## Success Criteria

- [x] Every batch 2 child task reflects the current repo state.
- [x] Fast-only packages are explicitly marked as such.
- [x] Packages with current `test/integration` are explicitly directed to `test/feat`.
- [x] No child task relies on stale `unit` / `integration` / deterministic `test/e2e` wording.

## Vertical Slice Decomposition

- This task is one planning slice that refreshes the batch 2 package migration specs before package-by-package implementation starts.

## Verification Plan

### Unit / Component Validation

- Re-check the current `test/` layout for every batch 2 package before finalizing child task text.
- Verify the fast+feat list matches the packages that currently have `test/integration/`.

### Failure / Invalid Path Validation

- Confirm no child task documents package-owned `ace-test-e2e <package>` work.
- Confirm no child task assumes `test/e2e/` exists today for any batch 2 package.
