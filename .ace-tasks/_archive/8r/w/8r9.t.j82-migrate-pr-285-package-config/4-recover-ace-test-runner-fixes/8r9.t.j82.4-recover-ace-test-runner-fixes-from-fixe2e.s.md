---
id: 8r9.t.j82.4
status: done
priority: medium
created_at: "2026-04-10 13:01:09"
estimate: TBD
dependencies: [8r9.t.j82.3]
tags: [fix-recovery, fix-e2e, ace-test-runner]
parent: 8r9.t.j82
bundle:
  presets: [project]
  files: [ace-test-runner/test/molecules/package_resolver_test.rb, .ace/test/suite.yml]
  commands: [git diff $(git merge-base main fix/e2e)..fix/e2e -- ace-test-runner/test/molecules/package_resolver_test.rb .ace/test/suite.yml]
needs_review: false
---

# Recover ace-test-runner fixes from fix/e2e

## Objective
- Recover the suite completeness regression guard from `fix/e2e`.

## User Experience
- Maintainers get a failing test when a new testable package is not represented in `.ace/test/suite.yml`.
- The guard stays focused on suite completeness instead of importing unrelated E2E migration behavior.

## Expected Behavior
- `PackageResolver` coverage includes a regression test that compares available testable packages against `.ace/test/suite.yml`.
- The task stays limited to the guardrail and does not pull in E2E migration behavior.

## Interface Contract
- Inputs:
  - `git diff $(git merge-base main fix/e2e)..fix/e2e -- ace-test-runner/test/molecules/package_resolver_test.rb .ace/test/suite.yml`
- Outputs:
  - one regression test that fails when testable packages are missing from the suite config
- Validation conditions:
  - the guard compares discovered package names against configured package names
  - this child remains dependent on the suite inventory child

## Success Criteria
- [x] the suite completeness test is specified
- [x] the task depends on the monorepo config package inventory task

## Verification Plan
- [x] `ace-test ace-test-runner`
- [x] Confirm the regression test reports missing package names when the suite inventory is incomplete.

## Out Of Scope
- modifying package discovery behavior outside the regression guard
- restarted E2E path migration logic
