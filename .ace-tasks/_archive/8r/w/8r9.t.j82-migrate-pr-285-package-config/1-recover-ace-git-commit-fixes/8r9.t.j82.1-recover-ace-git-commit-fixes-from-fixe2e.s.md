---
id: 8r9.t.j82.1
status: done
priority: medium
created_at: "2026-04-10 13:01:09"
estimate: TBD
dependencies: []
tags: [fix-recovery, fix-e2e, ace-git-commit]
parent: 8r9.t.j82
bundle:
  presets: [project]
  files: [ace-git-commit/.ace-defaults/git/commit.yml, ace-git-commit/test/default_config_test.rb]
  commands: [git diff $(git merge-base main fix/e2e)..fix/e2e -- ace-git-commit/.ace-defaults/git/commit.yml ace-git-commit/test/default_config_test.rb]
needs_review: false
---

# Recover ace-git-commit fixes from fix/e2e

## Objective
- Restore the packaged default config that makes config-scope auto-splitting explicit in `ace-git-commit`.

## User Experience
- A fresh install of `ace-git-commit` advertises config-scope auto-splitting as the packaged default.
- Users can see from the default config text that `--no-split` is the supported override for forcing one commit.

## Expected Behavior
- Packaged defaults enable split commits by config scope.
- The default config documents `--no-split` as the override.
- A regression test proves the packaged default values are present.

## Interface Contract
- Inputs:
  - `git diff $(git merge-base main fix/e2e)..fix/e2e -- ace-git-commit/.ace-defaults/git/commit.yml ace-git-commit/test/default_config_test.rb`
- Outputs:
  - packaged default config values for `git.split.enabled`, `git.split.strategy`, and descriptive override guidance
- Validation conditions:
  - defaults express the intended contract without requiring runtime behavior changes in this slice

## Success Criteria
- [x] `git.split.enabled` is specified as `true`.
- [x] `git.split.strategy` is specified as `config-scope`.
- [x] The regression test for packaged defaults is included.

## Verification Plan
- [x] `ace-test ace-git-commit`
- [x] Confirm the bundled test asserts the `--no-split` override text.

## Out Of Scope
- changing the split algorithm itself
- any runtime changes outside packaged defaults and the regression test
