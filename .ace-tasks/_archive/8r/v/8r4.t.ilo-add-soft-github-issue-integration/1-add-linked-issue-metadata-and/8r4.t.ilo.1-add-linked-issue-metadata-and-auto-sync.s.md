---
id: 8r4.t.ilo.1
status: done
priority: medium
created_at: "2026-04-05 12:24:09"
estimate: TBD
dependencies: [8r4.t.ilo.2]
tags: []
reviewed_by: [User]
review_completed: 2026-04-05
parent: 8r4.t.ilo
bundle:
  presets: [project]
  files: [ace-task/lib/ace/task/cli/commands/create.rb, ace-task/lib/ace/task/cli/commands/update.rb, ace-task/lib/ace/task/organisms/task_manager.rb, ace-task/lib/ace/task/atoms/task_frontmatter_defaults.rb, ace-task/lib/ace/task/molecules/task_frontmatter_validator.rb]
  commands: [ace-task create "Example" --github-issue 276, ace-task update t.example --set status=done, ace-task github-sync t.example]
needs_review: false
---

# Add linked issue metadata and auto sync hooks

## Objective

Teach `ace-task` to understand linked GitHub issue metadata and automatically invoke sync behavior whenever a linked task changes.

## Behavioral Specification

### User Experience

- A user can create a task with `--github-issue` and have the link stored in task metadata.
- A user can update, move, archive, or reparent a linked task without manually revisiting the GitHub issue.
- A user can run a dedicated sync command to backfill or repair GitHub issue tracking for linked tasks.

### Expected Behavior

1. `ace-task create` accepts GitHub issue IDs and stores them in machine-readable frontmatter.
2. `ace-task` supports a dedicated sync command for one task or all linked tasks.
3. Relevant task mutations automatically invoke GitHub issue sync for linked tasks only.
4. Sync triggers include create, title and status updates, path-changing moves, archive moves, and reparenting.
5. Tasks without linked GitHub issues remain unaffected.

### Interface Contract

- **CLI contract**
  ```bash
  ace-task create "Example" --github-issue 276
  ace-task create "Example" --github-issue 276
  ace-task github-sync t.abc
  ace-task github-sync --all
  ```
- **Frontmatter contract**
  ```yml
github_issue: 276
  ```
- **Sync trigger contract**
  - create, update, move, reparent, and archive operations sync linked issues automatically
  - no sync occurs when `github_issue` is absent

### Success Criteria

- [x] Users can add linked GitHub issue IDs through `ace-task` CLI and frontmatter.
- [x] Linked task mutations trigger sync automatically.
- [x] Manual repair and backfill are available through `ace-task github-sync`.
- [x] Frontmatter validation and docs accept the new GitHub linkage metadata cleanly.

### Validation Questions

- Should future iterations accept full `owner/repo#123` identifiers, or keep v1 scoped to same-repo numeric issue IDs?

## Review Questions (Resolved)

### [RESOLVED] Should future iterations accept full `owner/repo#123` identifiers, or keep v1 scoped to same-repo numeric issue IDs?
- **Decision**: Keep v1 scoped to same-repo numeric issue IDs.
- **Rationale**: This minimizes parsing and validation complexity while keeping v1 behavior stable.
- **Implications**: `ace-task --github-issue` accepts numeric IDs in v1, and cross-repo issue references are deferred to a later phase.
- **Resolved by**: User
- **Date**: 2026-04-05

## Review Completion Summary

**Date**: 2026-04-05
**Reviewed by**: User
**Questions Resolved**: 1 (1 MEDIUM)
**Implementation Readiness**: ✅ Ready for implementation

**Key Decisions Made**:
- Maintain numeric same-repo issue IDs for `github_issue` and `--github-issue` in v1.
- Defer full cross-repo identifier support to a later enhancement phase.

## Vertical Slice Decomposition (Task/Subtask Model)

- **Slice type**: subtask
- **Slice outcome**: `ace-task` becomes aware of linked GitHub issues and orchestrates sync at the right lifecycle points
- **Advisory size**: medium
- **Context dependencies**: task CLI, task metadata defaults and validation, task manager lifecycle hooks, `ace-git` issue sync primitives

## Verification Plan

### Unit / Component Validation

- Verify `--github-issue` input is normalized into `github_issue`.
- Verify frontmatter validators accept valid issue linkage and reject malformed values.
- Verify auto-sync is triggered only for linked tasks.

### Integration / E2E Validation

- Create a linked task and verify that sync is invoked automatically.
- Update a linked task title or status and verify sync runs again.
- Move, archive, or reparent a linked task and verify sync runs with the new task path and ref context.
- Run `ace-task github-sync --all` and verify linked tasks are reconciled.

### Failure / Invalid Path Validation

- Invalid issue IDs produce clear task CLI validation errors.
- GitHub sync failure does not corrupt task metadata or prevent the task file update from being written.

### Verification Commands

- `ace-task create "Example" --github-issue 276`
- `ace-task update t.example --set status=done`
- `ace-task github-sync --all`

## Scope of Work

- create-time GitHub issue linkage
- linked-task metadata storage and validation
- automatic sync hook orchestration inside `ace-task`
- explicit manual sync command

## Deliverables

### Behavioral Specifications

- create, update, and github-sync command behaviors
- linked-task sync trigger rules
- error behavior for invalid issue IDs and sync failures

### Validation Artifacts

- linked task lifecycle scenarios

## Out of Scope

- direct GitHub CLI implementation details
- GitHub issue label and comment rendering rules themselves

## References

- parent task `8r4.t.ilo`
- dependency subtask `8r4.t.ilo.2`
