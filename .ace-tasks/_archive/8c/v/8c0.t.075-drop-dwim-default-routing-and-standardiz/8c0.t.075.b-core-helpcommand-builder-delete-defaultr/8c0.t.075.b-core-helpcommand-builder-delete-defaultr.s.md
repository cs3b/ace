---
id: 8c0.t.075.b
title: "Core: HelpCommand builder + delete DefaultRouting"
status: done
priority: high
created_at: "2025-01-01 00:07:55"
estimate: 
dependencies: []
tags: [migrated]
parent: 8c0.t.075
---

# Core: HelpCommand builder + delete DefaultRouting

## Scope

Build the shared `HelpCommand.build()` infrastructure in ace-support-core that all multi-command CLI tools will use, mirroring the existing `VersionCommand.build()` pattern. Then delete `DefaultRouting` module, `HelpRouter`, and all related support code.

**CLI module:** `ace-support-core/lib/ace/core/cli/dry_cli/`
**Pattern:** n/a (infrastructure)
**Blocks:** All other subtasks (.03-.28)

## Deliverables

- [ ] Create `HelpCommand.build(program_name:, version:, commands:, examples:)` in ace-support-core
- [ ] HelpCommand outputs: version line, Commands section, Examples section, Options section (--help/-h, --version)
- [ ] HelpCommand exits with code 0
- [ ] Register both `--help` and `-h` as commands (like `--version` pattern)
- [ ] Delete `DefaultRouting` module (`ace-support-core/lib/ace/core/cli/dry_cli/default_routing.rb`)
- [ ] Delete `HelpRouter` if it exists
- [ ] Delete `KNOWN_COMMANDS`, `DEFAULT_COMMAND` constant references from support code
- [ ] Multi-command tools with no args show help (same as --help)
- [ ] All ace-support-core tests pass

## Acceptance Criteria

- [ ] `HelpCommand.build()` produces formatted output matching the interface contract in .01 spec
- [ ] `DefaultRouting` module no longer exists in codebase
- [ ] Existing `VersionCommand.build()` continues to work unchanged
- [ ] `ace-test` passes in ace-support-core