---
id: 8c0.t.03t.b
title: Unified subject resolution in ace-review
status: done
priority: medium
created_at: "2025-01-01 00:04:13"
estimate: 3h
dependencies: [8c0.t.03t.a]
tags: [migrated]
parent: 8c0.t.03t
---

# Unified subject resolution in ace-review

## Objective

Enhance SubjectExtractor to parse `type:value` syntax and delegate all content resolution to ace-context.

## Behavioral Specification

### User Experience

```bash
# Clean type:value syntax
ace-review --preset security --subject diff:origin/main..HEAD
ace-review --preset code --subject pr:123
ace-review --preset docs --subject files:docs/**/*.md
ace-review --preset spec --subject task:145

# Keywords (existing, still work)
ace-review --preset code --subject staged
ace-review --preset code --subject working

# Backward compatible (existing, still work)
ace-review --subject 'diffs: ["origin/main..HEAD"]'
ace-review --subject "HEAD~5..HEAD"
```

### Subject Type Resolution

| Input | Resolves To (ace-context config) |
|-------|----------------------------------|
| `diff:range` | `{ "diffs" => ["range"] }` |
| `pr:123` | `{ "pr" => 123 }` |
| `files:pattern` | `{ "files" => ["pattern"] }` |
| `task:ref` | Task lookup → relevant files |
| `staged` | `{ "diffs" => ["--staged"] }` |
| `working` | `{ "diffs" => [""] }` (unstaged) |

### Parsing Precedence

Subject input is parsed in this order (first match wins):

1. **Typed subjects** (`type:value`) - explicit, highest priority
2. **Keywords** (`staged`, `working`) - convenience shortcuts
3. **YAML detection** - starts with `{` or contains valid YAML keys
4. **Auto-detect** - git range patterns, file globs

### Keyword Behavior Note

`staged` and `working` keywords map to ace-context diffs configuration for consistency with typed subjects. This differs from the current direct `@git.staged_diff` call in SubjectExtractor, but ensures all subject resolution flows through ace-context.

### Subtask Resolution

For subtask references like `task:145.01`:
- Resolution finds the subtask file (e.g., `145.01-context-pr-support.s.md`)
- Returns files pattern for the subtask directory only
- Does NOT include parent task files
- Glob pattern: `{subtask_dir}/**/*`

Example: `task:145.01` → `{ "files" => [".ace-taskflow/v.0.9.0/tasks/145-feat-review-ace/145.01*/**/*"] }`

### Error Messages

**Unknown type:**
```
Error: Unknown subject type 'commit'.

Supported types: diff, pr, files, task
Did you mean: diff:abc123 (for commit range)?
```

**Invalid format:**
```
Error: Invalid subject format. Expected 'type:value' or keyword.

Valid formats:
  --subject diff:<range>     Git diff range
  --subject pr:<number>      GitHub PR
  --subject files:<pattern>  File pattern
  --subject task:<ref>       Task reference
  --subject staged           Staged changes
  --subject working          Unstaged changes
```

## Success Criteria

- [ ] `diff:range` → ace-context diffs config
- [ ] `pr:id` → ace-context pr config (delegates to 145.01)
- [ ] `files:pattern` → ace-context files config
- [ ] `task:ref` → task lookup + ace-context
- [ ] `staged`/`working` keywords still work
- [ ] Backward compatible with YAML syntax
- [ ] Clear error messages with examples
- [ ] README.md updated with new syntax

## Implementation Notes

### Affected Components

1. **SubjectExtractor** (`lib/ace/review/molecules/subject_extractor.rb`)
   - Add `parse_typed_subject(input)` method
   - Called first in `extract_from_string`
   - Returns ace-context config hash or nil (fall through)

### Simple Implementation

```ruby
def extract_from_string(input)
  # Try typed subject first (new)
  if (typed_config = parse_typed_subject(input))
    return use_ace_context(typed_config)
  end

  # Existing logic unchanged...
  # YAML parsing, keywords, auto-detection
end

def parse_typed_subject(input)
  case input
  when /^diff:(.+)$/
    { "diffs" => [$1] }
  when /^pr:(.+)$/
    { "pr" => $1 }  # ace-context handles the rest (145.01)
  when /^files:(.+)$/
    { "files" => $1.split(",").map(&:strip) }
  when /^task:(.+)$/
    resolve_task_subject($1)
  else
    nil  # Fall through to existing parsing
  end
end

def resolve_task_subject(ref)
  # Validate ref format to prevent injection (alphanumeric, dots, dashes only)
  unless ref.match?(/\A[\w.\-]+\z/)
    raise ArgumentError, "Invalid task reference format: #{ref}"
  end

  # Use Open3 with array args for safe subprocess execution
  require 'open3'
  stdout, stderr, status = Open3.capture3("ace-taskflow", "task", ref, "--path")

  if !status.success?
    raise TaskNotFoundError, "Task '#{ref}' not found. Run `ace-taskflow task #{ref}` for details."
  end

  task_path = stdout.strip
  if task_path.empty?
    raise TaskPathNotFoundError, "Task '#{ref}' exists but has no path. Check task status."
  end

  { "files" => ["#{File.dirname(task_path)}/**/*"] }
end
```

### Key Simplification

**No custom PR logic needed** - `pr:123` simply becomes `{ "pr" => 123 }` and ace-context (145.01) handles all the `gh` CLI interaction.

## Out of Scope

- Subject presets (`--subject-preset`) - Phase 2
- New subject types beyond diff, pr, files, task
- Changes to `--pr` flag behavior (stays as full PR mode)

### Note: `--pr` vs `--subject pr:`

| Flag | Behavior |
|------|----------|
| `--pr 123` | Full PR mode using GhPrFetcher directly (existing behavior) |
| `--subject pr:123` | Subject-only mode via ace-context (new, 145.01) |

The `--pr` flag continues to use ace-review's GhPrFetcher for full PR mode (includes metadata, comments). The `--subject pr:` syntax only fetches diff content through ace-context.

## Error Classes

Define custom errors for task resolution:

```ruby
class TaskNotFoundError < StandardError; end
class TaskPathNotFoundError < StandardError; end
```

## References

- Parent task: 145-orchestrator.s.md
- UX Specification: `ux/145.02-unified-subject-usage.md`
- Current SubjectExtractor: `ace-review/lib/ace/review/molecules/subject_extractor.rb`
- Depends on: 145.01 (ace-context pr: support)

---

## Technical Approach

### Architecture Pattern

**Pattern Selected:** Parsing layer with delegation to ace-context

**Rationale:**
- **Parsing Layer:** Add `parse_typed_subject` as first step in `extract_from_string`
- **Delegation Pattern:** All typed subjects convert to ace-context config hashes
- **Backward Compatibility:** Existing YAML/keyword/auto-detect logic remains unchanged as fallback
- **Security:** Input validation for task references prevents command injection

**Integration with Existing Architecture:**
- Minimal changes to SubjectExtractor (add one method, update one method)
- Follows existing pattern: parse → convert to hash → delegate to ace-context
- Reuses existing `use_ace_context` method (no duplication)
- Keywords (`staged`, `working`) now flow through ace-context for consistency

**Impact on System Design:**
- No breaking changes - purely additive feature
- Parsing precedence: typed → keywords → YAML → auto-detect
- Error handling: custom error classes for task resolution
- All subject types unified through ace-context delegation

### Technology Stack

**Libraries/Frameworks:**
- **Ruby stdlib (Open3):** Safe command execution for ace-taskflow calls
- **ace-context:** Unified content resolution (existing dependency)
- **ace-taskflow:** Task path resolution (external tool, optional)

**Version Compatibility:**
- Ruby 3.1+ (existing requirement)
- ace-context ~> 0.9 (existing dependency, requires 145.01 for pr: support)
- ace-taskflow >= 0.9.0 (external tool, must support `task <ref> --path`)

**Performance Implications:**
- Parsing overhead: Negligible (single regex match + case statement)
- Task resolution: Subprocess call to ace-taskflow (~50-100ms)
- No impact on existing subject types (same code path)

**Security Considerations:**
- Input validation: Task references validated with strict regex (`/\A[\w.\-]+\z/`)
- Safe execution: Open3.capture3 with array args (no shell interpolation)
- No credential handling: PR auth delegated to ace-context/gh CLI

### Implementation Strategy

**Step-by-step Approach:**
1. Add custom error classes to errors.rb
2. Implement `parse_typed_subject` method (pattern matching)
3. Implement `resolve_task_subject` method (ace-taskflow integration)
4. Update `extract_from_string` to call `parse_typed_subject` first
5. Add comprehensive test coverage (typed subjects, errors, edge cases)
6. Update README with new syntax examples

**Rollback Considerations:**
- No database changes - purely code-level feature
- Can be disabled by removing typed subject usage (backward compatible)
- No migration needed if feature is removed
- Keywords maintain current behavior (staged/working still work)

**Testing Strategy:**
- Unit tests: Each subject type (diff, pr, files, task)
- Integration tests: ace-context delegation, task resolution
- Error tests: Unknown type, invalid format, task not found
- Backward compatibility: YAML, keywords, auto-detect still work
- Edge cases: Empty values, special characters, multiple files

**Performance Monitoring:**
- No additional monitoring needed (parsing is fast)
- Task resolution may add 50-100ms for `task:` subjects
- Debug logging for typed subject parsing (if ACE_REVIEW_DEBUG=1)

## File Modifications

### Modify

#### 1. ace-review/lib/ace/review/errors.rb
- **Changes:**
  - Add `TaskNotFoundError < Error` class (line ~91)
  - Add `TaskPathNotFoundError < Error` class (line ~99)
  - Follow existing pattern (attr_reader for context, custom message)
- **Impact:** Provides clear error messages for task resolution failures
- **Integration Points:** Raised by `resolve_task_subject` in SubjectExtractor

**Error Classes to Add:**
```ruby
# Raised when ace-taskflow cannot find task
class TaskNotFoundError < Error
  attr_reader :task_ref

  def initialize(task_ref)
    @task_ref = task_ref
    message = "Task '#{task_ref}' not found.\n"
    message += "Run 'ace-taskflow task #{task_ref}' for details."
    super(message)
  end
end

# Raised when task exists but has no path
class TaskPathNotFoundError < Error
  attr_reader :task_ref

  def initialize(task_ref)
    @task_ref = task_ref
    message = "Task '#{task_ref}' exists but has no path.\n"
    message += "Check task status with: ace-taskflow task #{task_ref}"
    super(message)
  end
end
```

#### 2. ace-review/lib/ace/review/molecules/subject_extractor.rb
- **Changes:**
  - Update `extract_from_string` to call `parse_typed_subject` first (line ~33)
  - Add `parse_typed_subject(input)` private method (line ~67)
  - Add `resolve_task_subject(ref)` private method (line ~82)
  - Update keywords to use ace-context (lines 43-51)
  - Add require for Open3 (line ~3)
  - Add require for errors (line ~4)
- **Impact:** Enables typed subject parsing while maintaining backward compatibility
- **Integration Points:**
  - Calls `use_ace_context` for all typed subjects
  - Calls `ace-taskflow` subprocess for task resolution
  - Falls through to existing logic for YAML/auto-detect

**Changes in extract_from_string:**
```ruby
def extract_from_string(input)
  # Try typed subject first (new)
  if (typed_config = parse_typed_subject(input))
    return use_ace_context(typed_config)
  end

  # Try to parse as YAML first
  begin
    parsed = YAML.safe_load(input)
    return extract_from_hash(parsed) if parsed.is_a?(Hash)
  rescue Psych::SyntaxError
    # Continue with string processing
  end

  # Handle special keywords (updated to use ace-context)
  case input.downcase
  when "staged"
    return use_ace_context({ "diffs" => ["--staged"] })
  when "working", "unstaged"
    return use_ace_context({ "diffs" => [""] })
  when "pr", "pull-request"
    tracking = @git.tracking_branch
    range = tracking ? "#{tracking}...HEAD" : "origin/main...HEAD"
    return use_ace_context({ "diffs" => [range] })
  end

  # Check if it's a git range
  if looks_like_git_range?(input)
    return use_ace_context({ "diffs" => [input] })
  end

  # Check if it's a file pattern
  if input.include?("*") || input.include?("/")
    return use_ace_context({ "files" => [input] })
  end

  # Default to git diff
  use_ace_context({ "diffs" => [input] })
end
```

#### 3. ace-review/test/molecules/subject_extractor_test.rb
- **Changes:**
  - Add test cases for typed subjects (diff:, pr:, files:, task:)
  - Add test cases for error scenarios
  - Add test cases for edge cases (empty, special chars)
  - Add test for backward compatibility (YAML, keywords still work)
- **Impact:** Ensures typed subject parsing works correctly
- **Integration Points:** Mocks ace-context and ace-taskflow calls

**Test Cases to Add:**
- `test_diff_typed_subject` - diff:origin/main..HEAD
- `test_pr_typed_subject` - pr:123
- `test_files_typed_subject_single` - files:lib/**/*.rb
- `test_files_typed_subject_multiple` - files:lib/**/*.rb,test/**/*
- `test_task_typed_subject` - task:145
- `test_task_typed_subject_subtask` - task:145.02
- `test_task_not_found_error` - Invalid task raises TaskNotFoundError
- `test_task_no_path_error` - Task with no path raises TaskPathNotFoundError
- `test_keywords_use_ace_context` - staged/working flow through ace-context
- `test_backward_compatible_yaml` - YAML syntax still works
- `test_backward_compatible_auto_detect` - Auto-detect still works
- `test_unknown_typed_subject` - Returns nil, falls through
- `test_empty_value` - diff: (empty value) handled gracefully

#### 4. ace-review/README.md
- **Changes:**
  - Add "Unified Subject Syntax" section (after "Subject and Context Configuration")
  - Document type:value syntax with examples
  - Add examples for each subject type (diff, pr, files, task)
  - Document parsing precedence
  - Note backward compatibility
- **Impact:** Users discover and understand new syntax
- **Integration Points:** Links to ux/145.02-unified-subject-usage.md for details

**Section to Add:**
```markdown
### Unified Subject Syntax (v0.24.0+)

Clean, type-safe subject specification using `type:value` syntax:

```bash
# Git diff ranges
ace-review --subject diff:origin/main..HEAD --preset code

# GitHub pull requests (subject-only mode)
ace-review --subject pr:123 --preset code

# File patterns
ace-review --subject files:lib/**/*.rb --preset code

# Task references
ace-review --subject task:145 --preset spec

# Keywords (existing)
ace-review --subject staged --preset code
ace-review --subject working --preset code
```

**Backward Compatible:** YAML syntax and auto-detection still work.

See [Usage Guide](ux/145.02-unified-subject-usage.md) for complete documentation.
```

### Create

#### 1. ux/145.02-unified-subject-usage.md
- **Purpose:** Comprehensive usage documentation for typed subjects
- **Status:** ✅ Created
- **Location:** `.ace-taskflow/v.0.9.0/tasks/145-feat-review-ace/ux/145.02-unified-subject-usage.md`
- **Key Components:**
  - Overview and benefits
  - Command structure and syntax
  - Usage scenarios (6 scenarios with examples)
  - Command reference for each type
  - Tips and troubleshooting
  - Migration notes

## Risk Assessment

### Technical Risks

**Risk:** ace-taskflow not installed on user's system
- **Probability:** Medium (external tool, not always installed)
- **Impact:** Low (only affects `task:` subjects, clear error message)
- **Mitigation:** Provide clear error with installation instructions
- **Rollback:** Users can use other subject types (diff, pr, files)

**Risk:** Task reference validation too strict
- **Probability:** Low (regex pattern covers standard formats)
- **Impact:** Low (clear error, user adjusts format)
- **Mitigation:** Validate against known ace-taskflow formats (numeric, dots, dashes)
- **Rollback:** Relax regex if legitimate formats rejected

**Risk:** Keywords behavior change (staged/working now use ace-context)
- **Probability:** Low (same functional behavior)
- **Impact:** Low (output format unchanged, just routing)
- **Mitigation:** Keep functional behavior identical (same diffs, same output)
- **Rollback:** Revert to direct @git calls if issues arise

### Integration Risks

**Risk:** ace-context pr: support not available (145.01 incomplete)
- **Probability:** Low (dependency enforced)
- **Impact:** Medium (pr: subjects fail)
- **Mitigation:** Dependency on task 145.01 in task metadata
- **Monitoring:** Test pr: subjects in integration tests

**Risk:** Parsing precedence breaks existing behavior
- **Probability:** Low (typed subjects checked first, explicit)
- **Impact:** Medium (could misinterpret existing subjects)
- **Mitigation:** Typed subjects require explicit prefix (low collision risk)
- **Monitoring:** Comprehensive backward compatibility tests

**Risk:** Error messages not helpful for users
- **Probability:** Low (following existing error patterns)
- **Impact:** Low (user confusion, easy to fix)
- **Mitigation:** Clear error classes with examples and suggestions
- **Monitoring:** User feedback, error tracking

### Performance Risks

**Risk:** Task resolution subprocess adds latency
- **Mitigation:** Accept 50-100ms overhead for task: subjects (acceptable)
- **Monitoring:** No specific monitoring needed (one-time cost)
- **Thresholds:** Acceptable if <200ms (subprocess spawn + lookup)

**Risk:** Parsing regex slows down subject extraction
- **Mitigation:** Regex is simple, compiled once, negligible overhead
- **Monitoring:** No monitoring needed (microseconds)
- **Thresholds:** N/A (parsing overhead unmeasurable)

## Implementation Plan

### Planning Steps

* [x] Analyze existing SubjectExtractor implementation
  - Reviewed current parsing logic and delegation pattern
  - Identified integration points for typed subjects
  - Confirmed ace-context delegation pattern

* [x] Design typed subject parsing approach
  - Pattern matching with case statement
  - Parsing precedence: typed → keywords → YAML → auto-detect
  - Backward compatibility maintained (existing logic as fallback)

* [x] Plan task resolution integration
  - Use ace-taskflow task <ref> --path for path lookup
  - Validate input with strict regex (security)
  - Safe subprocess execution with Open3.capture3

* [x] Design error handling strategy
  - Custom error classes: TaskNotFoundError, TaskPathNotFoundError
  - Clear messages with actionable suggestions
  - Follow existing error pattern in errors.rb

* [x] Create comprehensive usage documentation
  - Created ux/145.02-unified-subject-usage.md
  - Documented all subject types and scenarios
  - Included troubleshooting and migration guidance

### Execution Steps

- [x] Add custom error classes to ace-review/lib/ace/review/errors.rb
  - Add TaskNotFoundError class
  - Add TaskPathNotFoundError class
  - Follow existing error pattern (attr_reader, custom message)
  > TEST: Error Classes
  > Type: Unit Test
  > Assert: Error classes instantiate with correct messages
  > Command: cd ace-review && ace-test test/errors_test.rb --filter Task

- [x] Implement parse_typed_subject method in SubjectExtractor
  - Add private method parse_typed_subject(input)
  - Pattern match: diff:, pr:, files:, task:
  - Return ace-context config hash or nil (fall through)
  - Add require for Open3 and errors
  > TEST: Typed Subject Parsing
  > Type: Unit Test
  > Assert: Each subject type (diff, pr, files, task) parses to correct config hash
  > Command: cd ace-review && ace-test test/molecules/subject_extractor_test.rb --filter typed

- [x] Implement resolve_task_subject method in SubjectExtractor
  - Add private method resolve_task_subject(ref)
  - Validate ref format with strict regex
  - Call ace-taskflow task <ref> --path via Open3.capture3
  - Handle errors: command failed, empty path
  - Return files config hash with task directory glob
  > TEST: Task Resolution
  > Type: Integration Test (mock subprocess)
  > Assert: Task path resolves correctly, errors raised appropriately
  > Command: cd ace-review && ace-test test/molecules/subject_extractor_test.rb --filter task_resolution

- [x] Update extract_from_string to call parse_typed_subject first
  - Add typed subject parsing as first step
  - Return result if non-nil
  - Fall through to existing logic (YAML, keywords, auto-detect)
  - Update keywords to use ace-context (staged, working)
  > TEST: Parsing Precedence
  > Type: Integration Test
  > Assert: Typed subjects take precedence, fallback logic still works
  > Command: cd ace-review && ace-test test/molecules/subject_extractor_test.rb --filter precedence

- [x] Add comprehensive test coverage for typed subjects
  - Test each subject type (diff:, pr:, files:, task:)
  - Test error scenarios (unknown type, task not found, invalid format)
  - Test edge cases (empty values, special characters, multiple files)
  - Test backward compatibility (YAML, keywords, auto-detect)
  - Mock ace-context and ace-taskflow calls
  > TEST: Complete Test Coverage
  > Type: Unit + Integration Tests
  > Assert: All subject types, errors, edge cases covered
  > Command: cd ace-review && ace-test test/molecules/subject_extractor_test.rb

- [x] Update README.md with unified subject syntax documentation
  - Add "Unified Subject Syntax" section
  - Document type:value format with examples
  - Note backward compatibility
  - Link to ux/145.02-unified-subject-usage.md
  > TEST: Documentation Accuracy
  > Type: Manual Validation
  > Assert: Examples in README work correctly
  > Command: cd ace-review && ace-review --subject diff:HEAD~1..HEAD --dry-run

- [x] Run full test suite to ensure no regressions
  > TEST: Full Regression Suite
  > Type: Regression Test
  > Assert: All existing tests pass (unit, molecules, organisms)
  > Command: cd ace-review && ace-test

- [x] Manual validation of end-to-end scenarios
  - Test each subject type with real ace-review commands
  - Verify error messages display correctly
  - Confirm backward compatibility (YAML, keywords work)
  > TEST: End-to-End Validation
  > Type: Manual Test
  > Assert: All subject types work in real scenarios
  > Command:
  > ace-review --subject diff:HEAD~1..HEAD --dry-run
  > ace-review --subject files:lib/**/*.rb --dry-run
  > ace-review --subject staged --dry-run

## Acceptance Criteria

- [x] `diff:range` syntax works → ace-context diffs config
- [x] `pr:id` syntax works → ace-context pr config (delegates to 145.01)
- [x] `files:pattern` syntax works → ace-context files config
- [x] `files:pattern1,pattern2` multiple files work → ace-context files array
- [x] `task:ref` syntax works → task lookup + ace-context files config
- [x] `task:145.02` subtask works → ONLY subtask directory files (not parent)
- [x] `staged` keyword still works → flows through ace-context
- [x] `working` keyword still works → flows through ace-context
- [x] Backward compatible with YAML syntax (existing configs unchanged)
- [x] Backward compatible with auto-detect (git ranges, file patterns)
- [x] Clear error messages for unknown types (with suggestions)
- [x] Clear error messages for task not found (with ace-taskflow command)
- [x] Clear error messages for task with no path
- [x] README.md updated with new syntax examples
- [x] Usage documentation created (ux/145.02-unified-subject-usage.md)
- [x] All tests pass (unit, molecules, integration)
- [x] No regressions in existing subject parsing

## Out of Scope

- Subject presets (`--subject-preset`) - Phase 2
- New subject types beyond diff, pr, files, task
- Changes to `--pr` flag behavior (stays as full PR mode)
- Validation of task reference format beyond regex (delegated to ace-taskflow)
- Custom error handling for ace-context failures (existing pattern sufficient)