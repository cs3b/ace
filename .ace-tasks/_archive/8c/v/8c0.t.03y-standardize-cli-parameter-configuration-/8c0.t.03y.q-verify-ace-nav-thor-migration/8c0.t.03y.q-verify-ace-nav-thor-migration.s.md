---
id: 8c0.t.03y.q
title: "Verify ace-nav: Thor migration"
status: done
priority: medium
created_at: "2025-01-01 00:04:22"
estimate: 30m
dependencies: [8c0.t.03y.o]
tags: [migrated]
parent: 8c0.t.03y
---

# Verify ace-nav: Thor migration

## Scope

Verify ace-nav Thor migration & CLI standardization. If issues found, propose solution → get user approval → fix → re-verify.

## Verification Checklist

- [ ] `lib/ace/nav/commands/resolve_command.rb` exists
- [ ] `lib/ace/nav/commands/list_command.rb` exists
- [ ] `lib/ace/nav/commands/create_command.rb` exists
- [ ] `class CLI < Ace::Core::CLI::Base` (not Thor directly)
- [ ] `default_task :resolve` set
- [ ] `method_missing` delegates to `resolve`
- [ ] Magic wildcard patterns route to `list` command
- [ ] Backward compat: `--sources` flag mapped
- [ ] Backward compat: `--create` flag mapped
- [ ] ConfigSummary displayed unless `--quiet`
- [ ] `long_desc` with SYNTAX, EXAMPLES, CONFIGURATION, OUTPUT sections
- [ ] `self.help` override with magic pattern documentation

## Manual Tests

```bash
./bin/ace-nav --help              # Should show full help with magic patterns
./bin/ace-nav --version           # Should show "ace-nav X.X.X"
./bin/ace-nav -v                  # Should trigger verbose (NOT version)
./bin/ace-nav wfi://work-on-task  # Should resolve to path
./bin/ace-nav 'wfi://*'           # Should route to list (wildcard)
./bin/ace-nav --sources           # Backward compat flag
```

## Automatic Tests

```bash
cd ace-nav && ace-test
```

## Remediation Process

If any verification check fails:

1. **Document the issue**: What check failed and what the actual behavior is
2. **Propose solution**: Describe the fix needed
3. **Get user approval**: Present proposal and wait for approval
4. **Implement fix**: Make the code changes
5. **Re-verify**: Run tests and manual checks again

## Acceptance Criteria

- [ ] All verification checks pass
- [ ] All manual tests produce expected output
- [ ] All automatic tests pass
- [ ] No issues found OR all issues found and fixed