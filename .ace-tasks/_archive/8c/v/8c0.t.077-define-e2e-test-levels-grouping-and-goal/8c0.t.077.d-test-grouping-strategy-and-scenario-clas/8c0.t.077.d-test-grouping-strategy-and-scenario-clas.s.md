---
id: 8c0.t.077.d
title: Test Grouping Strategy and Scenario Classification
status: done
priority: medium
created_at: "2025-01-01 00:07:59"
estimate: 
dependencies: [8c0.t.077.c]
tags: [migrated]
parent: 8c0.t.077
---

# Test Grouping Strategy and Scenario Classification

## Scope

Define standard tag groups for E2E scenarios, then classify all ~33 existing scenarios with appropriate tags. This subtask uses the tag infrastructure from 280.03 to create a meaningful, queryable test taxonomy.

> **Pilot note**: The ace-b36ts pilot (280.01) consolidated 4 procedural scenarios into 1 goal-mode scenario, reducing the total from 36 to ~33. The cost-tier "smoke" was validated as appropriate for the goal-based scenario.

> **Dependency note**: 280.02 must define what smoke / happy-path / deep mean for goal-mode scenarios (runner config, complexity, verification depth) before this task can identify which existing scenarios are candidates for goal-mode conversion at each tier. The current classification is tag-only; once 280.02 delivers tier semantics, this task should also map conversion candidates per tier.

## Behavioral Spec

### Standard Tag Groups

| Group | Tag | Purpose | Exec Time Target | Selection Criteria |
|-------|-----|---------|------------------|--------------------|
| Smoke | `smoke` | Fast confidence check | <20% of total suite time | Core happy-path per package, minimal setup |
| Happy Path | `happy-path` | Standard functional coverage | ~50% of suite time | Primary use-cases, expected inputs |
| Use-Case | `use-case:<verb-noun>` | Workflow-specific selection | Varies | Agent-driven selection by task context |
| Deep | `deep` | Edge cases, error paths, complex flows | Remaining ~30% | Multi-step workflows, error recovery, boundary conditions |

### Use-Case Tag Vocabulary

Namespaced tags that enable agent-driven test selection:
- `use-case:lint` — Linting and code quality
- `use-case:commit` — Git commit generation
- `use-case:review` — Code review analysis
- `use-case:assign` — Assignment workflow
- `use-case:bundle` — Context loading and bundling
- `use-case:nav` — Resource discovery and navigation
- `use-case:secrets` — Security scanning
- `use-case:worktree` — Git worktree management
- `use-case:overseer` — Task orchestration
- `use-case:prep` — Prompt preparation

### Tag-Based Cost Semantics (Convention)

Cost and depth are expressed through tags only:
- `smoke` = lowest-cost confidence checks
- `happy-path` = standard validation
- `deep` = expensive/edge-path validation

### Classification Requirements

- Every scenario gets at least one group tag (`smoke`, `happy-path`, or `deep`)
- Every package must have at least one `smoke` scenario
- Use-case tags assigned based on the workflow domain
- Multiple group tags allowed (a scenario can be both `smoke` and `happy-path`)

### Proposed Classification

**ace-assign** (6 scenarios):
- TS-ASSIGN-001: `smoke, happy-path, use-case:assign`
- TS-ASSIGN-002–006: `happy-path, use-case:assign` or `deep, use-case:assign`

**ace-b36ts** (1 goal-mode scenario, consolidated from 4 procedural — pilot-validated):
- TS-B36TS-001: `smoke, happy-path, use-case:b36ts, pilot` / mode: goal / 8 goals

**ace-bundle** (3 scenarios):
- TS-BUNDLE-001: `smoke, happy-path, use-case:bundle`
- TS-BUNDLE-002–003: `happy-path, use-case:bundle`

**ace-git-commit** (3 scenarios):
- TS-COMMIT-001: `smoke, happy-path, use-case:commit`
- TS-COMMIT-002, 004a: `happy-path, use-case:commit`

**ace-lint** (3 scenarios):
- TS-LINT-001: `smoke, happy-path, use-case:lint`
- TS-LINT-002–003: `happy-path, use-case:lint` or `deep, use-case:lint`

**ace-support-nav** (3 scenarios):
- TS-NAV-001: `smoke, happy-path, use-case:nav`
- TS-NAV-002–003: `happy-path, use-case:nav`

**ace-overseer** (2 scenarios):
- TS-OVERSEER-001: `smoke, happy-path, use-case:overseer`
- TS-OVERSEER-002: `deep, use-case:overseer`

**ace-prompt-prep** (1 scenario):
- TS-PREP-001: `smoke, happy-path, use-case:prep`

**ace-review** (3 scenarios):
- TS-REVIEW-001: `smoke, happy-path, use-case:review`
- TS-REVIEW-002, 005: `happy-path, use-case:review` or `deep, use-case:review`

**ace-git-secrets** (3 scenarios):
- TS-SECRETS-001: `smoke, happy-path, use-case:secrets`
- TS-SECRETS-002–003: `happy-path, use-case:secrets`

**ace-git-worktree** (2 scenarios):
- TS-WORKTREE-001: `smoke, happy-path, use-case:worktree`
- TS-WORKTREE-002: `deep, use-case:worktree`

### Expected Distribution

- **Smoke**: ~11 scenarios (one per package, ~33% of total)
- **Happy-path**: ~22 scenarios (~67%)
- **Deep**: ~8 scenarios (~24%)
- **Use-case tags**: All ~33 scenarios tagged by domain
- **Goal-mode**: 1 scenario (ace-b36ts pilot); more candidates after 280.05

## Deliverables

- [x] Standard tag groups documented with selection criteria
- [x] Use-case tag vocabulary defined
- [x] All ~33 scenarios classified with tags in scenario.yml files
- [x] Each package has at least one smoke scenario
- [x] Tag conventions for cost/depth documented and applied consistently
- [x] Classification summary table in task docs

## Acceptance Criteria

- [x] `ace-test-e2e-suite --tags smoke --dry-run` returns ~11 scenarios across all packages (including 1 goal-mode)
- [x] Every package represented in smoke tier
- [x] `ace-test-e2e-suite --tags use-case:lint --dry-run` returns exactly the ace-lint scenarios
- [x] No untagged scenarios remain after classification
- [x] Classification rationale documented for non-obvious assignments