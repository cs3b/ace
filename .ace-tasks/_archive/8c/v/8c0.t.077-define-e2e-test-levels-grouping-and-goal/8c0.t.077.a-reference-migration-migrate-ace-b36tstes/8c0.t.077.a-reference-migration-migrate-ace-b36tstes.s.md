---
id: 8c0.t.077.a
title: "Reference Migration: Migrate `ace-b36ts/test/e2e/` to Goal-Mode TC Structure"
status: done
priority: medium
created_at: "2025-01-01 00:07:59"
estimate: 
dependencies: []
tags: [migrated]
parent: 8c0.t.077
---

# Reference Migration: Migrate `ace-b36ts/test/e2e/` to Goal-Mode TC Structure

## Context

Task 280 defines a new E2E testing approach: scenario-level tags, goal-mode test cases, and optional independent verification. This subtask is the first live migration and the reference implementation for migrating other packages.

**Why ace-b36ts?** Simple tool, strong unit coverage (276 tests, 1022 assertions), and existing E2E tests that expose the exact problem Task 280 aims to solve.

**The problem:** The current 4 scenarios / 11 TCs massively duplicate unit tests. They test format lengths, sortability, config display — all thoroughly covered by unit tests. Real E2E value (binary contract, shell pipe behavior, tool integration) is buried and diluted.

## Design Principles

This pilot adopts an **all-goal, runner/verifier split** approach:

1. **All goal-oriented** — No procedural steps, no hardcoded commands. Specs describe WHAT to achieve, not HOW.
2. **Runner/verifier split** — `.runner.md` gives the agent a goal and constraints; `.verify.md` inspects artifacts independently.
3. **Sequential learning model** — A single agent executes all goals in order with accumulated context. Goal 1 is the discovery phase (`--help` exploration). Goals 2–8 build on that discovery — they reference earlier goals instead of re-discovering the tool independently. This mirrors how a real user learns a tool: explore once, then apply.
4. **Artifact-based verification** — Runner writes outputs to `results/tc/{NN}/` folders. Verifier reads those artifacts and renders a PASS/FAIL verdict.
5. **Loosely coupled** — If `--help` output changes and the tool still works, tests pass. If it changes and breaks behavior, tests fail naturally.
6. **Progressive complexity** — Goals form an ascending difficulty arc: discovery → basic operations → error handling → output control → composition → batch properties. Each goal builds on what the agent learned and produced in previous goals.

## Critical Analysis of Current E2E Tests

| Current Scenario | TCs | Verdict |
|-----------------|-----|---------|
| TS-B36TS-001 Core Roundtrip | 3 | **Replace** — format lengths + sortability + decode formats all unit-tested |
| TS-B36TS-002 Config & Defaults | 3 | **Replace** — config display + year-zero unit-tested. Error handling has E2E value → absorb |
| TS-B36TS-003 Split Format | 3 | **Replace** — split levels unit-tested. Output modes (JSON, path-only) → absorb |
| TS-B36TS-004 CLI Integration | 3 | **Replace** — help/version trivial. Pipeline → absorb into new roundtrip |

## New Design: 1 Scenario, 8 Goal-Mode TCs

All goal-mode TCs live in a single scenario (`TS-B36TS-001-pilot`) with the runner/verifier split.

### Goal 1 — Help Survey
- **Runner**: Explore `ace-b36ts --help` at every level. Summarize subcommands, flags, and note anything unclear.
- **Verifier**: File exists in `results/tc/01/`, contains substantive observations about the tool, mentions subcommands.

### Goal 2 — Encode Today's Date
- **Runner**: Encode today's date using the tool. Create a file whose filename IS the encoded token.
- **Verifier**: Exactly one file in `results/tc/02/`, filename is valid base36 (lowercase alphanumeric, 2–8 chars).

### Goal 3 — Decode Token
- **Runner**: Decode the token `i50jj3`. Save a file named `i50jj3` with the decoded result as content.
- **Verifier**: File `results/tc/03/i50jj3` exists, content is a valid, plausible date/timestamp.

### Goal 4 — Error Behavior
- **Runner**: Feed the tool clearly invalid input (nonsense strings, wrong subcommands). For each, capture the exit code, stdout, and stderr into separate files in `results/tc/04/`.
- **Verifier**: At least 2 error-case files exist. Each shows: non-zero exit code, error message on stderr, stdout is empty or clean.
- **E2E value**: Tests the full exception→message→exit-code path through the binary wrapper — invisible to unit tests that call Ruby methods.

### Goal 5 — Output Routing
- **Runner**: Run the same encode operation in different verbosity modes (discover modes via `--help`). For each mode, capture stdout and stderr into separate files in `results/tc/05/`.
- **Verifier**: At least 2 mode pairs exist. Quiet mode: stdout contains only the token. Verbose/default: stderr has additional content. Stream separation is correct.
- **E2E value**: fd routing only breaks in real shell execution — unit tests don't exercise the binary's output stream wiring.

### Goal 6 — Structured Output Integration
- **Runner**: Discover the tool's structured output formats via `--help`. Use one format's output directly as input to a real downstream tool (e.g., `mkdir` from path output, `jq` on JSON). Save evidence in `results/tc/06/`.
- **Verifier**: At least one integration artifact exists proving a downstream tool consumed the output successfully. No manual string munging was required.
- **E2E value**: Proves output is machine-usable by real tools, not just correctly formatted strings.

### Goal 7 — Roundtrip Pipeline
- **Runner**: Encode a known date, then pipe/feed the result into decode — all through shell pipeline or command substitution. Save original date, encoded token, and decoded result in `results/tc/07/`.
- **Verifier**: File exists with all three values. Decoded result contains the original date. Token is valid base36. No trailing whitespace corruption.
- **E2E value**: Tests pipe behavior, trailing whitespace, and command substitution — things that only break in real shell execution.

### Goal 8 — Batch Sort Order
- **Runner**: Encode at least 4 timestamps from different dates (not in chronological order). Save two files in `results/tc/08/`: one with IDs in encode order, one lexicographically sorted.
- **Verifier**: Both files exist with at least 4 IDs. Sorted order matches chronological order of original dates.
- **E2E value**: Sortability is a key design property of b36ts tokens — this tests it through the actual binary output, not Ruby method return values.

### Tag/Tier Summary

- **smoke**: All 8 goals (single scenario, smoke cost-tier)
- **happy-path**: Goals 1–3, 7
- **error-path**: Goal 4
- **use-case:b36ts**: All 8 goals
- **pilot**: All 8 goals
- **mode**: goal (all goals are goal-mode, no procedural TCs)

## Pilot Results

**Score**: 6/8 (0.75) — verdict: partial

| Goal | Title | Verdict | Notes |
|------|-------|---------|-------|
| 1 | Help Survey | PASS | |
| 2 | Encode Today | PASS | |
| 3 | Decode Token | FAIL | Token `70000` was invalid — spec error, fixed to `i50jj3` |
| 4 | Error Behavior | PASS | |
| 5 | Output Routing | PASS | |
| 6 | Structured Output | PASS | |
| 7 | Roundtrip Pipeline | FAIL | Timezone mismatch (UTC vs local) — tool-bug |
| 8 | Batch Sort Order | PASS | |

**Key validations**:
- Goal-based format works — agents plan approach from objectives, not scripts
- Runner/verifier split provides independent assessment
- Sequential execution model enables progressive learning (Goal 1 discovery reused by all later goals)
- Artifact-based verification decouples runner reasoning from pass/fail judgment
- Report format (YAML frontmatter with passed/failed arrays) is practical and parseable
- Cost-tier "smoke" is appropriate — full run (runner + verifier) completed within budget

## Acceptance Criteria

- [ ] `ace-b36ts/test/e2e/` contains migrated scenario directory `TS-B36TS-001-pilot`
- [ ] `TS-B36TS-001-pilot/scenario.yml` defines tags, mode, execution-model, and `sandbox-layout` for `results/tc/{NN}/`
- [ ] 8 standalone runner files (`TC-*.runner.md`) exist with Goal + Workspace + Constraints (no `## Steps`)
- [ ] 8 standalone verify files (`TC-*.verify.md`) exist with Expectations + Verdict
- [ ] Legacy procedural b36ts scenarios are replaced/removed as part of this migration (no duplicate dual suites)
- [ ] `ace-test-e2e run ace-b36ts TS-B36TS-001` resolves and executes the migrated scenario
- [ ] `ace-test-e2e run ace-b36ts TS-B36TS-001 --verify` is valid and expected once 280.06 lands

## Scope

**In scope:** Live migration of `ace-b36ts/test/e2e/` to the goal-mode TC structure:
- `TS-B36TS-001-pilot/scenario.yml`
- `TC-*.runner.md` and `TC-*.verify.md`
- `runner.yml.md` and `verifier.yml.md`

**Out of scope:** Other packages, global handbook/workflow/template migration (owned by 280.07), and cross-package classification/tag rollout (280.04).

## Deliverables

Live package files under `ace-b36ts/test/e2e/`:

```
ace-b36ts/test/e2e/
└── TS-B36TS-001-pilot/
    ├── scenario.yml
    ├── runner.yml.md
    ├── verifier.yml.md
    ├── TC-001-help-survey.runner.md
    ├── TC-001-help-survey.verify.md
    ├── TC-002-encode-today.runner.md
    ├── TC-002-encode-today.verify.md
    ├── TC-003-decode-token.runner.md
    ├── TC-003-decode-token.verify.md
    ├── TC-004-error-behavior.runner.md
    ├── TC-004-error-behavior.verify.md
    ├── TC-005-output-routing.runner.md
    ├── TC-005-output-routing.verify.md
    ├── TC-006-structured-output.runner.md
    ├── TC-006-structured-output.verify.md
    ├── TC-007-roundtrip-pipeline.runner.md
    ├── TC-007-roundtrip-pipeline.verify.md
    ├── TC-008-batch-sort.runner.md
    └── TC-008-batch-sort.verify.md
```