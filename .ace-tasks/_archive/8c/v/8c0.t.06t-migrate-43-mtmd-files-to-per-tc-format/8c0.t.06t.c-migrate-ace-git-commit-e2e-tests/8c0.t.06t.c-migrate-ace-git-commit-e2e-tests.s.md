---
id: 8c0.t.06t.c
title: Migrate ace-git-commit E2E Tests
status: done
priority: high
created_at: "2025-01-01 00:07:33"
estimate: TBD after Phase 0
dependencies: [8c0.t.06s.a]
tags: [migrated]
parent: 8c0.t.06t
---

# Migrate ace-git-commit E2E Tests

## Scope

Convert `.mt.md` files in `ace-git-commit/test-e2e/scenarios/` to directory-based per-TC format, **filtered through the E2E Value Gate**. The migration inventory below shows the source material; actual TC count will be determined by Phase 0's coverage review. Note the `004a/b/c/d` variant naming pattern for the auto-split test suite.

**Source material:** 7 `.mt.md` files, 20 TCs (pre-review)
**Expected output:** Fewer scenarios/TCs after Value Gate filtering and consolidation

## Phase 0: Coverage Review & Change Plan

Before migrating, run the 3-stage pipeline to determine which TCs have genuine E2E value:

1. **Review:** `/ace:review-e2e-tests ace-git-commit` — produces coverage matrix (functionality × unit tests × E2E)
2. **Plan:** `/ace:plan-e2e-changes ace-git-commit` — classifies each existing MT TC as REMOVE / KEEP / MODIFY / CONSOLIDATE / ADD
3. **Approve plan** before proceeding to migration

The ace-lint migration showed ~60% of TCs duplicate unit test coverage. Expect significant TC reduction.

### Migration scope is determined by the plan:
- **KEEP TCs** → migrate to TS format as-is
- **MODIFY TCs** → migrate to TS format with changes applied
- **CONSOLIDATE TCs** → merge during migration (single TS TC from multiple MT TCs)
- **REMOVE TCs** → skip (don't create TS files, delete MT files)
- **ADD TCs** → create new TS TCs for uncovered features (if any)

## Migration Inventory (Source Material — Subject to Value Gate Review)

| Source File | Target Directory | TCs | Key Fixtures | Special |
|-------------|-----------------|-----|-------------|---------|
| `MT-COMMIT-001-basic-commit-workflow.mt.md` | `TS-COMMIT-001-basic-commit-workflow/` | 4 | README.md, app.rb, helper.rb, to_move.rb, to_delete.rb | git-init + initial commit |
| `MT-COMMIT-002-specific-file-commit.mt.md` | `TS-COMMIT-002-specific-file-commit/` | 4 | README.md, main.rb, config.rb, lib/{core,utils}.rb, spec/core_spec.rb | git-init + dir structure |
| `MT-COMMIT-003-deleted-renamed-handling.mt.md` | `TS-COMMIT-003-deleted-renamed-handling/` | 4 | README.md, old_name.rb, to_delete.rb, keeper.rb | Tests Task 220 fix |
| `MT-COMMIT-004a-auto-split-basics.mt.md` | `TS-COMMIT-004a-auto-split-basics/` | 2 | pkg-a/.ace/git/commit.yml, pkg-b/.ace/git/commit.yml, pkg-{a,b}/service.rb | Multi-package config scopes |
| `MT-COMMIT-004b-path-rules-dryrun.mt.md` | `TS-COMMIT-004b-path-rules-dryrun/` | 2 | .ace/git/commit.yml (path_rules), docs/guide.md, test/service_test.rb | Path rules grouping |
| `MT-COMMIT-004c-scope-cascade.mt.md` | `TS-COMMIT-004c-scope-cascade/` | 2 | .ace-defaults/git/commit.yml, pkg-alpha/, pkg-beta/ with configs | Config cascade test |
| `MT-COMMIT-004d-moves-globs.mt.md` | `TS-COMMIT-004d-moves-globs/` | 2 | .ace/git/commit.yml (glob arrays), pkg-{a,b,c}/ with files | Glob array path rules |

**Total: 7 scenarios, 20 TCs, ~35 fixture files**

## Technical Approach

### Setup Pattern

All scenarios follow:
```yaml
setup:
  - git-init
  - copy-fixtures
  - run: git add -A && git commit -m "initial" --quiet
  - env:
      PROJECT_ROOT_PATH: .
```

The 004a-d variants additionally commit package structure before test modifications.

### Variant Naming

Preserve `004a/b/c/d` suffixes in directory names:
- `MT-COMMIT-004a-*.mt.md` → `TS-COMMIT-004a-*/`

### TC Extraction

- Strip `ace-test-e2e-sh "$TEST_DIR"` wrappers
- Preserve `$PROJECT_ROOT/bin/ace-git-commit` references as just `ace-git-commit`
- Many TCs modify files then run commit — keep as TC steps

## Implementation Plan

### Phase 0: Review & Plan
- [ ] Run `/ace:review-e2e-tests ace-git-commit` — capture coverage matrix
- [ ] Run `/ace:plan-e2e-changes ace-git-commit` — get TC classifications
- [ ] Present plan for approval (expected: N REMOVE, N KEEP, N MODIFY, N CONSOLIDATE, N ADD)

### Phase 1: Informed Migration
- [ ] For each KEEP/MODIFY TC: extract to TS format following migration-pattern.md
- [ ] For each CONSOLIDATE group: merge into single TC during extraction
- [ ] For each ADD: create new TS TC based on plan
- [ ] Validate: `ace-test-e2e setup ace-git-commit TS-COMMIT-{NNN}` for each scenario
- [ ] Remove all original `.mt.md` files
- [ ] Final verification: scenario count, TC count, no stale references

## Acceptance Criteria

- [ ] Phase 0 coverage review completed and change plan approved
- [ ] All KEEP/MODIFY TCs migrated to TS format with setup validation passing
- [ ] CONSOLIDATE groups merged into single TCs during migration
- [ ] REMOVE TCs skipped — no TS equivalent created
- [ ] All original `.mt.md` files removed after successful migration
- [ ] Fixture files are real files on disk (not heredoc creation commands)
- [ ] Final scenario count: 2-5 TCs per scenario (cost-aware guideline)
- [ ] `ace-test-e2e-sh` wrapper calls removed from TC steps

## Out of Scope

- ❌ Running the actual E2E tests (unless `--run-tests` flag is used)
- ❌ Changes to ace-test-e2e-runner code

## References

- Package: `ace-git-commit/test-e2e/scenarios/`
- Migration pattern: `.ace-taskflow/v.0.9.0/tasks/262-migration-refactor/docs/migration-pattern.md`
- Prerequisite: v.0.9.0+task.261.01
- Related: Task 220 (deleted file handling), Task 228 (auto-split feature)