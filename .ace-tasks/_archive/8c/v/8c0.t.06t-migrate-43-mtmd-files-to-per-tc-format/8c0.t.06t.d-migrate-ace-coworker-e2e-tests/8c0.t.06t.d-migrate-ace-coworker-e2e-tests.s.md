---
id: 8c0.t.06t.d
title: Migrate ace-coworker E2E Tests
status: done
priority: high
created_at: "2025-01-01 00:07:33"
estimate: TBD after Phase 0
dependencies: [8c0.t.06s.a]
tags: [migrated]
parent: 8c0.t.06t
---

# Migrate ace-coworker E2E Tests

## Scope

Convert `.mt.md` files in `ace-coworker/test/e2e/` to directory-based per-TC format, **filtered through the E2E Value Gate**. **This is the most complex migration** due to sequential test dependencies requiring composite lifecycle TCs. The migration inventory below shows the source material; actual TC count will be determined by Phase 0's coverage review.

**Source material:** 6 `.mt.md` files, 28 TCs → 8 after composite merging (pre-review)
**Expected output:** Potentially fewer TCs after Value Gate filtering

## Phase 0: Coverage Review & Change Plan

Before migrating, run the 3-stage pipeline to determine which TCs have genuine E2E value:

1. **Review:** `/ace:review-e2e-tests ace-coworker` — produces coverage matrix (functionality × unit tests × E2E)
2. **Plan:** `/ace:plan-e2e-changes ace-coworker` — classifies each existing MT TC as REMOVE / KEEP / MODIFY / CONSOLIDATE / ADD
3. **Approve plan** before proceeding to migration

The ace-lint migration showed ~60% of TCs duplicate unit test coverage. Expect significant TC reduction.

### Migration scope is determined by the plan:
- **KEEP TCs** → migrate to TS format as-is
- **MODIFY TCs** → migrate to TS format with changes applied
- **CONSOLIDATE TCs** → merge during migration (single TS TC from multiple MT TCs)
- **REMOVE TCs** → skip (don't create TS files, delete MT files)
- **ADD TCs** → create new TS TCs for uncovered features (if any)

## Migration Inventory (Source Material — Subject to Value Gate Review)

| Source File | TCs | Dependency | Strategy | Result TCs |
|-------------|-----|-----------|----------|------------|
| `MT-COWORKER-001-workflow-lifecycle.mt.md` | 11 | Fully sequential | Merge all → 1 composite | 1 |
| `MT-COWORKER-002-fork-context.mt.md` | 8 | Fully sequential | Merge all → 1 composite | 1 |
| `MT-COWORKER-003a-error-handling.mt.md` | 2 | Independent | Extract individually | 2 |
| `MT-COWORKER-003b-injection-renumbering.mt.md` | 3 | Sequential (shared setup) | Merge → 1 composite | 1 |
| `MT-COWORKER-003c-auto-completion.mt.md` | 2 | Sequential | Merge → 1 composite | 1 |
| `MT-COWORKER-003d-display-audit.mt.md` | 2 | Independent | Extract individually | 2 |
| **Totals** | **28** | | | **8** |

## Sequential Dependency Resolution

### MT-COWORKER-001 (11 TCs → 1 composite)

Linear workflow lifecycle: error checks → session creation → status → step completion → failure → retry → finish.

**Chain:** TC-001 (error: no config) → TC-002 (error: no session) → TC-003 (error: no report) → TC-004 (deprecated start) → TC-004b (cache auto-creation) → TC-005 (create session) → TC-006 (verify structure) → TC-007 (check status) → TC-008 (complete step) → TC-009 (mark failed) → TC-010 (dynamic step + retry) → TC-011 (complete remaining)

**Result:** `TC-001-full-workflow-lifecycle.tc.md` organized in phases:
- Phase 1: Error Cases (clean state)
- Phase 2: Session Creation
- Phase 3: Status and Completion
- Phase 4: Failure Handling
- Phase 5: Dynamic Steps and Retry
- Phase 6: Workflow Completion

### MT-COWORKER-002 (8 TCs → 1 composite)

Linear fork context workflow: parse frontmatter → regular step → fork steps → transitions → complete.

**Result:** `TC-001-full-fork-context-workflow.tc.md`

### MT-COWORKER-003b (3 TCs → 1 composite)

Shared session with progressive hierarchy manipulation: create child → inject sibling (renumber) → cascade renumber.

**Result:** `TC-001-hierarchy-injection-renumbering.tc.md`

### MT-COWORKER-003c (2 TCs → 1 composite)

Progressive auto-completion: single-level → multi-level hierarchy completion.

**Result:** `TC-001-hierarchy-auto-completion.tc.md`

## Technical Approach

### Composite TC Design

For merged sequential TCs, use phased structure preserving all verification points:

```markdown
## Steps

### Phase 1: [Logical group name]
[Steps from original TC-001, TC-002, ...]

### Phase 2: [Next group]
[Steps from original TC-00X, ...]

## Expected

### Phase 1
[Combined expectations]

### Phase 2
[Combined expectations]
```

### Session Discovery Pattern

Tests dynamically locate session directories — preserve in TC steps:
```bash
SESSION_DIR=$(find "$CACHE_BASE" -maxdepth 1 -mindepth 1 -type d | sort | tail -1)
```

### Fixture Extraction

| Scenario | Fixtures |
|----------|----------|
| 001 | job.yaml (3-step workflow), report.md, dummy-report.md, verify/implement/fix-report.md |
| 002 | job.yaml (4-step with forks), prepare/implement/verify/document-report.md |
| 003a | job.yaml (2-step flat) |
| 003b | job.yaml (2-step, children created in Test Data) |
| 003c | job2.yaml, job3.yaml, various report files |
| 003d | job4.yaml, job5.yaml |

### Setup Pattern

All scenarios:
```yaml
setup:
  - env:
      PROJECT_ROOT_PATH: .
      CACHE_BASE: .cache/ace-coworker
  - run: mkdir -p .cache/ace-coworker
  - copy-fixtures
```

## Implementation Plan

### Phase 0: Review & Plan
- [ ] Run `/ace:review-e2e-tests ace-coworker` — capture coverage matrix
- [ ] Run `/ace:plan-e2e-changes ace-coworker` — get TC classifications
- [ ] Present plan for approval (expected: N REMOVE, N KEEP, N MODIFY, N CONSOLIDATE, N ADD)

### Phase 1: Informed Migration
- [ ] For each KEEP/MODIFY TC: extract to TS format following migration-pattern.md
- [ ] For each CONSOLIDATE group: merge into single TC during extraction (note: sequential dependencies already require composite merging — see Sequential Dependency Resolution above)
- [ ] For each ADD: create new TS TC based on plan
- [ ] Validate: `ace-test-e2e setup ace-coworker TS-COWORKER-{NNN}` for each scenario
- [ ] Remove all original `.mt.md` files
- [ ] Final verification: scenario count, TC count, no stale references

## Acceptance Criteria

- [ ] Phase 0 coverage review completed and change plan approved
- [ ] All KEEP/MODIFY TCs migrated to TS format with setup validation passing
- [ ] CONSOLIDATE groups merged into single TCs during migration
- [ ] REMOVE TCs skipped — no TS equivalent created
- [ ] All original `.mt.md` files removed after successful migration
- [ ] Fixture files are real files on disk (not heredoc creation commands)
- [ ] Final scenario count: 2-5 TCs per scenario (cost-aware guideline)
- [ ] `ace-test-e2e-sh` wrapper calls removed from TC steps

## Out of Scope

- ❌ Running the actual E2E tests (unless `--run-tests` flag is used)
- ❌ Changes to ace-test-e2e-runner code

## References

- Package: `ace-coworker/test/e2e/`
- Migration pattern: `.ace-taskflow/v.0.9.0/tasks/262-migration-refactor/docs/migration-pattern.md`
- Prerequisite: v.0.9.0+task.261.01