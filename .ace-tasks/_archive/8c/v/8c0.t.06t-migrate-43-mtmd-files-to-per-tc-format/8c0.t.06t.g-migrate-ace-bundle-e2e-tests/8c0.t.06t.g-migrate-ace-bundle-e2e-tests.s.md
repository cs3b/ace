---
id: 8c0.t.06t.g
title: Migrate ace-bundle E2E Tests
status: done
priority: medium
created_at: "2025-01-01 00:07:33"
estimate: TBD after Phase 0
dependencies: [8c0.t.06s.a]
tags: [migrated]
parent: 8c0.t.06t
---

# Migrate ace-bundle E2E Tests

## Scope

Convert `.mt.md` files in `ace-bundle/test/e2e/` to directory-based per-TC format, **filtered through the E2E Value Gate**. The migration inventory below shows the source material; actual TC count will be determined by Phase 0's coverage review. Note the `001a/b/c` variant naming pattern.

**Source material:** 5 `.mt.md` files, 14 TCs (pre-review)
**Expected output:** Fewer scenarios/TCs after Value Gate filtering and consolidation

## Phase 0: Coverage Review & Change Plan

Before migrating, run the 3-stage pipeline to determine which TCs have genuine E2E value:

1. **Review:** `/ace:review-e2e-tests ace-bundle` — produces coverage matrix (functionality × unit tests × E2E)
2. **Plan:** `/ace:plan-e2e-changes ace-bundle` — classifies each existing MT TC as REMOVE / KEEP / MODIFY / CONSOLIDATE / ADD
3. **Approve plan** before proceeding to migration

The ace-lint migration showed ~60% of TCs duplicate unit test coverage. Expect significant TC reduction.

### Migration scope is determined by the plan:
- **KEEP TCs** → migrate to TS format as-is
- **MODIFY TCs** → migrate to TS format with changes applied
- **CONSOLIDATE TCs** → merge during migration (single TS TC from multiple MT TCs)
- **REMOVE TCs** → skip (don't create TS files, delete MT files)
- **ADD TCs** → create new TS TCs for uncovered features (if any)

## Migration Inventory (Source Material — Subject to Value Gate Review)

| Source File | Target Directory | TCs | Key Fixtures | Special |
|-------------|-----------------|-----|-------------|---------|
| `MT-BUNDLE-001a-error-and-sections.mt.md` | `TS-BUNDLE-001a-error-and-sections/` | 2 | src/{main,utils}.js, test/main.test.js, package.json, README.md, .ace/bundle/presets/comprehensive-review.md | git-init |
| `MT-BUNDLE-001b-xml-and-commands.mt.md` | `TS-BUNDLE-001b-xml-and-commands/` | 2 | Same 6 fixtures as 001a | git-init |
| `MT-BUNDLE-001c-patterns-and-presets.mt.md` | `TS-BUNDLE-001c-patterns-and-presets/` | 2 | Same as 001a + .ace/bundle/presets/security-scanning.md | git-init |
| `MT-BUNDLE-002-cli-auto-format.mt.md` | `TS-BUNDLE-002-cli-auto-format/` | 6 | .ace/bundle/presets/ (4 test presets, some dynamically generated) | write-file for large presets |
| `MT-BUNDLE-003-cli-api-parity.mt.md` | `TS-BUNDLE-003-cli-api-parity/` | 2 | test-context.md | git-init |

**Total: 5 scenarios, 14 TCs, 11 unique fixture files**

## Technical Approach

### Setup Pattern

Standard (001a/b/c, 003):
```yaml
setup:
  - git-init
  - copy-fixtures
  - env:
      PROJECT_ROOT_PATH: .
```

Dynamic generation (002): Large presets generated via loops → use `write-file:` steps or pre-generate as static fixtures.

### Variant Naming

Preserve `001a/b/c` suffixes: `MT-BUNDLE-001a-*.mt.md` → `TS-BUNDLE-001a-*/`

### Shared Fixtures (001a/b/c)

Each variant gets its own copy of fixtures (ensures test isolation), even though content overlaps.

## Implementation Plan

### Phase 0: Review & Plan
- [ ] Run `/ace:review-e2e-tests ace-bundle` — capture coverage matrix
- [ ] Run `/ace:plan-e2e-changes ace-bundle` — get TC classifications
- [ ] Present plan for approval (expected: N REMOVE, N KEEP, N MODIFY, N CONSOLIDATE, N ADD)

### Phase 1: Informed Migration
- [ ] For each KEEP/MODIFY TC: extract to TS format following migration-pattern.md
- [ ] For each CONSOLIDATE group: merge into single TC during extraction
- [ ] For each ADD: create new TS TC based on plan
- [ ] Validate: `ace-test-e2e setup ace-bundle TS-BUNDLE-{NNN}` for each scenario
- [ ] Remove all original `.mt.md` files
- [ ] Final verification: scenario count, TC count, no stale references

## Acceptance Criteria

- [ ] Phase 0 coverage review completed and change plan approved
- [ ] All KEEP/MODIFY TCs migrated to TS format with setup validation passing
- [ ] CONSOLIDATE groups merged into single TCs during migration
- [ ] REMOVE TCs skipped — no TS equivalent created
- [ ] All original `.mt.md` files removed after successful migration
- [ ] Fixture files are real files on disk (not heredoc creation commands)
- [ ] Final scenario count: 2-5 TCs per scenario (cost-aware guideline)
- [ ] `ace-test-e2e-sh` wrapper calls removed from TC steps

## Out of Scope

- ❌ Running the actual E2E tests (unless `--run-tests` flag is used)
- ❌ Changes to ace-test-e2e-runner code

## References

- Package: `ace-bundle/test/e2e/`
- Migration pattern: `.ace-taskflow/v.0.9.0/tasks/262-migration-refactor/docs/migration-pattern.md`
- Prerequisite: v.0.9.0+task.261.01