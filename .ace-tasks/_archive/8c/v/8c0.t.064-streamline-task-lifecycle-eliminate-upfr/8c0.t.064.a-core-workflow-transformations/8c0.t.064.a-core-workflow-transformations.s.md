---
id: 8c0.t.064.a
title: Core Workflow Transformations
status: done
priority: high
created_at: "2025-01-01 00:06:47"
estimate: 3h
dependencies: []
tags: [migrated]
parent: 8c0.t.064
---

# Core Workflow Transformations

## Behavioral Specification

### User Experience

- **Input**: Agent or user invokes `/ace:review-task 230` on a draft task
- **Process**: Workflow validates behavioral spec completeness, researches answers to open questions, promotes to pending when ready
- **Output**: Task promoted to pending status (ready for JIT planning + implementation) OR blocking questions documented

### Expected Behavior

**Current Problems:**
- plan-task.wf.md (540 lines) produces heavyweight implementation plans that go stale before execution
- No clear gate between "spec is ready" and "start implementing"
- review-task.wf.md reviews any status without making lifecycle decisions
- work-on-task.wf.md requires pre-existing implementation plan in task file

**Desired State:**

1. **review-task.wf.md** becomes the draft→pending readiness gate:
   - Validates behavioral spec (UX, interface contracts, success criteria, scope)
   - Uses existing autonomous research + question generation pattern
   - Promotes validated drafts to `status: pending`
   - Blocks with `needs_review: true` when questions remain

2. **plan-task.wf.md** becomes a JIT planning workflow:
   - Output goes to stdout/ace-assign phase reports (ephemeral)
   - Does NOT modify task spec files or change task status
   - Keeps technical research, file modification planning, test strategy
   - Removes: pending template, status promotion, UX docs, heavy templates

3. **draft-task.wf.md** minor update:
   - "Next step" references plan-task → review-task

4. **work-on-task.wf.md** minor update:
   - Accepts tasks with behavioral specs only (no implementation plan required)
   - Notes JIT planning as prior step when via ace-assign

### Interface Contract

```bash
# New lifecycle flow
/ace:draft-task          # Creates draft with behavioral spec
/ace:review-task 230     # Validates spec → promotes to pending
# Then via ace-assign:
# onboard → plan-task (JIT) → work-on-task → ...
```

**review-task outcome A** (ready):
```
Task v.0.9.0+task.230.01 promoted from draft to pending.
Readiness: All behavioral specs validated, no blocking questions.
Ready for implementation via ace-assign.
```

**review-task outcome B** (needs work):
```
Task v.0.9.0+task.230.01 remains draft.
needs_review: true — 2 HIGH priority questions require human input.
See Review Questions section in task file.
```

### Success Criteria

- [ ] review-task.wf.md refocused as draft→pending gate with readiness checklist
- [ ] plan-task.wf.md produces ephemeral output only (no task file modifications, no status changes)
- [ ] plan-task.wf.md has no pending task template
- [ ] draft-task.wf.md references review-task as next step
- [ ] work-on-task.wf.md accepts tasks without Implementation Plan section
- [ ] No stale cross-references between workflows

### Validation Questions

- [answered] Should review-task keep secondary support for non-draft statuses? **Yes, as brief notes, not primary focus**
- [answered] Should plan-task still be invocable standalone? **Yes, for ad-hoc JIT planning outside ace-assign**

## Objective

Transform 4 workflow files to implement the new streamlined task lifecycle where behavioral specs are validated via review-task and implementation planning happens JIT via ace-assign.

## Scope of Work

### Deliverables

#### Modify
- `ace-taskflow/handbook/workflow-instructions/review-task.wf.md` — Major refactor as readiness gate
- `ace-taskflow/handbook/workflow-instructions/plan-task.wf.md` — Major transform to ephemeral JIT
- `ace-taskflow/handbook/workflow-instructions/draft-task.wf.md` — Minor reference updates
- `ace-taskflow/handbook/workflow-instructions/work-on-task.wf.md` — Minor relaxation of plan requirement

## Out of Scope

- ace-assign integration (task 230.02)
- Skill metadata updates (task 230.03)
- Task status migration (task 230.04)
- ace-taskflow CLI code changes (status transitions already work via `task update --field`)
- ace-nav skill discovery (deferred — original 230.01 scope moved to backlog)

## Implementation Plan

### Planning Steps

* [x] Research current workflow files and understand structure
  > Already completed during task drafting. All 4 workflows read and analyzed.

* [x] Identify reusable patterns in review-task.wf.md
  > Autonomous research phase (steps 2-3), question generation with `needs_review` metadata,
  > escalation pattern — all excellent, keep as-is.

### Execution Steps

- [ ] **Refactor review-task.wf.md** — Major: Readiness gate for draft→pending
  - Replace Goal section: "Review draft behavioral specs → promote to pending"
  - Replace Prerequisites: Focus on draft status tasks
  - Restructure Process Steps:
    - Step 1: Load and Analyze Draft Task (verify `status: draft`, parse behavioral sections)
    - Step 2: Autonomous Research Phase (KEEP existing steps 2-3 as-is)
    - Step 3: Critical Question Generation (KEEP as-is)
    - Step 4: NEW — Readiness Checklist (behavioral spec complete? interface contracts? success criteria? scope? validation questions answered?)
    - Step 5: NEW — Promote or Block (if ready: `ace-taskflow task update <ref> --field status=pending`; if not: add `needs_review: true`, keep draft)
    - Step 6: Question Documentation (KEEP existing pattern)
  - Minimize status-specific sections for pending/in-progress/completed to brief notes (not primary focus)
  - Update Success Criteria: "Draft promoted to pending OR blocking questions documented"
  - Update Integration section: Remove plan-task references
  > TEST: Verify workflow has readiness checklist and status promotion step
  > Type: Content Validation
  > Assert: File contains "Readiness Checklist" and "ace-taskflow task update" and "status=pending"
  > Command: grep -c "Readiness\|status=pending\|promote" ace-taskflow/handbook/workflow-instructions/review-task.wf.md

- [ ] **Transform plan-task.wf.md** — Major: Ephemeral JIT planning
  - Replace Goal: "JIT implementation plan for ace-assign phase (ephemeral output)"
  - Replace Implementation Prohibition with "Planning Phase Context" (output to stdout/phase reports, NOT task files)
  - Update Prerequisites: Task with `status: pending` (validated by review-task)
  - Keep Process Steps 1-6: Technical research, file modification planning, implementation steps
  - Simplify Tool Selection: Remove matrix template, replace with 3-line decision note
  - Simplify Risk Assessment: Replace template with "Primary Risk + Mitigation + Rollback"
  - Remove Step 9 (UX/Usage Doc Creation) or make brief optional note
  - DELETE Step 10 (Task Status Promotion) entirely
  - DELETE embedded pending task template (`<template path="tmpl://task-management/task.pending">`)
  - DELETE tool-selection-matrix template
  - DELETE risk-assessment template (keep simplified inline version)
  - Keep file-modification-checklist template (still useful)
  - Keep technical-approach template (still useful)
  - Update Output/Success Criteria: "Ephemeral output, NO task file modifications, NO status changes"
  > TEST: Verify no status promotion and no pending template
  > Type: Content Validation
  > Assert: File does NOT contain "status: draft.*to.*status: pending" or pending task template
  > Command: grep -c "task.pending\|Change.*status.*draft.*to.*pending" ace-taskflow/handbook/workflow-instructions/plan-task.wf.md

- [ ] **Update draft-task.wf.md** — Minor: Reference updates
  - Step 10 "Provide Behavioral Summary": Change next step from "implementation planning phase" to "review-task workflow to validate and promote to pending"
  - Update "Draft Status Integration" section: Change "Clear handoff to replan/implementation phase" to "Clear handoff to review-task for readiness validation"
  - Output/Success Criteria: Update "Clear handoff to implementation planning phase" to "Clear handoff to review-task"
  > TEST: Verify references updated
  > Type: Content Validation
  > Assert: File references review-task as next step, not plan-task
  > Command: grep -c "review-task" ace-taskflow/handbook/workflow-instructions/draft-task.wf.md

- [ ] **Update work-on-task.wf.md** — Minor: Relax plan requirement
  - Update Goal line: Add "Tasks may have embedded implementation plans OR behavioral specifications only"
  - Step 2 (Load Task & Validate Plan): Update task structure to show `## Implementation Plan` as optional
  - Add branching logic after validation:
    - "Task Has Implementation Plan" → proceed as normal
    - "Task Has Only Behavioral Spec" → if via ace-assign: planning happened as prior phase; if standalone: suggest `/ace:plan-task` first or proceed with behavioral spec
  > TEST: Verify plan is optional
  > Type: Content Validation
  > Assert: File mentions behavioral spec as sufficient and plan as optional
  > Command: grep -c "optional\|behavioral spec" ace-taskflow/handbook/workflow-instructions/work-on-task.wf.md

- [ ] **Cross-reference consistency check**
  - Grep all .wf.md files for stale "plan-task" lifecycle references
  - Verify no workflow references plan-task as a required lifecycle step (only as JIT/ace-assign phase)
  > TEST: No stale lifecycle references
  > Type: Cross-Reference Validation
  > Assert: No workflow references plan-task as mandatory lifecycle step
  > Command: grep -rn "plan-task" ace-taskflow/handbook/workflow-instructions/*.wf.md

## Acceptance Criteria

- [ ] review-task.wf.md is the draft→pending gate with readiness checklist and status promotion
- [ ] plan-task.wf.md produces ephemeral output only; no task file modifications, no status changes, no pending template
- [ ] draft-task.wf.md references review-task as next step
- [ ] work-on-task.wf.md accepts tasks without Implementation Plan section
- [ ] No stale cross-references between workflows

## References

- Plan: `/home/mc/.claude/plans/gentle-wondering-honey.md`
- Current review-task: `ace-taskflow/handbook/workflow-instructions/review-task.wf.md`
- Current plan-task: `ace-taskflow/handbook/workflow-instructions/plan-task.wf.md`
- Current draft-task: `ace-taskflow/handbook/workflow-instructions/draft-task.wf.md`
- Current work-on-task: `ace-taskflow/handbook/workflow-instructions/work-on-task.wf.md`