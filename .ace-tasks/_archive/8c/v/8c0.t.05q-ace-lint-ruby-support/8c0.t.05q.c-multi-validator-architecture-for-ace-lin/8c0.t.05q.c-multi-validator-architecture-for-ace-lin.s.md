---
id: 8c0.t.05q.c
title: Multi-Validator Architecture for ace-lint
status: done
priority: medium
created_at: "2025-01-01 00:06:21"
estimate: 8h
dependencies: []
tags: [migrated]
parent: 8c0.t.05q
---

# Multi-Validator Architecture for ace-lint

## Objective

Enable ace-lint to run multiple validators per file type with pattern-based configuration, allowing comprehensive linting with tools like StandardRB (style) + RuboCop (extra rules).

## Scope of Work

- Named groups with glob patterns for file matching
- Sequential validator execution with result aggregation
- Config precedence: explicit > .ace/lint/ > native > gem defaults
- CLI override via `--validators` flag
- Diagnostic `ace-lint doctor` command

### Configuration Schema

Pattern-based groups define which validators run on which files:

```yaml
groups:
  ruby-strict:
    patterns: ["lib/**/*.rb", "app/**/*.rb"]
    validators: [standardrb, rubocop]
  tests:
    patterns: ["test/**/*.rb", "spec/**/*.rb"]
    validators: [rubocop]
  default:
    patterns: ["**/*.rb"]
    validators: [standardrb]
```

**CLI Override**: The `--validators` flag replaces group validators for the current run:
```bash
ace-lint lib/foo.rb --validators rubocop  # Runs only RuboCop, ignoring group config
```

**Pattern Specificity Rules** (highest priority first):
1. Exact filename match (e.g., `Rakefile`)
2. Deeper glob path (e.g., `lib/ace/**/*.rb` > `lib/**/*.rb`)
3. More specific directory (e.g., `lib/**/*.rb` > `**/*.rb`)
4. Catch-all pattern (e.g., `**/*.rb`)

### Deliverables

#### Create

- `ace-lint/lib/ace/lint/atoms/pattern_matcher.rb` - Glob matching with specificity scoring
- `ace-lint/lib/ace/lint/atoms/config_locator.rb` - Config file resolution with precedence
- `ace-lint/lib/ace/lint/atoms/validator_registry.rb` - Registry of available validators
- `ace-lint/lib/ace/lint/molecules/group_resolver.rb` - Match files to groups
- `ace-lint/lib/ace/lint/molecules/validator_chain.rb` - Run multiple validators, aggregate results
- `ace-lint/lib/ace/lint/organisms/lint_doctor.rb` - Config health checker
- `ace-lint/lib/ace/lint/cli/commands/doctor.rb` - Doctor CLI command
- `ace-lint/test/atoms/pattern_matcher_test.rb`
- `ace-lint/test/atoms/config_locator_test.rb`
- `ace-lint/test/atoms/validator_registry_test.rb`
- `ace-lint/test/molecules/group_resolver_test.rb`
- `ace-lint/test/molecules/validator_chain_test.rb`
- `ace-lint/test/organisms/lint_doctor_test.rb`

#### Modify

- `ace-lint/lib/ace/lint.rb` - Config loading for new schema
- `ace-lint/lib/ace/lint/cli.rb` - Register doctor command
- `ace-lint/lib/ace/lint/cli/commands/lint.rb` - Add `--validators` flag
- `ace-lint/lib/ace/lint/molecules/ruby_linter.rb` - Use ValidatorChain
- `ace-lint/lib/ace/lint/organisms/lint_orchestrator.rb` - Group-aware routing
- `ace-lint/.ace-defaults/lint/ruby.yml` - New schema format

## Implementation Plan

### Planning Steps

* [ ] Review existing RubyLinter implementation for integration points
* [ ] Document pattern specificity scoring algorithm
* [ ] Design config schema with validation rules

### Execution Steps

#### Phase 1: Foundation (Atoms)

- [ ] Create `PatternMatcher` atom
  - `specificity(pattern)` - score pattern by segment types
  - `matches?(path, pattern)` - glob match check
  - `best_match(path, patterns)` - find highest specificity match
  > TEST: Pattern Specificity
  > Assert: `lib/**/*.rb` scores higher than `**/*.rb`
  > Command: ace-test ace-lint test/atoms/pattern_matcher_test.rb

- [ ] Create `ConfigLocator` atom
  - `locate(tool, options:, project_root:, verbose:)` - find config with precedence
  - `native_config_name(tool)` - return `.standard.yml` or `.rubocop.yml`
  > TEST: Config Precedence
  > Assert: .ace/lint/.rubocop.yml overrides .rubocop.yml
  > Command: ace-test ace-lint test/atoms/config_locator_test.rb

- [ ] Create `ValidatorRegistry` atom
  - Registry of tool name → runner class mapping
  - `available?(tool)` - check if tool is installed
  > TEST: Registry Lookup
  > Assert: `:standardrb` returns StandardrbRunner
  > Command: ace-test ace-lint test/atoms/validator_registry_test.rb

#### Phase 2: Business Logic (Molecules)

- [ ] Create `GroupResolver` molecule
  - `resolve(file_path)` - return matching group config
  - `resolve_batch(file_paths)` - group files by matched group
  > TEST: Group Resolution
  > Assert: `lib/foo.rb` matches `lib/**/*.rb` group over `**/*.rb`
  > Command: ace-test ace-lint test/molecules/group_resolver_test.rb

- [ ] Create `ValidatorChain` molecule
  - `run(file_paths, options:)` - execute validators sequentially
  - `deduplicate_results(results)` - merge by line:column:message
  > TEST: Chain Execution
  > Assert: Both StandardRB and RuboCop results included
  > Command: ace-test ace-lint test/molecules/validator_chain_test.rb

- [ ] Modify `RubyLinter` to use `ValidatorChain`
  - Replace `run_with_fallback` with chain execution
  - Pass group's validators to chain
  > TEST: Linter Integration
  > Assert: Existing tests still pass
  > Command: ace-test ace-lint test/molecules/ruby_linter_test.rb

#### Phase 3: Integration (Organisms + CLI)

- [ ] Modify `LintOrchestrator` for group-aware routing
  - Use `GroupResolver` to match files to groups
  - Pass group validators to linter
  > TEST: Orchestrator Groups
  > Assert: Files routed to correct groups
  > Command: ace-test ace-lint test/organisms/lint_orchestrator_test.rb

- [ ] Add `--validators` CLI option to lint command
  - Parse comma-separated tool names
  - Override group config when provided
  > TEST: CLI Override
  > Assert: `--validators rubocop` runs only RuboCop
  > Command: ace-test ace-lint test/commands/lint_command_test.rb

- [ ] Update config loading in `Ace::Lint` module
  - Load new schema format
  - Parse groups and validators
  > TEST: Config Loading
  > Assert: Groups parsed correctly from YAML
  > Command: ace-test ace-lint

#### Phase 4: Doctor Command

- [ ] Create `LintDoctor` organism
  - `check_validator_availability` - warn if tools missing
  - `check_config_files` - warn if explicit paths don't exist
  - `check_cyclic_corrections` - detect potential conflicts
  - `check_pattern_coverage` - info about unused patterns
  > TEST: Doctor Checks
  > Assert: Missing validator detected as warning
  > Command: ace-test ace-lint test/organisms/lint_doctor_test.rb

- [ ] Create `doctor` CLI command
  - Register in CLI
  - Format output with status indicators
  > TEST: Doctor CLI
  > Assert: `ace-lint doctor` exits 0 for valid config
  > Command: ace-lint doctor

#### Phase 5: Polish

- [ ] Update `.ace-defaults/lint/ruby.yml` with new schema
  - Single `default` group with StandardRB enabled
  - RuboCop disabled by default
  > TEST: Default Config
  > Assert: Unchanged behavior with default config
  > Command: ace-lint ace-lint/lib/ace/lint.rb

- [ ] Add verbose config logging
  - Log group name and validators when `-v` flag
  - Log config file paths used
  > TEST: Verbose Output
  > Assert: Config path shown in verbose mode
  > Command: ace-lint ace-lint/lib/ace/lint.rb -v

- [ ] Update CHANGELOG.md
  - Document new multi-validator feature
  - Document doctor command

## Acceptance Criteria

- [ ] Can run multiple validators on same files: `ace-lint file.rb --validators standardrb,rubocop`
- [ ] Files matched to groups by pattern specificity
- [ ] Config precedence works: explicit > .ace/lint/ > native > gem defaults
- [ ] `ace-lint doctor` reports config health
- [ ] Verbose mode shows config resolution
- [ ] All existing tests pass
- [ ] New tests for all new components

## Out of Scope

- Non-Ruby file types (future work)
- Parallel validator execution
- Web UI or interactive mode

## References

- Plan: `/Users/mc/.claude/plans/magical-yawning-sunbeam.md`
- Current RubyLinter: `ace-lint/lib/ace/lint/molecules/ruby_linter.rb`
- Current config: `ace-lint/.ace-defaults/lint/ruby.yml`
