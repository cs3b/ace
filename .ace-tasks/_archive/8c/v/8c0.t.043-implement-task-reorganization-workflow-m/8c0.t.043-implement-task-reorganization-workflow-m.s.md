---
id: 8c0.t.043
title: Implement task reorganization workflow (move --child-of)
status: done
priority: medium
created_at: "2025-01-01 00:04:32"
estimate: 8-12h (total across subtasks)
dependencies: []
tags: [migrated]
---

# Implement task reorganization workflow (move --child-of)

## Overview

Enhance `ace-taskflow task move` to support converting standalone tasks to subtasks and vice versa, with `--child-of` and `--dry-run` options.

## Subtasks

- **01**: Add `parse_move_args_with_optparse` to TaskArgParser (molecules) ✅
- **02**: Add `--dry-run` to task create command ✅
- **03**: Implement `promote_to_standalone` in TaskManager (organisms) ✅
- **04**: Implement `demote_to_subtask` in TaskManager (organisms) ✅
- **05**: Implement `convert_to_orchestrator` in TaskManager (organisms) ✅
- **06**: Update TaskCommand `move_task` with new options ✅
- **07**: Create `reorganize-tasks.wf.md` workflow and Claude command ✅

## Completion Criteria

- [x] `ace-taskflow task move 019 --child-of 121` converts to subtask
- [x] `ace-taskflow task move 121.01 --child-of` promotes to standalone
- [x] `ace-taskflow task move 019 --child-of self` creates orchestrator structure
- [x] `--dry-run` shows operations without executing
- [x] All tests pass
- [x] Workflow is self-contained (no external dependencies)