---
id: 8c0.t.02m
title: Make ace-taskflow task transitions more flexible and idempotent
status: done
priority: high
created_at: "2025-01-01 00:02:54"
estimate: 2 days
dependencies: []
tags: [migrated]
---

# Make ace-taskflow task transitions more flexible and idempotent

## Behavioral Specification

### User Experience
- **Input**: Users run task transition commands like `ace-taskflow task done 058` or `ace-taskflow task update 058 --status done`
- **Process**: Users experience smooth transitions without rigid validation errors, receiving informative messages when operations are already satisfied
- **Output**: Tasks successfully transition to desired states, with clear feedback about what happened (transitioned, already in state, or moved)

### Expected Behavior
The ace-taskflow task transition system should be flexible and forgiving, allowing users to:
- Transition tasks from any status directly to "done" without requiring intermediate steps
- Mark tasks as done multiple times without errors (idempotent operations)
- Use custom statuses like "ready-for-review" that aren't in the predefined list
- Receive informative messages instead of errors when desired state is already achieved

When a user attempts to transition a task that's already in the desired state or location, the system should succeed with an informative message rather than failing with an error. This makes automation and scripting more reliable.

### Interface Contract
```bash
# CLI Interface
ace-taskflow task done <task-reference>
# Success: Task marked as done (even if already done)
# Output: "Task 058 marked as done" or "Task 058 is already done"
# Exit code: 0 (success)

ace-taskflow task update <task-reference> --status <status>
# Success: Task status updated (supports custom statuses)
# Output: "Task status updated to ready-for-review"
# Exit code: 0 (success)

# Configuration Interface (optional)
# .ace/taskflow/config.yml
strict_transitions: false  # Default: flexible transitions
# or via CLI flag
ace-taskflow task done 058 --strict  # Use rigid validation
```

**Error Handling:**
- [Invalid task reference]: "Task not found: 058"
- [File system error]: "Unable to move task: permission denied"

**Edge Cases:**
- [Already done]: Success with message "Task 058 is already done"
- [Already in done/ folder]: Success with message "Task 058 already in done/"
- [Custom status to done]: Success, transitions from any status
- [Same status transition]: Success with message "Task already has status: done"

### Success Criteria
- [ ] **Flexible Transitions**: Tasks can transition from any status (including custom) directly to "done"
- [ ] **Idempotent Operations**: Running `task done` multiple times succeeds without errors
- [ ] **Custom Status Support**: Tasks can have and transition from custom statuses like "ready-for-review"
- [ ] **Informative Messages**: Operations that are already satisfied return success with clear messages
- [ ] **Backward Compatibility**: Optional strict mode available for users who prefer rigid validation

### Validation Questions
- [ ] **Configuration Preference**: Should flexible transitions be the default, or should it be opt-in via configuration?
- [ ] **Strict Mode Interface**: Should strict validation be available via CLI flag (--strict) or only via config?
- [ ] **Message Verbosity**: Should info messages be shown by default or only with --verbose?
- [ ] **Custom Status Persistence**: Should the list of valid statuses be configurable per project?

## Objective

Enable more flexible and forgiving task state management in ace-taskflow, reducing friction for users who encounter rigid validation errors during normal workflows. This improves the user experience when managing tasks, especially in automated scripts or when dealing with tasks that may have custom statuses or are already in desired states.

## Scope of Work

- **User Experience Scope**: Task status transitions, task completion workflow, idempotent operations
- **System Behavior Scope**: Status validation logic, directory movement operations, error messaging
- **Interface Scope**: `ace-taskflow task done`, `ace-taskflow task update --status`, configuration options

### Deliverables

#### Behavioral Specifications
- Flexible status transition rules
- Idempotent operation behavior
- Custom status support specification

#### Validation Artifacts
- Test scenarios for flexible transitions
- Idempotency test cases
- Backward compatibility validation

## Out of Scope

- ❌ **Implementation Details**: Specific code changes to StatusValidator, TaskManager classes
- ❌ **Technology Decisions**: Whether to refactor molecules or organisms
- ❌ **Performance Optimization**: Caching strategies for status checks
- ❌ **Future Enhancements**: Bulk task transitions, status history tracking

## Technical Research

### Current Implementation Analysis

**Status Validation (`molecules/status_validator.rb`):**
- Rigid transition matrix defined in `TRANSITIONS` constant
- Only allows predefined transitions (e.g., `pending` → `done` is invalid)
- Custom statuses are rejected immediately
- Same-status transitions are invalid

**Task Status Updates (`organisms/task_manager.rb`):**
- `update_task_status` method validates transitions before updating
- `complete_task` method chains `update_task_status` then moves to done/
- Uses `task_loader.update_task_status` for file writing

**File Update Mechanism (`molecules/task_loader.rb`):**
- Uses regex substitution: `content.sub(/^status:\s*.+$/m, "status: #{new_status}")`
- **CRITICAL BUG**: This regex can corrupt frontmatter if not careful
- Uses `SafeFileWriter` but with `validate: false`
- No structured YAML editing

**Frontmatter Corruption Issue:**
- The regex-based approach in `task_loader.rb:206` is fragile
- Idea file from `.ace-taskflow/v.0.9.0/ideas/20251102-004209...` shows task files reduced to 3 lines
- Backups are created but original file is corrupted
- Root cause: Direct string manipulation instead of structured YAML editing

**Better Alternative Available:**
- `ace-support-markdown` provides `DocumentEditor` class
- Safe frontmatter editing via `update_frontmatter` and `set_field` methods
- Proper YAML parsing and reconstruction
- Built-in validation and backup

### Architecture Pattern

**Flexible Validation Pattern:**
- Replace rigid transition matrix with flexible validation
- Add configuration option for strict vs flexible mode
- Implement idempotency checks before validation
- Support custom status values

**Safe File Editing Pattern:**
- Replace regex-based editing with `DocumentEditor`
- Structured YAML manipulation instead of string substitution
- Proper validation and rollback
- Prevent frontmatter corruption

### Technology Stack

**No new dependencies required:**
- Use existing `ace-support-markdown` gem for safe editing
- Leverage existing `Molecules::ConfigLoader` for configuration
- Use existing `SafeFileWriter` correctly with validation enabled

### Implementation Strategy

**Phase 1: Fix Frontmatter Corruption**
1. Replace regex-based status updates with `DocumentEditor`
2. Enable validation in file writes
3. Add comprehensive error handling

**Phase 2: Flexible Transitions**
1. Add configuration option for strict vs flexible mode
2. Modify `StatusValidator` to support flexible mode
3. Implement idempotency checks
4. Support custom status values

**Phase 3: Enhanced Messaging**
1. Add informative messages for already-satisfied states
2. Distinguish between transition, no-op, and error cases
3. Improve user feedback

## File Modifications

### Modify

**ace-taskflow/lib/ace/taskflow/molecules/task_loader.rb**
- **Changes**: Replace regex-based `update_task_status` with `DocumentEditor`-based approach
- **Impact**: Prevents frontmatter corruption, enables safe YAML editing
- **Integration points**: Works with existing `SafeFileWriter`, integrates with `ace-support-markdown`
- **Lines**: ~200-220 (update_task_status method)

**ace-taskflow/lib/ace/taskflow/molecules/status_validator.rb**
- **Changes**:
  - Add `valid_transition?` with `flexible: true` option parameter
  - Add `idempotent_operation?` method to check if already in desired state
  - Support custom statuses in flexible mode
- **Impact**: Enables flexible transitions and custom status support
- **Integration points**: Called by `TaskManager.update_task_status`
- **Lines**: ~1-42 (entire module)

**ace-taskflow/lib/ace/taskflow/organisms/task_manager.rb**
- **Changes**:
  - Update `update_task_status` to support flexible mode via config
  - Add idempotency check before validation
  - Improve messaging for no-op transitions
  - Update `complete_task` to handle already-done tasks gracefully
- **Impact**: User-facing behavior changes, improved flexibility
- **Integration points**: Primary public API for task status management
- **Lines**: ~266-306 (update_task_status and complete_task methods)

**ace-taskflow/.ace.example/taskflow/config.yml** *(if not exists: create)*
- **Changes**: Add configuration example for strict_transitions
- **Impact**: Documents configuration options for users
- **Integration points**: Loaded by `Molecules::ConfigLoader`

### Create

**ace-taskflow/test/molecules/status_validator_flexible_test.rb**
- **Purpose**: Test flexible transition mode
- **Key components**: Tests for custom statuses, idempotent operations, config-driven behavior
- **Dependencies**: Existing test framework

**ace-taskflow/test/organisms/task_manager_idempotent_test.rb**
- **Purpose**: Test idempotent task transitions at organism level
- **Key components**: Integration tests for complete_task idempotency
- **Dependencies**: Test fixtures, existing test helpers

**ace-taskflow/test/molecules/task_loader_document_editor_test.rb**
- **Purpose**: Test DocumentEditor-based status updates
- **Key components**: Frontmatter safety tests, corruption prevention
- **Dependencies**: Test fixtures with various frontmatter formats

## Implementation Plan

### Planning Steps

* [ ] Review ace-support-markdown DocumentEditor API and examples
  > TEST: Understanding Check
  > Type: Pre-condition Check
  > Assert: DocumentEditor usage patterns are understood
  > Command: ace-search "DocumentEditor" --path ace-support-markdown/test --content
* [ ] Analyze current task transition error scenarios
  > TEST: Error Case Identification
  > Type: Analysis Validation
  > Assert: All error scenarios documented and reproducible
  > Command: ace-search "Invalid status transition" --path ace-taskflow --content
* [ ] Design configuration schema for strict vs flexible modes
* [ ] Plan test coverage for idempotent operations

### Execution Steps

**Step 1: Fix Frontmatter Corruption**

- [ ] Replace regex-based status update with DocumentEditor in task_loader.rb
  > TEST: Safe Frontmatter Update
  > Type: Unit Test
  > Assert: Status updates preserve frontmatter structure
  > Command: bundle exec rake test TEST=test/molecules/task_loader_document_editor_test.rb
- [ ] Enable validation in SafeFileWriter calls
- [ ] Add error handling for malformed frontmatter
- [ ] Create test to verify no corruption on status updates

**Step 2: Implement Flexible Status Validator**

- [ ] Add configuration loading for `strict_transitions` option
  > TEST: Config Loading
  > Type: Unit Test
  > Assert: Configuration option loads correctly with defaults
  > Command: bundle exec rake test TEST=test/molecules/config_loader_test.rb:test_strict_transitions_default
- [ ] Modify `StatusValidator.valid_transition?` to accept `flexible` parameter
  > TEST: Flexible Validation
  > Type: Unit Test
  > Assert: Flexible mode allows any status transition
  > Command: bundle exec rake test TEST=test/molecules/status_validator_flexible_test.rb
- [ ] Add `StatusValidator.idempotent_operation?` method
- [ ] Add custom status support (any string in flexible mode)
- [ ] Update tests for new flexible validation

**Step 3: Update TaskManager for Idempotency**

- [ ] Add idempotency check in `update_task_status` before validation
  > TEST: Idempotent Status Update
  > Type: Integration Test
  > Assert: Updating to same status returns success with informative message
  > Command: bundle exec rake test TEST=test/organisms/task_manager_idempotent_test.rb
- [ ] Update `complete_task` to handle already-done tasks
  > TEST: Idempotent Complete
  > Type: Integration Test
  > Assert: Completing already-done task returns success
  > Command: bundle exec rake test TEST=test/organisms/task_manager_idempotent_test.rb:test_complete_already_done_task
- [ ] Improve error messages to distinguish transition types
- [ ] Add configuration support for strict mode flag

**Step 4: Integration and Testing**

- [ ] Run full test suite to verify no regressions
  > TEST: Full Test Suite
  > Type: Integration Test
  > Assert: All existing tests pass with changes
  > Command: bundle exec rake test
- [ ] Test with real task files (task 097 itself)
  > TEST: Real-world Usage
  > Type: Manual Test
  > Assert: Can transition task 097 flexibly without errors
  > Command: ace-taskflow task update 097 --status pending && ace-taskflow task done 097
- [ ] Verify backward compatibility with strict mode enabled
- [ ] Update documentation in usage.md for new flexible behavior

**Step 5: Update Configuration Examples**

- [ ] Add example configuration file with strict_transitions option
- [ ] Document the flexible vs strict mode differences
- [ ] Update CLI help text if needed

## Acceptance Criteria

- [ ] **Frontmatter Safety**: Task status updates never corrupt frontmatter structure
- [ ] **Flexible Transitions**: Tasks can transition from any status directly to "done" (default behavior)
- [ ] **Idempotent Operations**: Running `task done` multiple times succeeds without errors
- [ ] **Custom Status Support**: Tasks can have and transition from custom statuses like "ready-for-review"
- [ ] **Informative Messages**: Operations that are already satisfied return success with clear messages
- [ ] **Backward Compatibility**: Strict mode available via configuration for users who prefer rigid validation
- [ ] **No Test Regressions**: All existing tests pass
- [ ] **Comprehensive Test Coverage**: New tests for flexible mode, idempotency, and safe editing

## Out of Scope

- ❌ **Status History Tracking**: Tracking history of status changes over time
- ❌ **Bulk Operations**: Transitioning multiple tasks at once
- ❌ **Custom Transition Rules**: User-defined transition matrices
- ❌ **UI/Web Interface**: Visual status management interface
- ❌ **Migration Tool**: Automated migration of existing tasks to new structure

## Risk Assessment

### Technical Risks

- **Risk:** Changing StatusValidator breaks existing workflows
  - **Probability:** Medium
  - **Impact:** High
  - **Mitigation:** Default to flexible mode, provide strict mode as opt-in, comprehensive test coverage
  - **Rollback:** Configuration flag allows reverting to strict mode

- **Risk:** DocumentEditor integration introduces performance overhead
  - **Probability:** Low
  - **Impact:** Low
  - **Mitigation:** DocumentEditor is already used in ace-support-markdown, proven performance
  - **Rollback:** N/A (performance impact minimal)

### Integration Risks

- **Risk:** ace-support-markdown dependency issues
  - **Probability:** Low
  - **Impact:** Medium
  - **Mitigation:** Already a dependency, version pinning in Gemfile
  - **Monitoring:** Test suite runs on CI

- **Risk:** Regex removal affects other status fields
  - **Probability:** Low
  - **Impact:** High
  - **Mitigation:** Comprehensive test coverage, use DocumentEditor for all frontmatter edits
  - **Monitoring:** Lint and test suite validation

## References

- Source idea: .ace-taskflow/v.0.9.0/ideas/done/20251007-215437-rethink-the-transitions-should-they-be-fixed-and.md
- Frontmatter corruption issue: .ace-taskflow/v.0.9.0/ideas/20251102-004209-ace-taskflow-task-done-fuckup-the-file-we-were.s.md
- Related error example: "Error: Invalid status transition: ready-for-review → done"
- Current rigid validation in ace-taskflow task management
- DocumentEditor API: ace-support-markdown/lib/ace/support/markdown/organisms/document_editor.rb
- SafeFileWriter usage: ace-support-markdown/lib/ace/support/markdown/organisms/safe_file_writer.rb
