---
id: 8r4.t.i68.0
status: pending
priority: medium
created_at: "2026-04-05 12:06:57"
estimate: TBD
dependencies: []
tags: []
parent: 8r4.t.i68
github_issue: 279
bundle:
  presets: [work-on-task]
  files: [ace-support-config/lib/ace/support/config/organisms/config_initializer.rb, docs/quick-start.md, AGENTS.md, CLAUDE.md]
  commands: [ace-config init]
needs_review: false
---

# Add .ace-local gitignore and sample agent guidance during init

## Objective

Ensure fresh project initialization includes durable local artifact hygiene and agent guidance defaults so downstream repos start with repository-safe behavior and predictable `ace-*` usage.

## Behavioral Specification

### User Experience

- A user runs project initialization and ends with canonical `.ace-local` handling already in place.
- New contributors get clear local agent instructions that describe how to use `ace-*` commands and where temporary outputs should live.

### Expected Behavior

1. New initialization sessions produce clear local artifact handling guidance.
2. `.ace-local/` is documented and treated as the canonical place for ACE temporary outputs.
3. Generated guidance explicitly discourages brittle post-processing of `ace-*` command output and recommends reading emitted files directly.
4. `ace-config init` updates bootstrap outputs non-destructively:
   - append or create a `.gitignore` entry for `.ace-local/` when missing
   - add sample agent guidance only when absent or when the user explicitly overwrites generated files
   - preserve unrelated user-owned content on rerun
4. The issue [#279](https://github.com/cs3b/ace/issues/279) is resolved by this behavior.

### Interface Contract

- **User-facing contract**
  - A completed initialization flow includes onboarding guidance and local-hygiene expectation in new projects.
- **Failure behavior**
  - If initialization artifacts already exist, behavior should remain idempotent and additive (no destructive overwrite of user-owned content).
  - Reruns must never replace unrelated `.gitignore`, `AGENTS.md`, or `CLAUDE.md` content unless the caller explicitly invokes overwrite behavior.

### Success Criteria

- [ ] A fresh project has guidance that makes `.ace-local/` usage clear.
- [ ] The guidance avoids `ace-*` output manipulation anti-patterns and points to direct file inspection.
- [ ] Initialization behavior is safe on repeated runs.
- [ ] Parent PR closes [#279](https://github.com/cs3b/ace/issues/279) when this child is complete.

## PR / Issue Tracking

- Include in PR description:
  - `Implements task 8r4.t.i68.0`
  - `Part of task 8r4.t.i68`
  - Do not use GitHub issue-closing keywords in child PRs; `#279` closes from the final parent PR.

## Vertical Slice Decomposition

- **Slice type**: tracer-first bootstrap/init slice
- **Observable outcome**: a newly initialized project immediately ignores `.ace-local/` and contains sample agent guidance that teaches canonical ACE local-artifact and command-output behavior
- **Advisory size**: small
- **Distinct scope**: init/bootstrap outputs only; no provider runtime or Claude CLI compatibility work

## Validation Plan

- **Primary checks**
  - Verify first-run initialization in a fresh project creates or updates the expected bootstrap outputs.
  - Verify rerun initialization is additive and non-disruptive when `.gitignore` and agent-guidance files already exist.
  - Verify generated guidance covers `ace-*` output-file-following behavior and `.ace-local` placement.
- **Failure path**
  - Verify rerun does not duplicate `.ace-local` entries or wipe unrelated user-authored content.
- **Success-criterion mapping**
  - `.ace-local` guidance clarity: inspect generated guidance text.
  - Anti-post-processing guidance: inspect generated guidance for direct file-reading instructions.
  - Repeated-run safety: compare first-run and rerun outcomes for additive behavior only.
