# Goal 4 — PR Summary

## Goal

Run `ace-git pr` (or `ace-git status` if PR context unavailable) and capture
output proving command handles PR metadata context gracefully.

## Workspace

Save artifacts to `results/tc/04/`.
## Constraints

- Use only declared scenario tools (`ace-*` and explicit exceptions from `requires.tools`).
- Execute actions and capture evidence only; do not assign PASS/FAIL verdicts.
- Keep all artifacts under `results/tc/04/`.
- Do not write outside the sandbox.
