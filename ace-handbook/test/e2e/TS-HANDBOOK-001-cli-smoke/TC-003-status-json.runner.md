# Goal 3 - Status JSON output

## Goal

Run status in JSON mode and capture output/exit evidence.

## Workspace

Save artifacts to `results/tc/03/`.

Capture:
- `results/tc/03/status-json.stdout`, `.stderr`, `.exit` from `ace-handbook status --provider pi --format json`

## Constraints

- Use only declared scenario tools.
- Capture evidence only; do not assign PASS/FAIL verdicts.
- Keep all artifacts under `results/tc/03/`.
