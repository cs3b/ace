# Goal 2 - Status table output

## Goal

Run status in table mode and capture output/exit evidence.

## Workspace

Save artifacts to `results/tc/02/`.

Capture:
- `results/tc/02/status-table.stdout`, `.stderr`, `.exit` from `ace-handbook status --provider pi`

## Constraints

- Use only declared scenario tools.
- Capture evidence only; do not assign PASS/FAIL verdicts.
- Keep all artifacts under `results/tc/02/`.
