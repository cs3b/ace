# Goal 1 - Sync provider projection

## Goal

Run sync for a single provider and capture user-visible output plus projected filesystem state evidence.

## Workspace

Save artifacts to `results/tc/01/`.

Capture:

- `results/tc/01/sync-pi.stdout`, `.stderr`, `.exit` from `ace-handbook sync --provider pi`

## Constraints

- Use only declared scenario tools.
- Capture evidence only; do not assign PASS/FAIL verdicts.
- Keep all artifacts under `results/tc/01/`.
