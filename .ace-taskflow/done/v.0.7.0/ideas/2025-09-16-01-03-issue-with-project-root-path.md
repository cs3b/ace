so the issue is deeper 

:> [!WARNING]
> 
proabaly detect proejt root needs to be updateds


capture-it "the create-path when run outsite the project root, doesn't work, doesn't lo
ad the config from .coding-agent (and btw. we should reanme this config file also"
Error: System prompt template not found: /Users/mc/Ps/ace-meta/.ace/.ace/handbook/templates/idea-manager/system.prompt.md
