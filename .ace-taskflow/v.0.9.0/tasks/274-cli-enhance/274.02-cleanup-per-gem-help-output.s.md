---
id: v.0.9.0+task.274.02
status: draft
priority: medium
estimate: 4h
dependencies:
- '274'
parent: v.0.9.0+task.274
---

# Unified CLI Help: Per-Gem Cleanup

## Overview

With the core formatter in place (task 274), sweep all 20 dry-cli gems to fix accumulated inconsistencies:
1. **Duplicate command names** in `example` arrays — ace-docs, ace-git-commit, ace-prompt-prep, ace-test
2. **7+ variants** of `--quiet`/`--verbose`/`--debug` descriptions → canonical strings from `standard_options.rb`
3. **Command grouping** for ace-taskflow (20+ commands), ace-models, ace-review
4. **Misleading examples** in ace-taskflow task add-dependency (shows `--depends-on` flag but command uses positional args)
5. **Hide deprecated** `ace-review feedback skip`

## Subtasks

- **01**: Apply all per-gem cleanup across the 20 dry-cli gems

## Execution Order

Depends on 274 (core formatter merged first).

## References

- Plan: `/home/mc/.claude/plans/goofy-sauteeing-owl.md`
- Affected: ace-docs, ace-git-commit, ace-prompt-prep, ace-test (examples); all 20 gems (option descs); ace-taskflow, ace-models, ace-review (grouping)
