---
id: v.0.9.0+task.274.03
status: draft
priority: medium
estimate: 3h
dependencies:
- '274'
parent: v.0.9.0+task.274
---

# Unified CLI Help: Non-dry-cli and Broken Tools

## Overview

Fix the 4 outlier binaries that don't participate in the dry-cli ecosystem:

1. **ace-test-suite** — uses OptionParser; add style-matching helper so its `--help` looks like the dry-cli tools
2. **ace-test-e2e-sh** — shell script; currently treats `--help` as a path argument (FATAL error); add `--help`/`-h` handler
3. **ace-framework** — LoadError at startup (exe points to non-existent file); fix or remove
4. **ace-models groups** — `ace-models cache --help`, `ace-models providers --help` exit code 1 and print to stderr twice

## Subtasks

- **01**: Fix ace-test-suite, ace-test-e2e-sh, ace-framework, ace-models help output

## Execution Order

Can run in parallel with 275. Depends on 274 for visual style reference.

## References

- Plan: `/home/mc/.claude/plans/goofy-sauteeing-owl.md`
- `bin/ace-test-suite` → `ace-test-runner/exe/ace-test-suite`
- `bin/ace-test-e2e-sh` → shell script
- `bin/ace-framework` → broken LoadError
- `ace-support-models/` → ace-models groups
