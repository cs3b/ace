---
id: v.0.9.0+task.206.05
status: pending
priority: high
estimate: 1h
dependencies:
  - v.0.9.0+task.206.04
parent: v.0.9.0+task.206
---

# Migrate ace-prompt to ace-bundle

## Behavioral Specification

### User Experience

- **Input**: ace-bundle is stable and tested
- **Process**: Update ace-prompt to use ace-bundle instead of ace-context
- **Output**: ace-prompt uses ace-bundle, tests pass

### Expected Behavior

1. **Gemspec Update**: `ace-prompt.gemspec` updated
   - `ace-bundle ~> 0.29` dependency
   - Remove ace-context dependency

2. **Requires Updated**: 2 lib files, 4 test files
   - `require 'ace/bundle'` instead of `require 'ace/context'`
   - `Ace::Bundle::` instead of `Ace::Context::`

3. **Version Bump**: 0.13.2 → 0.14.0

4. **Verification**: Tests pass
   - `ace-test ace-prompt` succeeds
   - No ace-context references in lib

### Success Criteria

- [ ] **Gemspec**: ace-bundle dependency, no ace-context
- [ ] **Requires**: All use ace-bundle
- [ ] **Version**: Bumped to 0.14.0
- [ ] **Tests Pass**: `ace-test ace-prompt` succeeds
- [ ] **Clean**: No ace-context references in lib

## Objective

Migrate ace-prompt package to use ace-bundle instead of ace-context.

## Scope of Work

- Update ace-prompt.gemspec
- Update requires (2 lib files, 4 test files)
- Bump version to 0.14.0
- Run tests to verify

## References

- Depends on: v.0.9.0+task.206.04 (ace-bundle must be stable)
- Original exploration found 2 lib files, 4 test files
