---
id: v.0.9.0+task.259.05
status: pending
priority: high
estimate: 3h
dependencies:
- v.0.9.0+task.259.04
parent: v.0.9.0+task.259
---

# E2E Only-Failures Suite Level

## Behavioral Specification

### User Experience
- **Input**: `--only-failures` flag on ace-test-suite-e2e command
- **Process**: Suite orchestrator collects failures by package, runs each with filtered test cases
- **Output**: Only failed test cases re-run across all packages

### Expected Behavior
The ace-test-suite-e2e CLI should accept an `--only-failures` flag that:
1. Scans cache for failed test cases across all packages
2. Groups failures by package and test ID
3. Executes tests with only failed test cases (using FailureFinder)
4. Aggregates results across all packages

This enables re-running only failed test cases across the entire test suite.

### Interface Contract

**CLI Invocation:**
```bash
# Re-run only failed tests across all packages
ace-test-suite-e2e --only-failures

# Re-run only failed tests for specific package
ace-test-suite-e2e ace-lint --only-failures

# Combined with --affected
ace-test-suite-e2e --affected --only-failures
```

**Error Handling:**
- No failures found: Message "No failed test cases found in cache"
- Mutually exclusive with --test-cases: Error message

**Edge Cases:**
- Empty cache: Helpful error message
- Package with no failures: Skip with log message
- Combined with --affected: Apply both filters

### Success Criteria
- [ ] **--only-failures flag works for suite**: Scans all packages
- [ ] **Groups failures by package**: Correct package-to-failures mapping
- [ ] **Passes to individual test commands**: Delegates to test-level filtering
- [ ] **Works with --affected**: Both filters apply correctly
- [ ] **Clear progress messages**: Shows which packages have failures

### Validation Questions
- [ ] **Package scope**: Should it scan all packages or respect package filter?
- [ ] **Test ID scope**: Should it scan all tests or respect test_id filter?
- [ ] **Progress reporting**: How to show progress for filtered execution?

## Objective

Enable suite-level re-running of only failed test cases. Builds on single-package --only-failures to work across multiple packages in suite execution.

## Scope of Work
- **User Experience Scope**: Re-running failed tests via ace-test-suite-e2e
- **System Behavior Scope**: Multi-package failure collection and delegation
- **Interface Scope**: `--only-failures` flag for ace-test-suite-e2e

### Deliverables

#### Behavioral Specifications
- --only-failures flag for suite command
- Failure aggregation by package and test
- Integration with test-level --only-failures
- Progress reporting for filtered suite execution

#### Validation Artifacts
- Multi-package failure collection verification
- Filter interaction testing (--affected, package filters)

## Out of Scope
- ❌ **Single-package only-failures**: Implemented in task 259.04
- ❌ **Test case filtering**: --test-cases functionality
- ❌ **Failed test metadata**: failed_test_cases array (task 259.03)

## References
- Task 259.04: E2E Only-Failures Single Package (depends on this)
- Task 259.03: E2E Failed Test Metadata (provides failed_test_cases)
- E2E Test Case Filtering Implementation Plan (task 255.03)

---

## Implementation Plan

### Planning Steps

* [ ] Analyze SuiteOrchestrator structure and test execution flow
  > TEST: Understanding Check
  > Type: Pre-condition Check
  > Assert: SuiteOrchestrator runs tests via test commands
  > Command: grep -A 30 "class SuiteOrchestrator" ace-test-e2e-runner/lib/ace/test/end_to_end_runner/organisms/suite_orchestrator.rb
* [ ] Analyze how --affected flag works (similar pattern)
* [ ] Design failure aggregation strategy (by package/test)
* [ ] Plan integration with test-level --only-failures

### Execution Steps

- [ ] Update RunSuite CLI command
  - Add --only-failures option (type: boolean)
  - Add validation: mutually exclusive with --test-cases
  - Pass only_failures to SuiteOrchestrator
- [ ] Update SuiteOrchestrator
  - Add only_failures parameter to run() method
  - Add collect_failures_by_package() method
  - Use FailureFinder to scan cache for each package
  - Build map of package → {test_id → [failed_test_cases]}
  - Pass test_cases to individual test commands
- [ ] Update progress reporting
  - Show which packages have failures
  - Skip packages with no failures
  - Log "No failures found for package X"

## Acceptance Criteria

- [ ] AC 1: --only-failures flag added to ace-test-suite-e2e
- [ ] AC 2: Failures collected and grouped by package/test
- [ ] AC 3: Test commands receive only failed test cases
- [ ] AC 4: Works with --affected flag (both filters apply)
- [ ] AC 5: Clear progress messages for filtered execution

## File Modifications

### Modify

- `ace-test-e2e-runner/lib/ace/test/end_to_end_runner/cli/commands/run_suite.rb`
  - Changes: Add --only-failures flag, validation
  - Impact: Users can re-run failed suite-level tests
  - Integration points: SuiteOrchestrator
- `ace-test-e2e-runner/lib/ace/test/end_to_end_runner/organisms/suite_orchestrator.rb`
  - Changes: Add only_failures handling, failure collection, delegation
  - Impact: Suite can re-run only failed tests
  - Integration points: FailureFinder molecule, individual test commands

## Risk Assessment

### Technical Risks
- **Risk:** Failure aggregation may be complex for multiple packages/tests
  - **Probability:** Medium
  - **Impact:** Medium (incorrect test case mapping)
  - **Mitigation:** Use FailureFinder consistently, test with various cache states
  - **Rollback:** Revert SuiteOrchestrator changes

### Integration Risks
- **Risk:** Package filters may interact unexpectedly with --only-failures
  - **Probability:** Low (filters should be independent)
  - **Mitigation:** Document filter interaction, test combinations
  - **Monitoring**: Test with --affected, package filters, --only-failures

## Technical Approach

### Architecture Pattern
- Extend SuiteOrchestrator with only_failures handling
- Use FailureFinder to scan cache per package
- Build package → test → failures map
- Pass failures to individual test commands

### Technology Stack
- Pure Ruby (no new dependencies)
- Reuse FailureFinder molecule
- Follow existing filter pattern (--affected)

### Implementation Strategy
- Add only_failures parameter through chain (CLI → Orchestrator)
- Collect failures before test execution loop
- Modify test invocation to include --test-cases with failures
- Update progress reporting for filtered execution
