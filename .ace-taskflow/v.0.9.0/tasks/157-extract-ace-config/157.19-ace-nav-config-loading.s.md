---
id: v.0.9.0+task.157.19
status: pending
priority: medium
estimate: 1h
dependencies:
- v.0.9.0+task.157.11
parent: v.0.9.0+task.157
---

# ace-nav - Add Config Loading

## Objective

Add config loading pattern to ace-nav. Currently `.ace-defaults/nav/config.yml` exists but the gem doesn't load it.

## Current Issues

- `.ace-defaults/nav/config.yml` has configuration options (cache, fuzzy, output settings)
- Main `lib/ace/nav.rb` has no config loading code
- No `reset_config!` method for test isolation

## Scope of Work

### Files to Modify

- `ace-nav/lib/ace/nav.rb` - Add config loading pattern and reset_config!

## Implementation Plan

### Execution Steps

- [ ] Add config loading to ace-nav.rb following ADR-022 pattern
  > Add `def self.config` method using Ace::Config.create
  > Use resolve_namespace("nav")
  > Add load_gem_defaults fallback

- [ ] Add `reset_config!` method for test isolation

- [ ] Verify config values are accessible
  > cache.enabled, cache.directory, cache.ttl
  > fuzzy.enabled, fuzzy.threshold
  > output.color, output.verbose

- [ ] Run tests with `ace-test ace-nav`

- [ ] Bump patch version

## Acceptance Criteria

- [ ] ace-nav.rb has config loading following ADR-022 pattern
- [ ] reset_config! method available for test isolation
- [ ] Config values from .ace-defaults/nav/config.yml accessible
- [ ] All ace-nav tests pass
- [ ] Gem version bumped
