session:
  name: work-on-task-261-e2e-per-tc
  description: Implement E2E Per-TC infrastructure (261.01-261.04) with PR and 2 code-deep reviews

steps:
  - name: batch-tasks
    number: "010"
    instructions: |
      Batch container for task 261 subtasks (261.01, 261.02, 261.03, 261.04).

      This step auto-completes when all child tasks are done.

      Each task runs in a forked context with full project context.
      After all tasks complete, consolidated review runs.

      Tasks to implement:
      - 261.01: SetupExecutor + ScenarioLoader Infrastructure (4h)
      - 261.02: TC-Level Execution Pipeline (4h)
      - 261.03: Orchestration + Suite Support (5h)
      - 261.04: Remove Legacy .mt.md Support (2h)

      Package: ace-test-e2e-runner
      Branch: 261-e2e-per-tc-infrastructure-implementation

  - name: work-on-261.01
    parent: "010"
    context: fork
    instructions: |
      ## Onboard

      Load context before starting work:
      - Run `ace-bundle project` to load project context
      - Run `ace-taskflow task 261.01` to load task details

      ## Work

      Implement task 261.01 (SetupExecutor + ScenarioLoader Infrastructure) following the specification:
      - Follow project conventions and coding standards
      - Make incremental commits as you progress
      - Run tests after significant changes

      Key deliverables:
      - TestCase model (new)
      - FixtureCopier molecule (new)
      - ScenarioLoader molecule (new)
      - SetupExecutor molecule (new)
      - `ace-test-e2e setup` CLI command (new)
      - Updated TestScenario model with new fields
      - Comprehensive unit tests for all new components

      ## Report

      Return a structured summary:
      - **Task**: 261.01 - SetupExecutor + ScenarioLoader Infrastructure
      - **Status**: completed | partial | blocked
      - **Changes**: list of files modified and what changed
      - **Commits**: commit hashes and messages created
      - **Issues**: any problems encountered or deferred decisions

  - name: work-on-261.02
    parent: "010"
    context: fork
    instructions: |
      ## Onboard

      Load context before starting work:
      - Run `ace-bundle project` to load project context
      - Run `ace-taskflow task 261.02` to load task details

      ## Work

      Implement task 261.02 (TC-Level Execution Pipeline) following the specification:
      - Follow project conventions and coding standards
      - Make incremental commits as you progress
      - Run tests after significant changes

      Key deliverables:
      - Updated TestExecutor with execute_tc method (new)
      - Updated PromptBuilder with build_tc method (new)
      - Updated ResultParser with parse_tc method (new)
      - Updated SkillPromptBuilder with TC-level methods (new)
      - Updated SkillResultParser with parse_tc method (new)
      - Updated ReportWriter for per-TC reports
      - Updated run-e2e-test.wf.md with TC-level execution mode
      - Comprehensive unit tests

      Important: All new methods are additive - existing methods unchanged for backward compatibility.

      ## Report

      Return a structured summary:
      - **Task**: 261.02 - TC-Level Execution Pipeline
      - **Status**: completed | partial | blocked
      - **Changes**: list of files modified and what changed
      - **Commits**: commit hashes and messages created
      - **Issues**: any problems encountered or deferred decisions

  - name: work-on-261.03
    parent: "010"
    context: fork
    instructions: |
      ## Onboard

      Load context before starting work:
      - Run `ace-bundle project` to load project context
      - Run `ace-taskflow task 261.03` to load task details

      ## Work

      Implement task 261.03 (Orchestration + Suite Support) following the specification:
      - Follow project conventions and coding standards
      - Make incremental commits as you progress
      - Run tests after significant changes

      Key deliverables:
      - Updated TestDiscoverer with find_scenarios method (new)
      - Updated TestOrchestrator with TS-format execution paths
      - Updated SuiteOrchestrator with TC-level queue
      - Updated display managers for TC-level display (Simple, Progress, SuiteSimple, SuiteProgress)
      - Updated FailureFinder verification (no changes expected)
      - Updated RunTest and RunSuite CLI commands
      - Comprehensive unit tests

      Important: Maintain dual-mode support (MT and TS formats) during transition.

      ## Report

      Return a structured summary:
      - **Task**: 261.03 - Orchestration + Suite Support
      - **Status**: completed | partial | blocked
      - **Changes**: list of files modified and what changed
      - **Commits**: commit hashes and messages created
      - **Issues**: any problems encountered or deferred decisions

  - name: work-on-261.04
    parent: "010"
    context: fork
    instructions: |
      ## Onboard

      Load context before starting work:
      - Run `ace-bundle project` to load project context
      - Run `ace-taskflow task 261.04` to load task details

      IMPORTANT: This task MUST NOT begin until all 262.* migration tasks are complete.

      ## Work

      Implement task 261.04 (Remove Legacy .mt.md Support) following the specification:
      - Follow project conventions and coding standards
      - Make incremental commits as you progress
      - Run tests after significant changes

      Key deliverables:
      - Delete ScenarioParser and its test file
      - Remove all MT-format code paths from:
        - TestScenario model (MT- short_id pattern, scenario_format method)
        - TestDiscoverer (.mt.md discovery)
        - TestOrchestrator (run_single_test, run_package_tests)
        - SuiteOrchestrator (MT-format queue items, extract methods)
        - TestExecutor (old execute method)
        - PromptBuilder (old build method)
        - ResultParser (old parse method)
        - SkillPromptBuilder (old methods)
        - SkillResultParser (old parse method)
        - Display managers (.mt.md basename logic)
        - CLI commands (ScenarioParser references)
      - Update config.yml (MT- patterns -> TS- patterns)
      - Update all test files to use TS-format test data
      - Update handbook documentation
      - Comprehensive grep verification (no legacy references remain)

      ## Report

      Return a structured summary:
      - **Task**: 261.04 - Remove Legacy .mt.md Support
      - **Status**: completed | partial | blocked
      - **Changes**: list of files modified and what changed
      - **Commits**: commit hashes and messages created
      - **Issues**: any problems encountered or deferred decisions

  - name: consolidated-review
    number: "020"
    skill: ace:review
    instructions: |
      Review all batch changes using code-deep preset.

      Run: `ace-review --preset code-deep`

      Review focus:
      - Cross-task consistency (naming, patterns, style)
      - Integration points between tasks (261.01 → 261.02 → 261.03 → 261.04)
      - Test coverage across all changes
      - Any shared code or refactoring opportunities
      - ATOM architecture compliance
      - Backward compatibility handling (dual-mode code)

      Tasks reviewed: 261.01, 261.02, 261.03, 261.04

  - name: finalize
    number: "030"
    skill: ace:commit
    instructions: |
      Commit any remaining uncommitted changes with a batch summary.

      If there are changes:
      - Create commit summarizing the batch work
      - Reference all task IDs in commit message (261.01, 261.02, 261.03, 261.04)

      Tasks completed: 261.01, 261.02, 261.03, 261.04

  - name: create-pr
    number: "040"
    skill: ace:create-pr
    instructions: |
      Create a pull request for the changes.

      PR title should reference task 261 and summarize the work.

      Capture the PR number for subsequent review steps.
      Update the next steps with the PR number.

  - name: review-cycle-1
    number: "050"
    skill: ace:review-pr
    instructions: |
      Review the code (iteration 1 of 2).

      Focus on correctness and architecture.

      Use code-deep preset for thorough review.

  - name: apply-feedback-1
    number: "060"
    instructions: |
      Apply feedback from review (medium+ priority).
      Commit changes with descriptive messages.

  - name: review-cycle-2
    number: "070"
    skill: ace:review-pr
    instructions: |
      Review the code (iteration 2 of 2).

      Focus on edge cases, error handling, and test coverage.

      Use code-deep preset for thorough review.

  - name: apply-feedback-2
    number: "080"
    instructions: |
      Apply final feedback from review.
      Commit changes.

  - name: finalize-pr
    number: "090"
    skill: ace:update-pr-desc
    instructions: |
      Update PR description with implementation summary.
      Push all changes to remote.
      Ensure PR is ready for human review.
