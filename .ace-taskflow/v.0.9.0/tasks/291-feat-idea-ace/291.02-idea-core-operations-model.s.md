---
id: v.0.9.0+task.291.02
status: pending
priority: medium
estimate: TBD
dependencies:
  - v.0.9.0+task.291.01
parent: v.0.9.0+task.291
---

# ace-idea Core Operations and Model

## Note

This subtask was auto-created during orchestrator conversion. Content merged into 291.01. See 291.01 for the core operations spec. This subtask can be repurposed or removed during planning.

## Repurposed: Integration Tests and Edge Cases

### Expected Behavior

After 291.01 delivers core operations, validate with integration tests and handle edge cases:

1. End-to-end roundtrip: create idea → load → update → move → verify
2. Clipboard edge cases: empty clipboard, binary content, very large paste
3. LLM enhancement failure fallback (when LLM unavailable, gracefully fallback to raw text)
4. Concurrent creation (two ideas within 1.85s b36ts window)
5. Deep nesting scan performance with large idea collections

### Success Criteria

- [ ] Integration test: create → show → update → move → list roundtrip
- [ ] Clipboard graceful degradation when no clipboard tool available
- [ ] LLM enhance falls back to raw content when LLM call fails
- [ ] Duplicate b36ts IDs detected and handled (append counter or warn)
- [ ] Performance acceptable with 500+ ideas across nested folders
