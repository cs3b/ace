---
id: v.0.9.0+task.278.08
status: draft
priority: medium
estimate: TBD
dependencies:
  - v.0.9.0+task.278.02
parent: v.0.9.0+task.278
---

# ace-git-worktree: migrate to standard help pattern

## Scope

Migrate ace-git-worktree from DefaultRouting to standard multi-command help pattern.

**Binary:** `ace-git-worktree`
**CLI module:** `ace-git-worktree/lib/ace/git/worktree/cli.rb`
**Pattern:** multi-command (registry)
**Current commands:** create, list (alias ls), switch (alias cd), remove (alias rm), prune, config
**Current routing:** DefaultRouting — clean delete, no custom logic. Has `COMMAND_ALIASES = %w[ls cd rm]`.

## Code Changes

- [ ] Remove `extend Ace::Core::CLI::DryCli::DefaultRouting`
- [ ] Remove `KNOWN_COMMANDS`, `DEFAULT_COMMAND`, `BUILTIN_COMMANDS`, `COMMAND_ALIASES` constants
- [ ] Remove `start()` method
- [ ] Add `HELP_EXAMPLES` constant
- [ ] Register HelpCommand for `--help` and `-h`
- [ ] Update exe/ wrapper to use `Dry::CLI.new(CLI).call` directly
- [ ] No args → show help

## HELP_EXAMPLES (curate)

```ruby
HELP_EXAMPLES = [
  ["Create worktree for task", "ace-git-worktree create 148"],
  ["List active worktrees", "ace-git-worktree list"],
  ["Switch to worktree", "ace-git-worktree switch 148"],
  ["Remove worktree", "ace-git-worktree remove 148"],
  ["Prune stale worktrees", "ace-git-worktree prune"],
].freeze
```

## Update References

- [ ] Update workflow files, guides, docs for ace-git-worktree CLI patterns

## Acceptance Criteria

- [ ] `ace-git-worktree --help` shows formatted help, exits 0
- [ ] `ace-git-worktree` (no args) shows help
- [ ] Command aliases (ls, cd, rm) still work via dry-cli alias registration
- [ ] `ace-test` passes in ace-git-worktree
