---
id: v.0.9.0+task.126.01
status: draft
priority: medium
estimate: 8h
dependencies: []
parent: v.0.9.0+task.126
---

# Multi-Model Execution

## Scope

Run a single review preparation against multiple LLM models concurrently with progress visibility.

## User Experience

```bash
# Multiple models via comma-separated list
ace-review --preset pr --model "gemini,gpt-4,claude" --auto-execute

# Multiple --model flags
ace-review --preset pr --model gemini --model gpt-4 --auto-execute

# Via preset configuration
# .ace/review/presets/multi-model.yml
# models:
#   - google:gemini-2.5-flash
#   - openai:gpt-4
```

## Expected Output

```
Preparing review...
✓ Preset loaded: pr
✓ Subject extracted: origin/main...HEAD (15 files)
✓ Prompts composed

Executing reviews (3 models):
  ⏳ gemini-2.5-flash: querying...
  ⏳ gpt-4: querying...
  ⏳ claude-sonnet: querying...
  ✓ gemini-2.5-flash: complete (2.3s)
  ✓ gpt-4: complete (4.1s)
  ✓ claude-sonnet: complete (3.8s)

Reviews saved:
  .cache/ace-review/sessions/review-20251201-175500/
  ├── review-gemini-2.5-flash.md
  ├── review-gpt-4.md
  └── review-claude-sonnet.md
```

## Deliverables

- [ ] CLI parses multiple `--model` values (comma-separated or multiple flags)
- [ ] Preset schema supports `models:` array
- [ ] Concurrent LLM execution with progress display
- [ ] Fault isolation - one model failure doesn't abort others
- [ ] Per-model report files: `review-{model-slug}.md`
- [ ] Session metadata includes all model results and timings
- [ ] Tests for CLI parsing, concurrent execution, fault isolation
- [ ] Documentation updates (README, usage.md)

## Acceptance Criteria

- [ ] `ace-review --model "a,b,c"` executes reviews against all specified models
- [ ] `ace-review --model a --model b` works equivalently
- [ ] Preset with `models: [a, b]` uses all listed models
- [ ] Progress is displayed for each model during execution
- [ ] One model failing doesn't prevent other models from completing
- [ ] Each model's report is saved with model-identifying filename

## Files to Modify

- `ace-review/lib/ace/review/cli.rb` - Multi-model parsing
- `ace-review/lib/ace/review/molecules/preset_manager.rb` - `models:` array support
- `ace-review/lib/ace/review/organisms/review_manager.rb` - Concurrent execution orchestration
- `ace-review/lib/ace/review/molecules/llm_executor.rb` - New `execute_batch()` method

## Notes

- Consider concurrency limit configuration
- Deduplicate model list if same model specified multiple times
- Validate model names before execution
