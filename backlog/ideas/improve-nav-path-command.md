We should have --dryrun that will work as it works now

But without --dryrun we should create a file / directory to user request for

move nav-path file to new cmd search-project file|dir|content|all <query> and implement it as full fuzzy search with matching index
