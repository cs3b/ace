wheever is the current path that we run then, they always behave in predictable way (know how to find project root / dev-taskflow, dev-tools / etc )

we might consider adding them to PATH - so they are accesible everywhere (just for the session -> bin/setup-env)
