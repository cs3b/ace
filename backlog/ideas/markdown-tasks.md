- linting markdown document
- checking the links format (and correct path)


# Improvements

## add to bin/lint summary - when we show how many errrors i warnings are in each lint part

- consider writing errors as file - tasks one per file - [ ] path \n erorrs
- we should return only few files to edit ( up to first 5 files to fix) - to not overload the context (so it will
  be actionable) and we can run tool another time
- in summary show list of all files
