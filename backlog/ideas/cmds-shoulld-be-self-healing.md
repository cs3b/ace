when some use the in the wrong way the errors should be more then helpful

e.g.:
- when there is not model for llm-query, it should try to search which one can fit and return list of models and aliases to worth autocorrect

- when there is no release as required for next task, it should show list of available release folders
- we should also provide more context, then less, about paths and so on
