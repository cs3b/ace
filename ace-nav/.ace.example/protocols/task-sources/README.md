# Task Protocol Sources

The task protocol is a special protocol that doesn't use the standard source mechanism.
Instead, it uses the TaskResolver with search paths defined in the protocol itself.

Task sources are configured directly in task.yml via the search_paths configuration.