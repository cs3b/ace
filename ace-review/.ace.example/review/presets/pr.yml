# Pull request review - comprehensive code changes review

description: "Pull request review - comprehensive code changes review"

prompt_composition:
  base: "prompt://base/system"
  format: "prompt://format/standard"
  guidelines:
    - "prompt://guidelines/tone"
    - "prompt://guidelines/icons"

context: "project"

subject:
  # New: Use diff key for consistent filtering via ace-git-diff
  # Respects global .ace/diff/config.yml configuration
  diff:
    ranges:
      - "origin/main...HEAD"

  # Alternative: Use commands for custom git operations
  # commands:
  #   - "git diff origin/main...HEAD"
  #   - "git log origin/main..HEAD --oneline"
