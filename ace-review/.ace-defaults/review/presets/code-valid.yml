# Valid — correctness review mapped to narrow reviewer lanes

description: "Correctness review - does the code work correctly?"
pipeline: phased-valid
providers:
  llm: [review-fast]
  tools_lint: [lint]
