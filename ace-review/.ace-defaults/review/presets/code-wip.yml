# Work-in-progress review - extends code.yml with WIP-specific subject

presets:
  - code

description: "Work-in-progress review - review uncommitted changes"

subject:
  bundle:
    sections:
      code_changes:
        title: "Working Changes"
        description: "Uncommitted code changes to review"
        commands:
          - "git diff HEAD"
