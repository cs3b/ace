#!/usr/bin/env ruby
# frozen_string_literal: true

require "bundler/setup"
require "ace/support/nav"

# Start CLI and exit with the returned status code
result = Ace::Support::Nav::CLI.start(ARGV)
exit(result.is_a?(Integer) ? result : 0)
