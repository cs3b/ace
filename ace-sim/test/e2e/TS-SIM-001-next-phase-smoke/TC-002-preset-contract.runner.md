# Goal 2: Preset and Step Config Contract

## Objective
Capture evidence that default preset and step bundle files exist in expected locations.

## Steps
1. List `ace-sim/.ace-defaults/sim/presets` into `results/tc/02/presets.ls`.
2. List `ace-sim/.ace-defaults/sim/steps` into `results/tc/02/steps.ls`.
3. Print `ace-sim/.ace-defaults/sim/presets/validate-idea.yml` into `results/tc/02/validate-idea.yml`.
4. Print each step file (`draft.md`, `plan.md`, `work.md`) into corresponding files under `results/tc/02/`.
