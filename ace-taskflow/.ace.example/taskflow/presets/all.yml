description: "All tasks in current release (all statuses)"
type:  # Universal preset
context: "current"
filters:
  glob: ["ideas/**/*.s.md", "tasks/**/*.s.md"]  # All items in ideas and tasks dirs
sort:
  by: "context"
  ascending: true
display:
  group_by: "context"