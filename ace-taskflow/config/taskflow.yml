taskflow:
  idea:
    directory: "./ideas"
    template: |
      # Idea

      %{content}

      ---
      Captured: %{timestamp}
    timestamp_format: "%Y-%m-%d %H:%M:%S"
  task:
    directory: "./tasks"
  release:
    directory: "./releases"