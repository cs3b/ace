#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative "../lib/ace/taskflow"

# The CLI.start method now handles:
# 1. Cache clearing at start
# 2. Default command routing (unknown args -> "task" command)
# 3. Proper exit code handling
exit_code = Ace::Taskflow::CLI.start(ARGV)
exit(exit_code.is_a?(Integer) ? exit_code : 0)
