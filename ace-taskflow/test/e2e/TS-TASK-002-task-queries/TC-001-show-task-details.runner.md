# Goal 1 — Show Task Details

## Goal

Create a task, then run `ace-task show {id}` and capture full detail output,
including frontmatter and body sections.

## Workspace

Save artifacts to `results/tc/01/`.
