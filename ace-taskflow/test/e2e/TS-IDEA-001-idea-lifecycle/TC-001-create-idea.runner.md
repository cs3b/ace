# Goal 1 — Create Idea

## Goal

Create an idea with `ace-idea create` and capture command output, created idea
reference, and any generated content details.

## Workspace

Save artifacts to `results/tc/01/`.
