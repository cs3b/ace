# Goal 2 — Start Task

## Goal

Use the task ID from Goal 1 and run `ace-task start {id}`. Capture output and
post-start task details that confirm transition to `in-progress`.

## Workspace

Save artifacts to `results/tc/02/`.

## Constraints

- Read task ID from Goal 1 artifacts.
- Capture command stdout, stderr, and exit code.
- Capture `ace-task show {id}` output after starting.
