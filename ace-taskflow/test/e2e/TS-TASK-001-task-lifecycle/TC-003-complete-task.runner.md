# Goal 3 — Complete Task

## Goal

Run `ace-task done {id}` for the same task and verify the state transitions to
`done` using `ace-task show {id}` output.

## Workspace

Save artifacts to `results/tc/03/`.

## Constraints

- Reuse the task ID from earlier goals.
- Capture done/show command outputs and exit codes.
- Keep evidence focused on status transition.
