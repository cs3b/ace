# Goal 4 — List Tasks

## Goal

Run `ace-task list --all --status done --include-drafts` and capture output that
includes the task completed in Goal 3.

## Workspace

Save artifacts to `results/tc/04/`.

## Constraints

- Use the same task reference tracked across goals.
- Capture list command stdout/stderr/exit code.
- Save a short note pointing to the matching output line.
