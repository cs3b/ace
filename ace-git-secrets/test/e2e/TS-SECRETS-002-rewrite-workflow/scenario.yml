test-id: TS-SECRETS-002
title: Rewrite Workflow
area: git-secrets
package: ace-git-secrets
priority: high
tags: [happy-path, "use-case:secrets"]
requires:
  tools: [ace-git-secrets, git, gitleaks, git-filter-repo]
  ruby: ">= 3.0"

setup:
  - git-init
  - copy-fixtures
  - run: git add -A && git commit -m "initial" --quiet
  - env:
      PROJECT_ROOT_PATH: .
