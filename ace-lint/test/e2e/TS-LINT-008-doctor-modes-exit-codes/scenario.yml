test-id: TS-LINT-008
title: Doctor Modes and Exit Codes
area: lint
package: ace-lint
priority: high
requires:
  tools: [ace-lint]
  ruby: ">= 3.0"

setup:
  - copy-fixtures
  - run: git -C syntax-error init --quiet .
  - env:
      PROJECT_ROOT_PATH: "."
