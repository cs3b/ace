---
# WFI Sources Protocol Configuration for ace-integration-claude gem
# This enables workflow discovery from the installed ace-integration-claude gem

name: ace-integration-claude
type: gem
description: Workflow instructions from ace-integration-claude gem
priority: 10

# Configuration for workflow discovery within the gem
config:
  # Relative path within the gem (default: handbook/workflow-instructions)
  relative_path: handbook/workflow-instructions

  # Pattern for finding workflow files (default: *.wf.md)
  pattern: "*.wf.md"

  # Enable discovery
  enabled: true