description: "Items with no urgency (anyday priority)"
# Universal preset - works for both ideas and tasks
context: "current"
filters:
  glob: ["anyday/**/*.s.md"]  # Keep glob for backward compatibility
sort:
  by: "created_at"
  ascending: false
display: {}
