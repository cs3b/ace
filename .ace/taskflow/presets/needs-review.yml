description: "Items requiring review (needs_review flag set)"
# Universal preset - works for both ideas and tasks
context: "current"
filters:
  glob: ["**/*.s.md"]
  metadata:
    needs_review: true
sort:
  by: "priority"
  ascending: true
display:
  group_by: null
