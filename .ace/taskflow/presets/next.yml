description: "Next actionable tasks (pending + in-progress)"
type: "tasks"
release: "current"
filters:
  status:
    - "pending"
    - "in-progress"
sort:
  by: "sort"
  ascending: true
display:
  group_by: null