description: "All tasks in current release (all statuses)"
type:  # Universal preset
release: "current"
filters:
  glob: ["**/*.s.md"]  # All items (folder prefix added by PresetManager based on context)
sort:
  by: "sort"
  ascending: true
display: {}