description: "Items for future consideration (maybe priority)"
# Universal preset - works for both ideas and tasks
release: "current"
filters:
  # Prefer configured default (_maybe), but include legacy maybe/ for compatibility.
  glob: ["_maybe/**/*.s.md", "maybe/**/*.s.md"]
sort:
  by: "created_at"
  ascending: false
display: {}
