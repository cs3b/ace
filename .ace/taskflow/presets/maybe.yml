description: "Items for future consideration (maybe priority)"
# Universal preset - works for both ideas and tasks
release: "current"
filters:
  glob: ["maybe/**/*.s.md"]  # Items in maybe subdirectory (folder prefix added by PresetManager)
sort:
  by: "created_at"
  ascending: false
display: {}
