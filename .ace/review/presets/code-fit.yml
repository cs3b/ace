# Fit — quality/architecture review mapped to narrow reviewer lanes

description: "Quality review - is the code well-structured and maintainable?"
partition: by-concern
pipeline: phased-fit
providers:
  llm: [fast]
  tools_lint: [lint]
