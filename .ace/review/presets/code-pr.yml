# Pull request review - extends code.yml with PR-specific subject

presets:
  - code

description: "Pull request review - comprehensive code changes review"

# Note: No default subject since this preset is designed to work with --pr flag
# When used with --pr, the system will automatically create PR-specific subject configuration
