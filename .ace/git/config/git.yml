git:
  conventions:
    format: conventional
    scopes:
      enabled: true
      detect_from_paths: true
      custom:
        - core
        - context
        - git
        - taskflow
        - test-runner
        - test-support
  intentions:
    default: feat
  signature:
    enabled: false  # No AI signature for now