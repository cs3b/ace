#!/usr/bin/env ruby
# frozen_string_literal: true

require "ace/prompt_prep"

# Start dry-cli with exception-based exit code handling (per ADR-023)
begin
  Ace::PromptPrep::CLI.start(ARGV)
rescue Ace::Core::CLI::Error => e
  warn e.message
  exit(e.exit_code)
end
