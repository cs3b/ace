name: split-subtree-root
description: Orchestrate subtree delegation and execution for split parent phases

instructions:
  common:
    - "Subtree root orchestrator phase."
    - "This phase is orchestration-only."
    - "Do not execute {{source_skill}} directly in this parent phase."
    - "Child phases: {{sub_phases}}."
  fork:
    - "Delegate this subtree into forked context:"
    - "- ace-assign fork-run --assignment <assignment-id>@{{parent_number}}"
    - "Inside the forked agent, run /ace-assign-drive for this subtree scope."
    - "Note: fork-run may take 10-30 minutes. If your environment has bash timeout limits, run in background and poll ace-assign status every 5 minutes."
    - "After fork-run completes: read all subtree report files before continuing. The driver is the guard — review reports to verify quality before advancing."
  inline:
    - "Run /ace-assign-drive and execute only child phases under this node."

goal_header: "Goal to satisfy through child phases:"

effort: low
tags: [orchestration, fork, split-phase]
