session:
  name: fork-scope-isolation
  description: Verify fork-run executes only requested subtree

steps:
  - name: precheck
    instructions: |
      Create precheck marker in repository root.

  - name: subtree-a
    context: fork
    instructions: |
      Execute subtree A in forked context only.
    sub_steps:
      - onboard
      - plan-task
      - work-on-task

  - name: postcheck
    instructions: |
      Create postcheck marker in repository root.
