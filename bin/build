#!/bin/sh
# Wrapper for project building (if applicable).
# This project (coding-agent-workflow-toolkit-meta) primarily consists of
# documentation and workflow definitions, and does not have a traditional build step.
# This script serves as a placeholder.

set -e
cd "$(dirname "$0")"/.. # Ensure execution from project root

echo "INFO: Running 'bin/build' from project root: $(pwd)"
echo "INFO: This is 'bin/build' for the 'coding-agent-workflow-toolkit-meta' project."
echo "INFO: This project does not have a dedicated build step."
echo "INFO: This script is a placeholder and will exit successfully."

# No build command is applicable for this project.

echo "INFO: No build command configured in bin/build (as expected for this project)."
exit 0